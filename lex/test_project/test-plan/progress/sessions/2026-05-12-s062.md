---
date: 2026-05-12
clusters: [10]
tests_added: "7 (10.17–10.23)"
suite_tally: "7 pass of 7; +7 scenarios on the customer-notifications + Keycloak-roles SDK"
---

(migrated session 62)

**Sub-cluster 10h landed — `LexAPI` outbound-client SDK (ROI batch #5 of the May 12 coverage sweep).** Tiny file (38 stmts / 31 missed, 14.58% baseline) — fastest +0.3% project-wide win on the ROI list. The SDK customer apps (and framework internals) call to reach back into the lex platform's central email + Keycloak APIs from inside a worker or a customer task: `send_email(...)`, `get_client_roles(...)`, `build_attachments_from_paths(...)` for inline attachments. Regression in `send_email` that drops `attachments` or flips the auth header silently breaks the entire customer-notifications surface; the `DEPLOYMENT_ENVIRONMENT` env-var no-op is the documented "your test runs don't blast real emails to customers" guard. **TestCluster10h_BuildAttachments** (10.17 known mimetype `.pdf` → `application/pdf` + base64 round-trip reconstructs original bytes + missing extension `anonymous_blob` → `application/octet-stream` documented fallback — silent drop to `None` would crash every downstream MIME parser). **TestCluster10h_SendEmail** (10.18 `DEPLOYMENT_ENVIRONMENT` blank → silent no-op never POSTs — regression here means every CI run sends production-ish email to customers, the kind of bug you only learn about from a screenshot; 10.19 happy path: URL `https://<DOMAIN>/api/send_email/` + `Authorization: Api-Key <key>` + JSON body with `subject`/`emails`/`body`, returns response.json(), and pin: NO `attachments` key when none supplied so the platform service doesn't reject a payload with a stray empty list; 10.20 `attachments` only included when truthy — empty list still absent, populated list passes through verbatim; 10.21 non-200 raises so callers route through their error handler instead of silently believing the email landed). **TestCluster10h_GetClientRoles** (10.22 happy path: `params={'keycloak_client_id': ...}` querystring placement — the platform's GET handler ignores body data, drift here breaks every Keycloak-roles read; 10.23 non-200 raises — a regression that returned `[]` on 403 would silently grant zero-role access, locking customers out of every protected page without any visible error). All scenarios use `unittest.mock.patch.object(LexAPI.requests, 'post'/'get', ...)` so no network ever hits the wire. **Run summary:** 7 tests, 0 fail, 0 skip, 0.005s. Suite total scenarios: 548 → **555 planned**, 548 → **555 implemented**, 527 → **534 passing**. Cluster 10 dashboard +1 sub-cluster row (10h).
