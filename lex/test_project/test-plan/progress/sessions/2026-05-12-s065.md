---
date: 2026-05-12
clusters: [10]
tests_added: "10 (10.24–10.31 + 10.31b/c)"
suite_tally: "10 pass of 10; +10 scenarios on the `/api/<model>/fields/` schema endpoint the React form layer reads"
---

(migrated session 65)

**Sub-cluster 10i landed — `Fields` APIView dispatch + `create_list_ui_info` helper (ROI batch #7 of the May 12 coverage sweep — extension to 10e).** 10e (Sessions 18 + 20) had covered `create_field_info` purely as a unit helper; the `/api/<model>/fields/?serializer=…` request handler itself + the small `create_list_ui_info` companion helper feeding it were both still dark. `lex/api/views/model_info/Fields.py` 33.68% baseline (75 stmts / 45 missed). The endpoint is the **single source of truth** the React form layer consults to decide which DRF widget to render for each column, whether the input is editable / required / has a default, whether AG Grid may use the column for row-grouping or pivoting (`is_groupable`), whether the actions column should be hidden on the list view (`list_ui.hide_actions_column`), and which serializer alternates exist (the `?serializer=…` dispatch surface). A regression in any of these branches silently mis-renders a form or, worse, hides actions an admin needed to fix bad data. **`SimpleTestCase`-only** — model_container / model._meta / DRF fields all `MagicMock` so no DB, no router, no real serializer round-trip; whole batch runs in 0.007s. 10 scenarios across three classes plus a small belt-and-braces type-map sanity class. **TestCluster10i_CreateListUIInfo** (10.24 `get_list_ui_options` classmethod takes priority over `Meta.hide_actions_column` so platform-shipped custom toolbars survive — drift to "Meta wins" semantics would silently strip every custom toolbar button; 10.25 `Meta.hide_actions_column` reflected for True/False, missing Meta → False default — drift would hide actions for every model that hasn't opted in explicitly). **TestCluster10i_FieldsView** (10.26 unknown `?serializer=…` raises `APIException` with both `error` (containing model name + offending serializer name) and `available` (list of valid keys) so the frontend can surface a usable diagnostic — a refactor that drops the `available` key would force the UI to a generic toast; 10.27 back-compat: container without `get_serializers_map()` falls back to `.serializers_map` attribute — regression that hard-required the getter would 500 every legacy-container request without a clear error; 10.28 Django model field path (real-looking IntegerField via `get_field` mock) emits `is_groupable=True` so AG Grid SSRM `qs.values(field).annotate(...)` can light up — drift here disables grouping on every column the user actually wants to group by, a quiet UX regression that only an investigator who knows AG Grid would catch; 10.29 `get_field` raises → DRF-only fallback path with `is_groupable=False` (SerializerMethodField / computed properties have no underlying Django column, the SSRM `_execute_group_level` / `_execute_pivot_mode` paths cannot group/pivot on them — flag prevents an empty grid when the user toggles row-group on a computed column), DRF type mapping (`FloatField → "float"`), `empty` sentinel → None, `read_only=True → editable=False`; 10.30 `ID_FIELD_NAME` / `SHORT_DESCR_NAME` stripped from response (internal-only fields would otherwise surface as duplicate or confusing form columns), `Meta.lex_field_type_overrides={"notes": "markdown"}` beats auto-derived type — drift that ignored overrides would silently swap the editor widget back, plus bare class object as `default` (`int`) coerces to None — otherwise serialises as `<class 'int'>` and breaks the form; 10.31 `PrimaryKeyRelatedField` (DRF-only) → `target` set to `queryset.model._meta.model_name` so the autocomplete picker renders right (without it the dropdown shows a free-text input and lets users save garbage IDs), defensive try/except: queryset that raises drops `target` silently, doesn't 500 the whole `/fields/` response — exercised by attaching a `property` that raises on access). **TestCluster10i_TypeMaps** (10.31b `DJANGO_FIELD2TYPE_NAME` covers ForeignKey / Integer / Float / Boolean / Date — drift canary against silent dict-key rename; 10.31c `DRF_FIELD2TYPE_NAME` covers Integer / Decimal / Char / PrimaryKeyRelated / JSON + `DEFAULT_TYPE_NAME == "string"`). **One mid-session fix:** initial draft of 10.31 used a `_fake_drf_field(queryset_model_name="category")` helper without setting `label`, which falls back to None in the helper; the view's `getattr(drf_field, "label", fname).title()` then crashed on `None.title()`. Fixed by passing `label="Category"` explicitly. **Run summary:** 10 tests, 0 fail, 0 skip, 0.007s. Suite total scenarios: 584 → **594 planned**, 584 → **594 implemented**, 563 → **573 passing**. Cluster 10 dashboard +1 sub-cluster row (10i).
