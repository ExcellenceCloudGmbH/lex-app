---
date: 2026-04-25
clusters: [1]
tests_added: "8"
suite_tally: "8 pass of 8; cross-file CLI ↔ PyCharm contract now gated"
---

(migrated session 40)

**Sub-cluster 1m landed — `lex` CLI ↔ PyCharm `.run.xml` cross-file contract.** Closes the long-standing drift risk between `generate_pycharm_configs.py` (declares 16 `.run/*.run.xml` files an operator can click in PyCharm) and `lex/bin/lex.py` (declares the actual CLI subcommands). 1a covered three of the sixteen files; the per-builder helpers in `lex/tests/unit/cli/test_lex_cli.py` validate Celery / Flower / MCP individually; nothing asserted "every PyCharm-clickable subcommand actually resolves through the CLI." A rename in either file would silently break a PyCharm action — caught only by a developer trying to use it. New `test_1m_cli_commands.py` (`SimpleTestCase`-only, fast, in-process, no subprocess) lands 8 scenarios across 5 classes: **TestCluster01m_RunFileSetParity** (1.102 — scaffolding produces exactly the 16 expected `.run.xml` files, no orphans, no missing — canary against "I removed Test_Audit but the constant still has it" drift); **TestCluster01m_RunFileScriptName** (1.103 — every `SCRIPT_NAME` is `lex` so a copy-paste of a different binary cannot bypass our env handling / Django bootstrap); **TestCluster01m_RunFileSubcommandResolution** (1.104 — every first-token of `PARAMETERS` resolves to either an explicit `@lex.command(...)` Click handler or a registered Django management command via `_install_dynamic_commands`; uses `shlex.split` to handle quoted-prompt prefixes; this is the cross-file contract that nothing else asserts); **TestCluster01m_ExplicitClickCommands** (1.105 — pins the explicit Click registry: `celery` / `celery-workers` / `flower` / `streamlit` / `start` / `setup` / `setup-with-ai` / `ai-update` / `ai-faq` — removing one is what would silently break a `.run.xml`; 1.106 — `_SKIP_BOOTSTRAP_COMMANDS` allow-list is a subset of the explicit registry, otherwise listed names silently fall through to dynamic forwarding); **TestCluster01m_HelpInvocability** (1.107 — `lex --help` exits 0 and names every explicit command; 1.108 — `lex <cmd> --help` exits 0 for every explicit handler via Click's in-process `CliRunner` — catches decorator/signature regressions that `--help` surfaces but real runs would mask); **TestCluster01m_DjangoSideContract** (1.109 — every Django-side subcommand a `.run.xml` references — `init` / `migrate` / `makemigrations` / `flush` / `test` / `create_db` — is registered with Django's command loader; otherwise the dynamic-forwarding fall-through to `execute_from_command_line` produces a less-helpful error). Implementation note: the test scaffolds `.run/` into a `TemporaryDirectory` per scenario so the live project's `.run/` (which the user has stagged-modified files in) is not touched. All 8 pass in 0.020s. Suite total 304 → 312 scenarios; passing 269 → 277.
