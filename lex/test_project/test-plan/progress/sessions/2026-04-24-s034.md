---
date: 2026-04-24
clusters: [8]
tests_added: "9"
suite_tally: "9 pass of 9; exhaustive WFT/FF scope-contract coverage per docs/lex_topics/12-celery-async-dispatch.md"
---

(migrated session 34)

**Sub-cluster 8i landed — `WaitForTasks` / `FireAndForget` scope contracts.** User flagged that FF and WFT deserved "heavy" coverage across the nested + filter + no-op scenarios documented in `docs/lex_topics/12-celery-async-dispatch.md` — 8g/8h had only exercised the headline priority case (FF wins over WFT). This sub-cluster closes every other branch in the 60-line ContextVar state machine: nesting, filters, no-op pass-through, and exception propagation. Nine scenarios across six classes in `test_8i_scope_contracts.py`, split between real eager execution (for anything observing `is_calculated` after a real task body ran) and ContextVar inspection with a fake task (for pure routing decisions made by `EnhancedBoundTaskMethod.__call__` before `.delay` would ever fire). **TestCluster08i_FFInsideWFT** — **8.22** (FF nested inside WFT, all three scenarios: parent-A / FF / parent-B — the FF result lands on the inner FF scope, the two parents land on the outer WFT, and at no point does the FF result appear on the outer WFT's `dispatched_results`; after exit every row reaches SUCCESS because eager ran them all inline). **TestCluster08i_NestedWFT** — **8.23** (outer WFT wrapping an inner WFT — inner exit drains only the inner's own results; outer's result survives the inner scope boundary and is drained on outer exit; neither WFT steals the other's result; the entire composability contract in one test). **TestCluster08i_WFTFilters** — **8.24** (`WaitForTasks(include_tasks={"compute_a"})` — only `compute_a` `.delay`'d, `compute_b` runs sync; uses fake tasks + MagicMock `.delay` so the routing decision is assertable without eager execution) + **8.25** (`WaitForTasks(exclude_tasks={"compute_b"})` — excluded name stays sync inside an otherwise-async block; mirror of 8.24 for the blacklist branch). **TestCluster08i_FFFilters** — **8.26** (`FireAndForget(force_tasks={"email"})` inside a surrounding WFT — only `email` is FF-forced; `compute_nav` falls through to the WFT scope and gets `.delay`'d there; proves `force_tasks` is a scope-selective filter, not a blanket FF). **TestCluster08i_NoOpWhenCeleryInactive** — **8.27** (with `CELERY_ACTIVE` unset, entering WFT **and** FF is a pure pass-through: `tasks_context` stack stays empty, `unblock_tasks_context` stack stays empty, `.delay` is never called, task body returns its sync result; this is the "your code doesn't change" promise from the docs). **TestCluster08i_ExceptionPropagation** — **8.28** (a `should_fail=True` task dispatched inside WFT with `propagate=False` — Celery's trace invokes `on_failure` first, then WFT's scope-exit `wait_for_completion` calls `.get()` which re-raises the original `RuntimeError`; the exception surfaces at the outer `with` boundary verbatim — a silent failure here would be a data-loss bug; fixture learning: we create the row with default `NOT_CALCULATED` instead of passing `is_calculated=IN_PROGRESS` at create time, because the AFTER_CREATE `calculate_hook` would otherwise fire the failing calc synchronously at `.save()` time before we enter the eager scope). **TestCluster08i_MultipleDispatchesInWFT** — **8.29** (three rows dispatched in one WFT block; scope captures all three on `dispatched_results`; exit drains every one; all three rows land at SUCCESS — proves WFT scales to batch dispatch, not just the single-task case). **TestCluster08i_FFDoesNotBlockOnExit** — **8.30** (FF `__exit__` does **not** drain `dispatched_results` — that is the entire behavioural difference from WFT, and it must be explicit: only a subsequent `wait_for_completion()` call drains; asserted with MagicMock `.delay` so we can observe `.get()` being called exactly once per result after the explicit drain). Full Cluster 8 run: **8a 4 + 8g 9 + 8h 6 + 8i 9 = 28 tests, 0 fail, 0 xfail, ~13s**. Suite total 236 → 245 scenarios, 217 → 226 implemented, 190 → 199 passing. Cluster 8 now has genuinely **exhaustive** coverage of the customer-facing Celery primitives — every documented contract in `12-celery-async-dispatch.md` is gated by a test.
