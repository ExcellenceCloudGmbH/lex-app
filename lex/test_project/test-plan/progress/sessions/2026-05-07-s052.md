---
date: 2026-05-07
clusters: [5, CI]
tests_added: "7 (5l 5.91–5.97); 4 CI selector updates"
suite_tally: "7 land green; PyPI release gate now exercises history + audit_logging clusters automatically"
---

(migrated session 52)

**Cluster 5l landed end-to-end + history & audit_logging clusters wired into the release CI selector.** Closed the future-dated bitemporal save / scheduled-activation gap from Session 50's audit. **History side (1 new test file, 7 scenarios):** `test_5l_future_valid_from.py` pins the customer's `test → test1 → test2` rename example where `test2.valid_from = now + 1h`. **5.91 (no L2 required):** Level-0 main table still reads `test1` after the future save, L1 history carries 3 rows in `valid_from` order with names `["test","test1","test2"]`, and the future row's `valid_from` round-trips as the future moment, not silently overwritten with `now()`. **5.92:** under `CELERY_ACTIVE=true`, exactly one `PeriodicTask(task="activate_history_version", one_off=True, name=meta.meta_task_name)` exists with `args = [app_label, model_name, history_id]` and a `ClockedSchedule.clocked_time` matching `valid_from`. **5.93:** the `> now + timedelta(seconds=5)` boundary is pinned — `valid_from = now + 2s` does NOT schedule (no `PeriodicTask` for that history_id, L2.meta_task_status not flipped to SCHEDULED), `valid_from = now + 60s` does. Catches a regression that would either flip the threshold to 0 (broker spam on every save) or push it past 60s (silent drop of small future edits). **5.94:** editing a future-dated row reschedules cleanly — old `PeriodicTask` deleted by name, new task with a *different* `meta_task_name` registered, new `ClockedSchedule.clocked_time` matches the new `valid_from`. **5.95:** simulates "Beat fired the task at the clocked time" by patching `django.utils.timezone.now` to a moment past `valid_from`, then invokes `activate_history_version(...)` directly — Level-0 catches up to `name="test2"` + `value=2`, L2.meta_task_status flips off SCHEDULED (DONE), and `item.history.count()` is unchanged (activation does NOT mint a new history row). The patch-around-`timezone.now` pattern is the cleanest broker-free way to drive the worker's `> now + 5s` guard past the future boundary without a 1-hour real-time wait. **5.96:** deleting the future-dated *L1 history row* (which is the actual handler — `pre_delete` on the historical model, mirroring `lex/tests/integration/test_event_scheduling.py`) cancels the queued task and flips L2.meta_task_status → "CANCELLED". **5.97:** two distinct rows with distinct future `valid_from` values produce two distinct PeriodicTask names with distinct ClockedSchedule.clocked_time values; cancelling one (via L1 delete) leaves the other intact (PeriodicTask + L2.meta_task_status==SCHEDULED both unchanged for the survivor). Pins fan-out — a regression that namespaced both schedules under the same key would silently coalesce to one. **Discovery during implementation:** the 5k auto-skip probe is on the wrong attribute (`HistSimpleItem.meta_history` — that path is on the *historical* model, not the main model). The canonical access is `HistSimpleItem.history.model.meta_history.model`, and on this build L2 *is* wired for HistSimpleItem — so 5l's L2-side scenarios (5.92–5.97) all run live, not skipped. **CI integration (the user's primary ask alongside 5l):** added `history,audit_logging` to the default cluster selector in **all four** workflow locations: `.github/workflows/showcase_tests.yml` (workflow_dispatch input default + workflow_call input default) and `.github/workflows/pip_publish.yml` (the input default + the `gate-release` reusable-workflow `clusters:` parameter that gates PyPI publish). The default selector goes from 6 clusters to 8: `init,crud_api,api_layer,calculations,validation_hooks,celery_async,history,audit_logging`. Both clusters were already registered in `.github/scripts/showcase_clusters.py` (keys present on lines 131 / 152) — only the selector defaults needed updating. **Run summary:** `lex.test_project.tests.history` + `lex.test_project.tests.audit_logging` together — **65 tests, 53 pass + 9 skipped + 3 expected failures, 0 unexpected failures**, ~94s. The 7 new 5l scenarios all land green. Suite total scenarios: 430 planned (unchanged) → **435 planned** (5.91–5.97 added; 5l flipped from "planned" to "implemented" in `test-clusters.md`); **435 implemented**, **414 passing**, 3 expected failures (5.66 / 6.7 / 6.10), 18 skipped (env-gated + a few fixture-gated).
