---
date: 2026-04-24
clusters: [8]
tests_added: "14"
suite_tally: "14 pass of 14; closes Bucket-C with end-to-end coverage of `celery_tasks.py` lines 696–957"
---

(migrated session 37)

**Sub-cluster 8j landed — Celery task bodies end-to-end (Bucket C).** User directive (Cluster C in the A/B/C gap bucket): cover the three `@lex_shared_task`-decorated task bodies in `lex/lex_app/celery_tasks.py` lines 696–957 — `load_data` (the `initial_data_upload` task), `calc_and_save` (the per-model batch loop every `CalculatedModelMixin.create()` dispatch eventually runs), and `activate_history_version` (the Celery Beat bitemporal-activation task). 8g/8h/8i covered the infrastructure (CallbackTask, CeleryCalculationContext, EnhancedBoundTaskMethod, WaitForTasks/FireAndForget scope contracts) but the task bodies themselves — 150+ lines of customer-visible work — were dark. **Broker-free by design**: `@lex_shared_task` returns an `EnhancedTaskMethodDescriptor` whose `__call__` routes to `self.task(...)` (the underlying celery shared_task shim) when `CELERY_ACTIVE` is unset — which invokes the real body synchronously in the calling thread via `lex_shared_task`'s `wrapper` function. No Redis, no RabbitMQ, no worker process; the wrapper returns `(inner_result, args)` and we assert on side effects. New `test_8j_task_bodies.py` in `lex/test_project/tests/celery_async/` with 14 scenarios across 3 classes: **TestCluster08j_LoadData** (`SimpleTestCase`, fake harness) — **8.31** `initial_data_load` falsy → early-return no-op (neither `setUp` nor `setUpCloudStorage` called, audit factory not invoked — the "customer never configured initial data" gate); **8.32** LEGACY + `is_running_in_celery=True` → `test.setUp(audit_logger)` direct, `asyncio.run` NOT called, `finalize_batch(failure_error=None)`, `test.test_path` stamped from the `initial_data_load` value; **8.33** LEGACY + not-in-Celery → `asyncio.run` invoked exactly once (bridges through `sync_to_async`), `setUpCloudStorage` untouched; **8.34** non-LEGACY + `CELERY_ACTIVE=true` → `setUpCloudStorage(models, audit_logger)` direct, `setUp` never called; **8.35** setUp raises → original `RuntimeError` propagates (identity-preserved via `assertIs ctx.exception is boom` — framework must not swallow or rewrap because `CallbackTask.on_failure` depends on the original type), `finalize_batch` fires in `finally` with `failure_error` kwarg embedding `"RuntimeError: seed crashed"` + traceback; **8.36** `audit_logger` is `None` (audit disabled or import failed) → `finally` guard `if audit_logger:` prevents `AttributeError`, clean exit, `setUp(None)` called. **TestCluster08j_CalcAndSave** (`E2ETestCase` with `CelerySyncCalc`) — **8.37** 2-row happy path → both saved, summary `{total:2, processed:2, conflicts:0, errors:0}` (bookkeeping drift here would silently skip rows); **8.38** `IntegrityError` on first save → conflict resolver: `delete_models_with_same_defining_fields` returns the pre-existing row, task rewires current-instance pk to the existing pk, retry save succeeds (now an UPDATE not an INSERT), summary `conflicts_resolved:1` + `processed_successfully:1`; driven by `patch.object(CelerySyncCalc, "save", flaky_save)` that raises once then delegates to real save — MagicMock on `delete_models_with_same_defining_fields` as an instance attribute (so `hasattr(model, ...)` returns True); **8.39** `calculate()` raises non-IntegrityError → re-raised verbatim (`CallbackTask.on_failure` depends on it), row not persisted. **TestCluster08j_ActivateHistoryVersion** (`SimpleTestCase` with mocked `apps.get_model`) — **8.40** LookupError on model resolution → returns `"failed_model_lookup"` (stale Beat schedule against renamed/removed model, must not crash worker); **8.41** history row `DoesNotExist` (race: user deletes while Beat has activation queued) → returns `"skipped_missing_record"` — built by attaching a custom `_HDN(Exception)` class to `fake_model.history.model.DoesNotExist` and making `.objects.get` raise it; **8.42** `valid_from` 1 hour in future → returns `"failed_too_early"`, `BitemporalSynchronizer.sync_record_for_model` NOT called (the 5-second clock-drift guard short-circuits before sync); **8.43** valid due record → `sync_record_for_model(fake_model, 42, history_model)` fires with exact args, `MetaModel.objects.filter(history_object_id=7, meta_task_status="SCHEDULED").update(meta_task_status="DONE")` observed via MagicMock chain, returns `"success"`; **8.44** sync blows up → `RuntimeError` propagates (not silently swallowed — Beat retries/alerts, silent None would look like a dead task and hide real bitemporal drift). One implementation learning: `_create_audit_logger_for_task` and `should_load_data` are imported **inside** `load_data`'s function body (not at module top), so patching must target `lex.lex_app.apps._create_audit_logger_for_task` (the source module) not the celery_tasks binding. Full run: **14 tests, 0 fail, 0 skip, 1.5s**. Suite total 259 → 273 scenarios; passing 235 → 249. Closes Cluster-A/B/C Bucket C (the April-24 coverage push targeting the 52% → 70% baseline gap).
