---
date: 2026-05-05
clusters: [5, 6]
tests_added: "33 (5g 2 + 5h 6 + 5i 4 + 5j 2 + 5k 4 + 6d 8 + 6e 3 + 6f 4 + 6g 4); 1 fixture model"
suite_tally: "33 land green (28 pass + 5 auto-skip on missing fixture + 1 documented xfail); 6.45/6.46 promoted from expectedFailure → live pass"
---

(migrated session 51)

**Sub-clusters 5g / 5h / 5i / 5j / 5k + 6d / 6e / 6f / 6g implemented end-to-end.** Closed the 35 planned scenarios from Session 50's audit. **History side (5g–5k, 9 new tests files):** `test_5g_valid_to_chaining.py` pins the chained `valid_to → valid_from` contract that 5.4's docstring promises but the original test never asserts (2 scenarios — three-saves chain + delete closes the chain with NULL valid_to). `test_5h_suppression_toolkit.py` covers the 4 documented knobs beyond `skip_history_when_saving`: `save_without_historical_record()` (single-save toggle), `untrack()` / `track()` (sticky), `bulk_create(skip_history=True)` + the default `bulk_create` still tracks. The 5.66 `suspend_bitemporal()` CM is marked `expectedFailure` — docs reference it but only the lower-level guards (already covered by 9.7-9.10) are exposed today. `test_5i_api_contract.py` pins the documented `GET /history/` response shape (every key: `history_id` / `valid_from` / `valid_to` / `history_type` / `user` / `snapshot` / `system_history`) + `get_queryset_as_of(Model, t)` valid-time slice; 5.73/5.74 (system-time + `?as_of=`) auto-skip when MetaHistorical* isn't wired (test_project skips `register_standard_model()` for its private fixtures). `test_5j_snapshot_actor.py` proves history rows carry the *full* field snapshot (not a diff) and API-driven saves stamp `history_user`. `test_5k_metahistory.py` lands the positive L2 contract (1 row per save, `sys_to` chaining across 3 saves, `meta_task_status` default) — auto-skips when L2 not wired, with documentation pointing at `lex.tests.unit.api.test_history_endpoint` + `lex.tests.unit.infra.test_bitemporal_service` for the Mock-backed unit-level coverage. **Audit side (6d–6g, 4 new test files + 1 fixture model):** added `AuditPreValItem` to `extra_models.py` (togglable `pre_validation` raise) — the failure-path fixture that replaces the previously-skipped 6.4 scenarios. `test_6d_payload_and_gfk.py` pins `content_type` + `object_id` GFK population (without these, the per-record Audit Log Tab UI cannot find rows), full create-body + post-save `id` in payload, update payload refreshed to full post-save state (not a diff), delete payload preserves pre-delete state, plus the **`pre_validation`-raise → failure audit row** path the original 6.4 was waiting for a middleware seam to test. **Surprise pass on 6.45/6.46**: planned as `@expectedFailure` to match the test-plan rule "assert intent even when the framework doesn't ship it yet" — turns out the framework already writes the failure audit row through the validation-hook path, so the markers were dropped and the tests stand as live regression gates. `test_6e_bulk_audit.py` covers `BulkAuditLogMixin`'s "1 audit row per record" promise via `DELETE /many/?ids=…` (3 records → 3 audit rows, each with its own `content_type` + `object_id`). `test_6f_resilience.py` pins the deadlock-retry contract: retryable PG `40P01` retries 2× with exponential backoff (BASE × 2⁰, BASE × 2¹) then succeeds, exhaustion re-raises with pgcode preserved, non-retryable errors propagate immediately. The constants (`RETRYABLE_SQLSTATE_CODES = {"40P01", "40001"}`, `MAX_UPDATE_RETRIES = 3`) are pinned at the test level so a regression that drops a code is caught directly. ContentType cache-healing split into input-validation half (`safe_get_content_type(None)` raises `ValueError`) + recovery half (first `get_for_model` raises `DoesNotExist`, helper clears cache and retries — DB-backed `TestCase`). `test_6g_immutability.py` pins `AuditLog.permission_create == False`, `permission_delete == False` even for admin, and `permission_edit` returns `PermissionResult(allowed=False)` with the documented "read-only" reason substring; sub-pin on `AuditLogStatus` so a regression flipping write access there is caught (otherwise an attacker could flip `failure → success` to hide errors). **Re-enabled the BUG-007 `@expectedFailure` marker on `test_6_7`** (it was commented out — the test was producing an unexpected red because BUG-007 is open). **One pre-existing red turned to xfail** with this restoration. **Run summary:** `lex.test_project.tests.history` + `lex.test_project.tests.audit_logging` together — **58 tests, 46 pass + 9 skipped + 3 expected failures, 0 unexpected failures**, ~22s. Suite total scenarios: 430 planned, **428 implemented** (5.67 model_structure.yaml fixture + 5.83 retroactive valid_from deferred for fixture work), **407 passing**, 3 expected failures (5.66 BUG / 6.7 BUG-007 / 6.10 BUG-001 family), 21 skipped (env-gated 1k/1l + fixture-gated 5.73/5.74/5k/6.47/6.48/6.52/5.67/etc.).
