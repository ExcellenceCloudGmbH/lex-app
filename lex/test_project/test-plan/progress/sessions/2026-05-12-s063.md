---
date: 2026-05-12
clusters: [8]
tests_added: "25 (8.47–8.71)"
suite_tally: "25 pass of 25; +25 scenarios on every defensive branch the production sync-fallback story depends on"
---

(migrated session 63)

**Sub-cluster 8l landed — `CeleryTaskDispatcher` full surface (ROI batch #6 of the May 12 coverage sweep).** Largest non-calculation-logging file remaining on the ROI list at 45.69% baseline (186 stmts, 98 missed) — 8h had only ever exercised the happy-path real eager dispatch through this orchestrator (one group, one success), and 8j had only driven the body of `calc_and_save` itself; everything around the orchestrator's defensive scaffolding (group validation, scope selection, sync fallback, `_handle_task_results`'s ResultSet processing + per-task failure routing, the `_get_calculation_context` swallowing-raise contract) was dark. The orchestrator is the **single seam** between a `CalculatedModelMixin.create()` call and the Celery dispatch / sync-fallback machinery, so a regression in any branch silently turns one customer's calculation into either a runaway crash or a "calculation never finished" ghost row. **`SimpleTestCase`-only batch** — Celery, broker, ORM all `MagicMock` / `patch.object` (`from lex.lex_app.celery_tasks import calc_and_save` patched at the *runtime* import site inside `dispatch_calculation_groups`, since that module imports it lazily to dodge a circular import; same trick for `FireAndForget` / `WaitForTasks` / `register_task_with_context`). 25 scenarios across six classes, all of which run in 0.044s combined. **TestCluster08l_DispatchValidation** (8.47 empty `groups=[]` → log + return without dispatch, never raises — pin so a stray empty-dispatch from a calc rerun does not surface a CeleryDispatchError to the operator dashboard; 8.48 wrong-type `groups` (string / dict / None) raises `CeleryDispatchError(groups_type='str'|'dict'|'NoneType')` so the diagnostic field surfaces in the operator log rather than a generic 500; 8.49 all-empty groups `[[], []]` → log + return; 8.50 mixed `[[a], [], [b]]` filters silently with the "Filtered out N empty groups from M total groups" warning that operators key off). **TestCluster08l_DispatchImportError** (8.51 ImportError on the `from lex.lex_app.celery_tasks import calc_and_save` line → wrapped CeleryDispatchError preserving cause-chain via `__cause__` so the missing-module case isn't silently swallowed; tested by patching `sys.modules` to make the import fail at the boundary). **TestCluster08l_WaitScopeSelection** (8.52 no active FF/WFT scope → enters fresh `WaitForTasks()` so the dispatcher always drains; 8.53 active FireAndForget scope detected → uses `nullcontext()` so we don't double-wrap and break drain semantics; 8.54 active WaitForTasks scope detected → also uses `nullcontext()`). **TestCluster08l_TopLevelFallback** (8.55 setup exception (e.g. broker connection refused) → flatten all groups and call `calc_and_save_sync` over the whole pile, recovering the calculation; 8.56 setup-AND-sync-fallback both raising → wrapped CeleryDispatchError carrying both `celery_error` + `sync_error` strings so operators can triage from one log entry without hunting two tracebacks). **TestCluster08l_SingleGroup** (8.57 empty group → warn-and-skip, returns None to signal "synchronous fallback used"; 8.58 wrong-type group inside an otherwise-valid groups list → CeleryDispatchError with `group_index` + `group_type` diagnostic fields; 8.59 calc_and_save import failure inside per-group dispatch → wrapped error; 8.60 dispatch raises CeleryDispatchError → falls back to `calc_and_save_sync` for that group only, returns None to indicate sync fallback; 8.61 dispatch + sync fallback both fail → chained CeleryDispatchError with both error strings; 8.62 unexpected non-CeleryDispatchError exception during dispatch → wrapped). **TestCluster08l_HandleTaskResults** (8.63 empty `task_results` → warn-and-return; 8.64 wrong-type results → raise; 8.65 wrong-type group_mapping → raise; 8.66 all tasks succeed → no sync fallback fires; 8.67 single failed task → corresponding group routed through `calc_and_save_sync` via `failed_groups` retry queue; 8.68 `task_result.failed()` itself raising (e.g. backend connection drop during status check) → group still queued for retry — pin so a flaky `.failed()` doesn't drop the calculation on the floor; 8.69 ResultSet processing failure → flatten ALL groups + complete-sync fallback over `group_mapping.values()`; 8.70 ResultSet failure AND complete-sync failure both raising → chained CeleryDispatchError carrying both strings). **TestCluster08l_GetCalculationContext** (8.71 happy/missing/raise — returns calc_id when present, None otherwise, swallows raises so a context-var bug never crashes the dispatcher — pin all three branches in one scenario via `patch.object(operation_context, 'get', side_effect=...)`). One mid-session learning: `CeleryDispatchError` accepts `**kwargs` as diagnostic fields, but `task_result.failed()` returning truthy *and* `task_result.id` set is what triggers the retry path — earlier draft of 8.68 wired `task_result.failed.side_effect=Exception` but didn't set `task_result.id`, so the `if task_id in group_mapping` branch silently skipped the retry without surfacing the error; fixed by stamping `task_result.id="fake-task"` explicitly. **Run summary:** 25 tests, 0 fail, 0 skip, 0.044s. Suite total scenarios: 555 → **580 planned**, 555 → **580 implemented**, 534 → **559 passing**. Cluster 8 dashboard +1 sub-cluster row (8l).
