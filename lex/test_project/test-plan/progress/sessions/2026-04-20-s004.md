---
date: 2026-04-20
clusters: [4, 5, 6, 7, 8, 9, 10]
tests_added: "68"
suite_tally: "TBD"
---

(migrated session 4)

Scaffolded Clusters 4–10 (permissions, history, audit_logging, calculations, celery_async, signals_ws, api_layer). Each cluster has its own `models.py` + per-sub-cluster test files. Tests that need the Keycloak/AuditLog/Celery/signals fixtures are `@unittest.skip`'d with explicit reasons; ORM-path scenarios (history, calc state machine, hierarchy propagation) assert real intent. BUG-007 added (actor resolution via API create path).
