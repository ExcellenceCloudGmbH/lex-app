---
date: 2026-04-24
clusters: [8]
tests_added: "9"
suite_tally: "9 pass of 9; coverage +24.5 pts on `celery_tasks.py`"
---

(migrated session 31)

**Sub-cluster 8g landed — `lex/lex_app/celery_tasks.py` customer-visible surface.** Target was the 958-line Celery plumbing module (`CallbackTask`, `CeleryCalculationContext`, `FireAndForget` / `WaitForTasks`, `EnhancedBoundTaskMethod`, `lex_shared_task` decorator) — 21.5% baseline, the largest dark surface among production-critical files after 7g/7h. **No Redis required** across all 9 scenarios, by design: (a) `CELERY_ACTIVE=true` branches driven by `patch.dict(os.environ, ...)` so the framework never opens a broker connection; (b) `.delay(...)` return values patched onto fake tasks to return `MagicMock` stand-ins for `AsyncResult`; (c) `allow_join_result` swapped for `contextlib.nullcontext` on the `WaitForTasks` join path so the blocking seam executes end-to-end without talking to anyone; (d) `CallbackTask` instantiated directly with real ORM + real signals — Celery itself never runs, no task registration, no broker lookup. New `test_8g_task_infrastructure.py` in `lex/test_project/tests/celery_async/` with four `TestCase` classes matching the mental-model of the file: **TestCluster08g_CallbackTask** lands **8.7** (`on_success` → `is_calculated=SUCCESS` via direct `.update()` — not `.save()`, so the stale-snapshot branch of `_persist_status_fields` cannot overwrite unrelated columns; `ensure_terminal_calculation_audit` spy called with `audit_status="success"`), **8.8** (`on_failure` → `is_calculated=ERROR` + audit `audit_status="failure"` + exception message + stack-trace string forwarded verbatim from `einfo`), and **8.9** (`initial_data_upload` task short-circuits both callbacks — regression gate against the opt-out ever being silently removed, proves no `.update()` / no audit with nonsense args). **TestCluster08g_CeleryContext** lands **8.10** (`CeleryCalculationContext(context=<incoming>)` stamps the worker-side `operation_context` — calculation_id preserved, `celery_task=True`, `task_name="calc_and_save"`, fresh `operation_id` minted — and restores the prior token on exit; the seam that lets `CalculationLog.log()` inside a worker reach back to the originating calc row) and **8.11** (`CeleryCalculationContext(context=None, ...)` is a safe no-op — `operation_context` is NOT clobbered, matching the sync-mode bypass in `lex_shared_task`). **TestCluster08g_DispatchRouting** lands the three-branch priority hierarchy — **8.12** (no FF/WFT scope, CELERY_ACTIVE=true → runs sync, `.delay` NOT called, task body invoked with `(instance, *args, **kwargs)`); **8.13** (FireAndForget **wins** over an enclosing WaitForTasks — `.delay` called, result lands on the FF scope not the WFT scope; the sync body did NOT run); **8.14** (WaitForTasks alone → `.delay` called, result registered on WFT, scope exit calls `.get()` on the fake `AsyncResult` with `allow_join_result` patched to `nullcontext`; `dispatched_results` cleared after the blocking join). **TestCluster08g_LexSharedTaskWrapper** lands **8.15** (`lex_shared_task` strips `context`/`model_context` from kwargs that reach the inner function, enters `CeleryCalculationContext` when truthy context is supplied, returns the `(inner_result, args)` tuple shape `CallbackTask._extract_model_instances` depends on — a regression in this tuple would silently stop callbacks from seeing their model instances). Implementation learnings: (a) the audit spy must patch `lex.lex_app.celery_tasks.ensure_terminal_calculation_audit` at the import site, not the source module, because `celery_tasks.py` does `from … import ensure_terminal_calculation_audit` at module load — patching too late misses the binding; added `e2e_unpatch = {"ensure_terminal_calculation_audit"}` to opt out of the default mock in those three scenarios. (b) `WaitForTasks.__init__` snapshots `_celery_is_active()` once at construction time, so tests force `_active=True` explicitly instead of depending on env-var timing inside the `with` block. (c) `lex_shared_task`'s wrapper only pops `model_context` when it is truthy — passing `model_context=None` leaks the kwarg through to the inner body, causing `unexpected keyword argument`; scenario 8.15 exercises the context-only branch by omitting `model_context` entirely. **Coverage delta: `celery_tasks.py` 21.5% → 46% (+24.5 pts, +128 lines) on 9 new scenarios.** Full Cluster 8 run: **13 tests (8a 4 + 8g 9), 0 fail, 0 xfail, ~14s.** Cluster 8g dashboard row already tracked at the 9/9/9 target state. Remaining dark code on the file (46% → ~100%): `load_data` / `calc_and_save` / `activate_history_version` task bodies (need real Django fixtures with STORAGE_TYPE branching), `is_celery_worker_process` / `is_running_in_celery` env-detection variants, `print_context_state`, `register_task_with_context`, `respect_fire_and_forget` decorator — all candidates for a follow-up 8h sub-cluster.
