---
date: 2026-06-10
clusters: [1u]
tests_added: "8"
suite_tally: "8 pass / 0 fail in 0.16s (`python -m lex pytest lex/test_project/tests/init/test_1u_runtime_health_metadata.py -v`)"
---

(migrated session 80)

**Sub-cluster 1u superseded the startup-push design with encrypted runtime metadata on `/health` / `/api/health`.** Goal expanded: IC must show both the running lex-app package version and the running customer-project commit SHA. The failure mode is release drift: IC writes the attempted `commit_sha` before Terraform completes, Terraform fails, and the old pod keeps running with its old `COMMIT_SHA` env. The old pod's health response is therefore the right running-state source. Source changes: added `lex/lex_app/runtime_health.py` to build the legacy `{"status":"Healthy :)"}` payload plus optional Fernet-encrypted `runtime` token derived from `LEX_API_KEY`; token decrypts to `lex_app_version`, `instance_commit_sha`, `commit_sha_source`; updated both `views.py` and production `fast_health.py` to use the same builder; removed the old `LexAppConfig.ready()` startup push and deleted `report_lex_app_version.py`. Added direct dependency `cryptography`. Tests replaced old reporter cluster with `test_1u_runtime_health_metadata.py`, scenarios 1.171–1.178: missing key legacy fallback, encrypted/decryptable deployed payload, no plaintext leakage, wrong-key failure, encryption-failure fallback, Django route, fast ASGI route, and missing `COMMIT_SHA` → `unknown`.
