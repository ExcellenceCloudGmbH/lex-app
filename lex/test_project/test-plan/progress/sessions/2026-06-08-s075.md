---
date: 2026-06-08
clusters: [1s]
tests_added: "10 (1.159–1.168) + 1 source fix in `model_structure_builder.py` (test pending 13b)"
suite_tally: "10 pass / 0 fail locally (0.26s)"
---

(migrated session 75)

**Sub-cluster 1s landed — log-noise cleanup + lex-namespace debug control (ticket EXC-1787).** Customer (Joos Sauer, via Melih) reported `urllib3` `InsecureRequestWarning` spam on every Keycloak request when running LEX App V2 locally, and asked for a way to enable framework debug logs cleanly. **Root cause of the spam:** `KeycloakManager.initialize()` defaults `KEYCLOAK_VERIFY_SSL` to `False`, so the admin client makes unverified HTTPS calls and urllib3 warns per request; nothing suppressed it. **Source (`lex/lex_app/settings.py`, 1 file):** (A) gated `urllib3.disable_warnings(InsecureRequestWarning)` next to the existing `CacheKeyWarning` filter — default on, opt-out via `LEX_SUPPRESS_INSECURE_WARNING=False`; the `verify=False` default itself was deliberately left unchanged (separate security decision, out of ticket scope). (B) new `LEX_LOG_LEVEL` env var (default INFO) driving a new `lex` logger entry (`propagate=False`) so the `lex.*` namespace can be raised to DEBUG **without** elevating root — third-party DEBUG stays gated; the console handler level became `min(CONSOLE_LEVEL, LEX_LOG_LEVEL)` so it doesn't drop lex DEBUG records. **Letter note:** intended letter 1r was already taken on disk by an untracked, in-flight Streamlit `lex_view` embed batch (`test_1r_lex_view_embed_helper.py`, up to 1.158, not in the plan and currently failing on an unrelated `_lex_view_component` ImportError — left untouched); took next free letter **1s**, scenarios after 1.158 → 1.159. Test file `test_1s_log_cleanup_and_lex_debug.py` reloads the settings module under patched env (sentry mocked) to exercise both branches of each gate. Design spec + plan in `docs/superpowers/specs/2026-06-07-log-cleanup-and-lex-debug-control-design.md`; operator env-var doc added. **Follow-up in same session (developer ran `lex start` and saw remaining startup noise):** (C) added a gated blanket `warnings.filterwarnings("ignore")` in `settings.py` (default on, opt-out `LEX_SUPPRESS_WARNINGS=False`) to quiet Python warnings such as Django's "Accessing the database during app initialization" `RuntimeWarning`; scenarios 1.166–1.168 (`TestCluster01s_BlanketWarningSuppression`) added to the 1s file. (D) the visible `Warning: <hook> not found in <module>` lines were raw `print()` calls in `lex/process_admin/utils/model_structure_builder.py:142,144` (bypassing logging entirely, so neither `LEX_LOG_LEVEL` nor `warnings` filters could touch them) — converted to a module `logging.getLogger(__name__)` under the `lex.*` namespace: the benign not-found case → `logger.debug`, the hook-call error case → `logger.warning`. At INFO they now vanish; at `LEX_LOG_LEVEL=DEBUG` they surface as proper DEBUG lines. **Note:** `model_structure_builder.py` is slotted in *planned* (not-started) batch 13b; its paired logging test is pending the 13b allocation decision rather than written ad-hoc into cluster 1. Plan docs synced (`test-writing-plan.md` 1s row, `test-clusters.md`, `dashboard.md`).
