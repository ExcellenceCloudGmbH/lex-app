---
date: 2026-06-26
clusters: [1v]
tests_added: "5 (1.179–1.183) + source in 1 file"
suite_tally: "5 pass / 0 fail; regression: history+init+settings 63 pass / 1 skip, celery_async+audit_logging 262 pass / 4 skip / 1 xfail"
---

(migrated session 84)

**Sub-cluster 1v landed — pin `TIME_ZONE` to `USE_TZ` so `django_celery_beat`'s `DatabaseScheduler` is timezone-correct for BOTH the recovery sweep and future history edits.** Root cause of two field symptoms (recovery `IntervalSchedule` never becomes due — observed next≈6969s; future-edit `ClockedSchedule` fires 1–2h late, DST-dependent): `django_celery_beat` reads naive datetimes through `celery.utils.time.maybe_make_aware`, which **hardcodes naive==UTC** (`ModelEntry.is_due` line 153 and `clocked.__init__`). Under `USE_TZ=False` (the deliberate GCP/default production setting from `f9d4769`, "fixed USE_TZ compatibility for production") Django stores **naive Berlin wall-clock** (`PeriodicTask.last_run_at`, `History.valid_from`), but beat reads it as UTC → every beat-driven feature is wrong by the UTC offset. Rejected `USE_TZ=True` (reverts the production bitemporal/audit naive-datetime fix) and rejected Celery-side knobs (`DJANGO_CELERY_BEAT_TZ_AWARE`/`enable_utc` — the misread is in the Django-written seed, no Celery flag touches it). **Fix:** `lex/lex_app/settings.py` now sets `TIME_ZONE = "Europe/Berlin" if USE_TZ else "UTC"` (was unconditional `"Europe/Berlin"`), so under `USE_TZ=False` the naive frame Django writes **is** real UTC and beat's naive-as-UTC assumption is correct; under `USE_TZ=True` datetimes are tz-aware so the Berlin display zone stays free. This makes future history edits work automatically under Celery beat (same path that worked with the in-mem scheduler) and unblocks the recovery sweep, with no aware/naive mixing. **Cluster 1v** (next-free letter after 1u; 1.179 picks up after cluster max 1.178), Type U: 1.179 `USE_TZ=False ⟹ TIME_ZONE=="UTC"`; 1.180 `timezone.now()` naive frame is within seconds of real UTC; 1.181 recovery `IntervalSchedule` is due in the live frame (just-ran → next≤interval, 3×-overdue → due) replicating `ModelEntry.is_due` via `maybe_make_aware(...).astimezone(app.timezone)`; 1.182 future-edit `clocked(now+30s)` is ~30s away not hours and a past target is due; 1.183 living-doc regression rationale — same instant stored naive-UTC round-trips exact, naive-Berlin misread by ≥3600s. Source: `lex/lex_app/settings.py`. Test: `lex/test_project/tests/init/test_1v_scheduler_tz_invariant.py`. Terraform: `recoveryDriver=beat` already the default; no TZ env in terraform (fix ships in the lex-app image); the separate `imagePullPolicy: Always` ops fix already pushed as `lex-instance-v3.0.9`.
