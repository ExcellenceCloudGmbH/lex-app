---
date: 2026-05-12
clusters: [1]
tests_added: "22"
suite_tally: "22 pass of 22; +22 scenarios on settings constants / URL conf / health endpoint / project-config / autodiscovery helpers"
---

(migrated session 54)

**Sub-cluster 1p landed — second batch of PR-6 (init-foundation tier).** Five files from the cleanup-and-coverage-plan COMPLETE bucket whose contracts are visible the moment Django starts. **TestCluster01p_SettingsConstants** (1.125 ROOT_URLCONF == `lex_app.urls` — drift here disconnects every route; 1.126 `DATABASE_DEPLOYMENT_TARGET` non-empty + the `default` connection always registered, since the migration runner depends on it; 1.127 the four Celery resilience flags pinned — `acks_late=True`, `reject_on_worker_lost=True`, `prefetch=1`, `track_started=True` — these are the exact knobs whose drift would re-introduce the worker-loss-loses-tasks footgun documented in NOTES_TODO §4; 1.128 `pickle` stays in `CELERY_ACCEPT_CONTENT` because every `CalculatedModelMixin.create()` async dispatch serialises the instance via pickle, and dropping it would silently break with a `ContentDisallowed` only at worker-side deserialisation; 1.129 `CELERY_TASK_DEFAULT_QUEUE` is the env-driven `INSTANCE_RESOURCE_IDENTIFIER` so multi-tenant deployments stay isolated; 1.130 `REACT_APP_BUILD_PATH` resolves — the catch-all SPA route would 500 every frontend request without it). **TestCluster01p_UrlConfResolves** (1.131–1.134 `reverse()` over the three named routes the framework hard-codes — `health_view`, `api_health_view`, `current-user` — plus a `NoReverseMatch` on an unknown name; defensive gate against silent empty-string returns). **TestCluster01p_HealthEndpoint** (1.135 `GET /health` returns 200 + body contains `Healthy` — the K8s/GCP liveness probe contract; the smiley is pinned because operators grep dashboards for the literal string and a silent change of "Healthy :)" → "OK" would break alerting without breaking the probe; 1.136 `GET /api/health` is the proxy-stripped variant, both routes must work in parallel). **TestCluster01p_LexProjectConfig** (1.137 empty dir → defaults + `_loaded=False`; 1.138 every advertised attr — `INITIAL_DATA`, `PROJECT_GROUPS`, `DEFAULT_SERIALIZER_NAME`, `TAB_DISPLAY_NAMES` — is read; 1.139 falls back to legacy `_authentication_settings.py`; 1.140 user config that raises does NOT crash startup — defaults preserved, customer-footgun guard; 1.141 `get_configured_default_serializer_name` caches AND `reset_default_serializer_name_cache()` forces a re-read — the cache is what makes serializer wiring cheap; 1.142 `get_tab_display_names_for_model` merges `__default__` + model-specific entries with the model winning). **TestCluster01p_GenericAppConfigHelpers** (1.143 `_is_structure_yaml_file` / `_is_structure_file` recognise documented filenames + reject `.yml` look-alikes; 1.144 `_dir_filter` excludes `venv` / `.venv` / `build` / `migrations` / underscore + dot prefixes — walking into `.venv` historically caused infinite recursion on app startup; 1.145 `_is_valid_module` table — settings.py / urls.py / asgi.py rejected by name, test_-prefixed and underscore-postfixed files rejected by pattern; 1.146 `add_model` first-write-wins — re-imports during reload must not silently swap the registered class). **One bug fixed mid-session in 1.141**: first version referenced the temp-dir path after the `TemporaryDirectory` context exited, so the third `load()` call hit a deleted dir and silently returned `"default"` instead of `"second"`. Restructured to keep the file mutation + cache reset inside the same `with` block. **Run summary:** 22 tests, 0 fail, 0 skip, 0.016s. Suite total scenarios: 450 → **472 planned**, 450 → **472 implemented**, 429 → **451 passing**. Cluster 1 dashboard +1 sub-cluster row (1p).
