---
date: 2026-06-02
clusters: [7, 8]
tests_added: "1 (7.176) + source fix in 2 files"
suite_tally: "Not run locally (sandbox lacks Postgres test DB); CI required"
---

(migrated session 72)

**Sub-cluster 7o landed — FK integrity violations now abort calculation batches immediately.** Added `test_7o_fk_violation_abort.py` (scenario 7.176) to pin the regression: in `CalculatedModelMixin.create()` a ForeignKey violation raised from inside `calculate()` must abort the full computation and must not continue to later objects. Source fix updates both sync and Celery batch paths: `calc_and_save_sync` now detects FK-integrity exceptions in the exception chain and re-raises immediately; `calc_and_save` skips duplicate-resolution fallback for FK violations and aborts the task. Added dedicated cluster fixtures (`FKViolationAbortCalc`, `FKAbortWrite`, `FKAbortTarget`) in `calculations/models.py`. Updated `test-clusters.md`, `dashboard.md`, and `test-writing-plan.md` for 7o allocation/status. **Local validation:** unable to execute DB-backed cluster tests in this sandbox because `python -m lex pytest ...` cannot create the Postgres test DB (`connection refused on localhost:5432`); regression gate is committed for CI verification.
