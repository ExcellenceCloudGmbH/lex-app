---
date: 2026-05-05
clusters: [5, 6]
tests_added: "0 (plan-only)"
suite_tally: "unchanged on disk; 35 new planned scenarios queued"
---

(migrated session 50)

**Cluster 5 + Cluster 6 doc-vs-test audit; gap sub-clusters 5g–5k + 6d–6g planned.** Walked the implemented Cluster 5 / Cluster 6 tests against `docs/features/tracking/history.md`, `docs/features/tracking/bitemporal history.md`, `docs/features/tracking/audit logs.md`, and `docs/interface/record-detail/{audit log,history} tab.md`. Outcome: the implemented spine (5.1–5.10, 5.11, 6.1–6.10) is correct as far as it goes but is significantly *narrower* than the documented customer contract. **Added "Audit notes — May 5" callouts** under both clusters in `test-clusters.md` enumerating exactly what each implemented scenario *does* and *does not* assert versus the docs. **Added 9 new gap sub-clusters with 35 planned scenarios**: **5g** chained `valid_to → valid_from` (2 scenarios — pins the contract 5.4's docstring promises but the implementation never asserts); **5h** suppression toolkit (6 scenarios — `save_without_historical_record`, `untrack()/track()`, `bulk_create(skip_history=…)` on/off, `suspend_bitemporal()` positive contract, `untracked_models`); **5i** history API contract (4 scenarios — full response shape per `bitemporal history.md`, `get_queryset_as_of` valid + system time, `?as_of=...` REST branch); **5j** snapshot completeness + `history_user` actor stamping on API path (2 scenarios); **5k** MetaHistory positive contract (4 scenarios — Level-2 row created, `sys_to` chains, retroactive `valid_from` round-trip across both layers, `meta_task_status` default); **6d** audit payload + GenericForeignKey (8 scenarios — `content_type`+`object_id` populated, full create payload, update payload refreshed to final state, delete payload preserves pre-delete state, **5 failure-path scenarios that replace the skipped 6.4 by using `pre_validation` raises instead of needing a middleware-level audit hook**); **6e** bulk audit (3 scenarios — `BulkAuditLogMixin`'s "1 audit row per record" promise from the docs); **6f** resilience (3 scenarios — deadlock retries on PG `40P01` / `40001` SQLSTATE codes, exhaustion re-raise, ContentType cache healing post-migration); **6g** immutability (3 scenarios — `permission_create=False` / `permission_delete=False` / `permission_edit→deny` gated through the API). Also re-enabled the `@unittest.expectedFailure` marker on `test_6_10_audit_survives_calculation_failure` (it was commented out, so when the test fails it was producing an *unexpected* red — now correctly tracked as BUG-001 family xfail). Sequencing block in `test-clusters.md` updated; progress dashboard rows added with status . **No new tests written this session** — pure plan reconciliation per the test-plan rule "test intent, not implementation; a missing scenario is a documented gap, not a failure". Suite total scenarios: 395 → **430 planned**, 376 implemented (unchanged), **35 not started**.
