---
date: 2026-04-28
clusters: [4, 10]
tests_added: "1 (4.40) + 1 retirement (10.8)"
suite_tally: "4.40 pass; full Cluster 4 deny matrix complete"
---

(migrated session 44)

**Cluster 4 deny-coverage audit + sub-cluster 4h landed; scenario 10.8 retired.** Walked the full Cluster-4 deny matrix (read / edit / create / delete / export / list × unit / serializer / API layer) and confirmed every action × outcome cell is gated except one: `permission_export` returning `PermissionResult.deny(...)` was tested at the unit level (4.8b Keycloak no-scope) and at the per-field-mask level (13.4 / 13.12) but no test exercised what the **export endpoint** actually returns when a model fully denies export. Added `ExportDeniedItem` to `permissions/models.py` (read open, `permission_export` returns `deny`) and new `test_4h_full_deny_matrix.py` with scenario **4.40**: POST `/api/<model>/export` against a fully-denied model returns 200 with rows present (read is open) but every domain field blanked — only the framework's `{id, created_by, edited_by}` columns may carry data. Pins the union behaviour in `ModelExportView.get_exportable_fields_for_object` against both over-restrictive (rows dropped → compliance-id export breaks) and over-permissive (any domain column leaking → P0 leak) drift. **Also retired scenario 10.8**: the API-layer companion to BUG-009 (`is_calculated` editable=False so PATCH silently drops it) was an `@expectedFailure` xfail and the underlying contract is already covered at the calculation-state-machine layer (7.14). Removed `test_10d_api_calc_trigger.py`, dropped `ApiAtomicCalc` + `API_ATOMIC` from `api_layer/models.py`, and updated `test-clusters.md` + BUG-009 entry to reflect that 7.14 is the single canonical gate. Cluster 4 dashboard: 12 → **13** scenarios; Cluster 10: 10 → **9** scenarios. Total: 324 / **324** scenarios (4.40 added, 10.8 removed → net +0), passing 304 → **305**, xfail 1 → **0** (BUG-009 still tracked, but the xfail moved entirely under 7.14).
