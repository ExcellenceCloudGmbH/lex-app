---
date: 2026-04-25
clusters: [1]
tests_added: "0 (move-only)"
suite_tally: "unchanged"
---

(migrated session 39)

**Initial-data tests consolidated under `tests/init/initial_data/`.** Pure folder reorganization — no test logic changed, no scenarios added or removed. Moved the three files that exercise the customer-facing `INITIAL_DATA` seed-loading pipeline into a dedicated subfolder so the relationship is obvious from the directory tree alone: `test_1c_initial_data.py` (seed-data parse contract — JSON shape + `lex_config.py` declarations), `test_1f_seed_idempotency.py` (idempotency gate — `load_data` "all-or-nothing" guard + declaration-order preservation), `test_1i_initial_data_journey.py` (full upload journey — seed walker + `InitialDataAuditLogger` CRUD + batch finalize + create/update/delete arc with audit on/off). Added `__init__.py` documenting the grouping. Sub-cluster letters (1c / 1f / 1i) and scenario numbers (1.17–1.22, 1.18b/c, 1.20b, 1.51 / 1.51b, 1.52, 1.54–1.60) preserved verbatim — Django's test discovery picks up the new path automatically, no CI label change needed.
