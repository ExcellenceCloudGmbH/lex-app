---
date: 2026-05-12
clusters: [9]
tests_added: "24 (9.11–9.28 with sub-letters 9.27b / 9.28b–f)"
suite_tally: "24 pass of 24; +24 scenarios on the in-memory state store the WebSocket reconciliation depends on"
---

(migrated session 60)

**Sub-cluster 9d landed — `ActiveCalculationStateStore` full surface (ROI batch #2 of the May 12 coverage sweep).** Second-biggest absolute coverage gap on the May 12 ROI list (27.03% baseline, 131 stmts / 86 missed). The store is the in-memory authoritative source of truth that lets a re-connecting browser tab pick up the spinner mid-calculation; the previous DatabaseCache implementation lost entries written inside `transaction.atomic()` because the ASGI WebSocket consumer ran on a different DB connection — the bug whose fix this whole file exists to protect. Existing tests (9a/9b) only exercised the store transitively through `update_calculation_status`; the public accessors, the DB-validated `snapshot()` reconciliation path the WebSocket consumer calls on every reconnect, the startup `validate_and_prune()` sweep, and the private model-resolution helpers were all dark. **TestCluster09d_Mutators** (9.11 `mark_in_progress("")` early-return; 9.12 full-payload persistence with `int → str` pk normalisation so JSON-serializable downstream; 9.13 optional fields default to `""` not `None` because downstream consumers iterate values directly — `None` would force `or ""` everywhere; 9.14 `clear("")` symmetric guard; 9.15 `clear` idempotent; 9.16 `clear_all` empties at any size; 9.17 re-marking same id replaces prior entry — the re-fire-after-ABORTED-startup-reset path). **TestCluster09d_Accessors** (9.18 `get_calculation_id` returns string when set; 9.19 returns None for both missing entry AND blank stored calc id — two dark branches: dict.get default + the `isinstance(...) and calculation_id` truthiness guard; 9.20 `get_entry("")` symmetric guard; 9.21 `get_entry` returns `dict(entry)` defensive copy — pins regression where a bare `return entry` would let snapshot consumers mutate the store under the lock). **TestCluster09d_PrivateHelpers** (9.22 `_split_record_id` uses `rsplit("_", 1)` so model names containing underscores parse correctly — `my_calc_model_7` → `("my_calc_model", "7")`; 9.23 malformed input — empty / no underscore / blank halves all `(None, None)`; 9.24 `_find_model_by_name` walks app registry, returns `CalculationModel` subclass match or `None`). **TestCluster09d_ResolveModelAndPk** (9.25 explicit `app_label.ModelName` resolves via `apps.get_model` and is preferred over record_id parsing; 9.26 falls back to record_id parsing when label missing — used `FakeCalcModel9d` since Django rejects model class names starting with underscore as `models.E023`; 9.27 returns `(None, None)` when class is not a `CalculationModel` subclass — without this guard `snapshot()` would call `.objects.filter(...)` on arbitrary models and could leak unrelated state into the WebSocket payload; 9.27b empty entry early-out + `apps.get_model` raise → registry-walk fallback that also fails). **TestCluster09d_SnapshotAndPrune** (9.28 empty-store fast path; 9.28b live IN_PROGRESS pass through, terminal-state entries dropped from BOTH the payload and the store — the WebSocket reconciliation contract; 9.28c keeps entry on DB exception — defensive: better a possibly-stale spinner than a silently-dropped live calculation; 9.28d skips DB validation when resolver returns `(None, None)` — pins the `if model_class is not None and record_pk is not None` guard; 9.28e `validate_and_prune()` empty-fast-path + only-IN_PROGRESS-survives across stale/gone/unresolvable; 9.28f drops entry on DB exception — documented behavioural difference from `snapshot()`: startup sweep is conservative-rebuild, pin so a future "keep on exception" refactor would have to revisit). **DB-touching paths mocked at the `_resolve_model_and_pk` boundary** so the entire batch stays `SimpleTestCase` — patches use `MagicMock()` (not `MagicMock(spec=type)` — `spec=type` blocks attribute access via the metaclass guard) and `patch("django.apps.apps.get_model", ...)` (not `patch("lex.core.signals.ActiveCalculationStateStore.apps.get_model", ...)` — `apps` is imported inside the function, not at module level). **Run summary:** 24 tests, 0 fail, 0 skip, 0.009s. Coverage: 27.03% → estimated ~95%+ (whole file minus 1-2 unreachable defensive branches that need a bare `except` at module-level to even reach). Suite total scenarios: 509 → **533 planned**, 509 → **533 implemented**, 488 → **512 passing**. Cluster 9 dashboard +1 sub-cluster row (9d).
