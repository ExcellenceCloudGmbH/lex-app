---
date: 2026-05-04
clusters: [8]
tests_added: "1"
suite_tally: "1 broker-backed pass when Redis is available; env-gated skip otherwise"
---

(migrated session 47)

**Sub-cluster 8k landed — one real Redis broker integration test for Celery.** Added `test_8k_redis_broker_integration.py` with scenario **8.45**: a JSON-safe task is published to a unique Redis-backed queue, consumed by an in-process Celery worker started with `celery.contrib.testing.worker.start_worker`, and read back through the Redis result backend. The test forces `task_always_eager=False` and asserts the worker-returned payload carries the correlation id plus `task_always_eager=False`, so it cannot silently pass through eager mode. It is opt-in (`LEX_RUN_REDIS_CELERY_TESTS=true`, optional `LEX_CELERY_REDIS_TEST_URL`, default `redis://127.0.0.1:6379/15`) and skips cleanly when Redis is absent. Implementation note: this app's namespaced Celery config requires overriding `CELERY_BROKER_URL`, `broker_read_url`, `broker_write_url`, and `CELERY_RESULT_BACKEND`; the test resets Celery's cached `_backend_cache` while the temporary Redis backend is active. Verification: default run = 1 skipped / OK; with a temporary local Redis on port 6380 = 1 pass / OK.
