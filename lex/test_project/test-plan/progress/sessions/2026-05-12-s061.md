---
date: 2026-05-12
clusters: [6]
tests_added: "15 (6.141–6.155 with 6.142b sub-letter)"
suite_tally: "16 pass of 16; +15 scenarios on the Audit Tab UI's read path"
---

(migrated session 61)

**Sub-cluster 6n landed — `AuditLogSerializer` + `AuditLogMixinSerializer` surface (ROI batch #3 of the May 12 coverage sweep).** Two files that feed the frontend Audit-Tab UI — `AuditLogSerializer.py` 37.65% baseline (read-only-scopes mixin + 5 status SerializerMethodFields + the `calculation_record` builder powering the per-row drill-down link without dereferencing the GFK on every list page) + `AuditLogMixinSerializer.py` 53.73% baseline (the type-coercion layer every audit writer routes through to keep payload JSON-stable across Decimal/UUID/datetime/FieldFile/QuerySet/Promise — regression here either crashes JSON encoding mid-write or surfaces non-serializable artefacts at the API boundary). **TestCluster06n_ReadOnlyMixin** (6.141 lock `{edit:[], delete:False, export:True}` regardless of caller — drift would silently let a non-system caller PATCH/DELETE audit rows and erase compliance evidence). **TestCluster06n_StatusFields** (6.142 `_get_latest_status_record` uses `_prefetched_objects_cache['status_records']` as last-is-newest, no DB hit — pins AG Grid list-view fast path; 6.142b empty prefetch list → None still no DB hit; 6.143 fallback to `.order_by('-created_at').first()`; 6.144 all 5 computed fields return None for the mid-flight zero-status-records case operators see right after audit row creation — UI renders `—`, regression that returns `""` or `0` would render confusing dashboard rows; 6.145 live record values flow through with isoformat for datetimes). **TestCluster06n_CalculationRecord** (6.146 None when `object_id` is None — line 81 early-return for free-form audit events; 6.147 None when `safe_get_content_type` raises — defensive try/except at lines 90-93 prevents 500 on stale ContentType; 6.148 full output shape + display-name fallback chain `short_description > name > display > "<model> #<pk>"` over all four priorities; 6.149 `details` only includes `is_calculated` + `error_message` when present in payload — pin so other payload keys like `secret` cannot leak into the drill-down preview; 6.150 non-dict payload [None / [] / 42 / `"not-a-dict"`] treated as `{}` so the `.get()` chain doesn't crash on older pre-schema-tightening audit rows). **TestCluster06n_ModuleWiring** (6.151 `AuditLog.api_serializers` registry default+reference + `_lex_skip_serializer_alias` + `_lex_hidden_serializers={'reference'}` set — the published-API contract the frontend reads). **TestCluster06n_IsoSeconds** (6.152 None passthrough + tz-naive microseconds dropped + tz-aware non-UTC converted to UTC then stripped + tz-aware UTC same wall-clock stripped — strict `YYYY-MM-DDTHH:MM:SS` contract). **TestCluster06n_FileReference** (6.153 happy path `{name, url}` + `.url` raising returns `url=None` for unsaved FieldFile + `name=None` short-circuits url access entirely). **TestCluster06n_GenericInstancePayload** (6.154 round-trip via real `AuditLog` row exercises `model_to_dict(fields=concrete_fields)` editable-only filter, pk → `id` injection, and the field-coercion ladder; pin pinned `author`/`resource`/`payload` columns in particular because losing them silently drops compliance evidence). **TestCluster06n_SerializePayload** (6.155 walks every coercion branch in one scenario: dict/list recursion, datetime/date/time isoformat, Decimal/UUID coercion to `str`, set → list, Model → `{id, display}`, QuerySet-like via `.all()` callable, fallback `str(obj)` and the unserializable last-resort `<Unserializable: type>` placeholder). **One mid-session fix:** 6.154's first version asserted every concrete field landed in `data` — but `model_to_dict` filters out `auto_now_add` columns like AuditLog's `date` even when explicitly passed via `fields=`. Reframed to assert only `editable=True and not auto_created` fields land, which is the documented behaviour. **Run summary:** 16 tests, 0 fail, 0 skip, 0.009s. Suite total scenarios: 533 → **548 planned**, 533 → **548 implemented**, 512 → **527 passing**. Cluster 6 dashboard +1 sub-cluster row (6n).
