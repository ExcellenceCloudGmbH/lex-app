---
date: 2026-06-18
clusters: [1u, 9f]
tests_added: "11 (1.171–1.175 + 9.37–9.42) + source in 5 files"
suite_tally: "11 pass / 0 fail via `DJANGO_SETTINGS_MODULE=lex_app.settings python -m pytest ...`; documented `python -m lex pytest ...` blocked locally by missing PostgreSQL service (`localhost:5432` refused)"
---

(migrated session 81)

**Coverage task #620 — ASGI health/readiness + core WebSocket consumer coverage for PR #615.** PR #615 also touched the runtime surfaces that keep the frontend from showing stale server-not-ready state: fast HTTP liveness/readiness in `fast_health.py`/`asgi.py`, the public backend-health WebSocket, calculation notification WebSocket, and per-calculation log WebSocket. Added **1u** (5 U scenarios 1.171–1.175, `test_1u_fast_health_asgi.py`) for disjoint health/readiness helpers, body-draining static health response, readiness 200/503 DB gate, ASGI short-circuit of probe paths, and Django fallback for non-probe HTTP. Added **9f** (6 U scenarios 9.37–9.42, `test_9f_core_consumers.py`) for backend-health connect/receive/disconnect, calculations group fan-out, calculation-log record-prefix grouping/log envelope, and `disconnect_all` shutdown cleanup across all three consumers.
