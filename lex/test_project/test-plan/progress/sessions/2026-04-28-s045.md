---
date: 2026-04-28
clusters: [7]
tests_added: "64"
suite_tally: "64 pass; full 3-level atomicity matrix complete"
---

(migrated session 45)

**Sub-cluster 7j landed — exhaustive 3-level grandparent/parent/child atomicity matrix (64 scenarios).** Extends 7i's parent/child matrix with a third level. Every combination of GP / P / C atomicity (2³ = 8 triplets) × failure outcome (2³ = 8 triplets) = **64 scenarios**, IDs 7.48–7.111. **Fixtures**: 8 thin grandparent classes (`Triple_AAA_Calc` … `Triple_NNN_Calc`) added to `calculations/models.py`, each with `is_atomic` set per its triplet and a shared `_run_3l_grandparent` helper that spawns the matching 7i 2-level matrix-parent class. Total +8 model classes, +1 helper, +1 lookup dict (`TRIPLE_CLASSES`). **Test driver**: `test_7j_three_level_matrix.py` parametrically generates 64 test methods at class-load time via `setattr` — each named `test_7_NN_<aaa>_<TFT>` so a CI failure log identifies the cell directly (atomicity letters a/n for atomic/non-atomic; outcome letters T/F for gp_fail/p_fail/c_fail). **Two framework rules surfaced and pinned that the 2-level matrix could not distinguish**: (a) **a failing level's ERROR row is wiped only by the OUTERMOST atomic ancestor's rollback** — its own atomic block and any intermediate atomic ancestor's savepoint rollback both leave the ERROR row intact; only collapsing the entire connection-level transaction (i.e. GP atomic in a 3-level chain) wipes it. This explains why `test_7_34` (atomic parent fails after atomic child) lands the parent at ERROR — there's no outer ancestor — but in 7j the equivalent middle-level row gets wiped when GP is atomic. (b) **A successful descendant's row is wiped by ANY atomic ancestor that raises** — its IN_PROGRESS+SUCCESS save goes through the active connection-level transaction, and any atomic block that rolls back wipes it. The two rules are *different*: rule (a) only the outermost wipes; rule (b) any ancestor wipes. They coincide in the 2-level matrix because there's only one ancestor candidate, which is why 7i never had to distinguish them. The expected-state formula went through three iterations against live framework behaviour (24 fails → 8 fails → 16 fails → 0 fails) before converging on the correct rules — each iteration's failure pattern eliminated one wrong hypothesis. Final `_expected_states()` is 9 lines of logic with extensive doc comment explaining both rules. Run summary: full Cluster 7 = **108 tests, 0 fail, 1 xfail (BUG-009 / 7.14)**, ~108s. Cluster 7 dashboard: 30 → **94** scenarios, 29 → 93 passing. Suite total: 324 → **388** scenarios, 305 → **369** passing.
