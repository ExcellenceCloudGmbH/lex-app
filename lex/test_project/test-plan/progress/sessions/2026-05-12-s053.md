---
date: 2026-05-12
clusters: [1]
tests_added: "15"
suite_tally: "15 pass of 15; +15 scenarios on lazy-import / Keycloak-exclusion / history-tracking gate surfaces"
---

(migrated session 53)

**Sub-cluster 1o landed — first batch from the test-writing-plan PR-6 (init-foundation tier).** Four files from the cleanup-and-coverage-plan COMPLETE bucket that 1a–1n never reached: `lex/process_admin/__init__.py` (lazy `__getattr__`), `lex/lex_app/keycloak_exclusions.py`, `lex/lex_app/simple_history_config.py`, and the `lex/lex_app/__init__.py` package alias. All four files are pure-Python helpers — no DB, no Keycloak, no Celery — so the entire batch runs as `SimpleTestCase` in **0.001s**. Four classes, 15 scenarios, scenario IDs 1.110–1.124. **TestCluster01o_ProcessAdminLazyGetattr** (1.110 `__all__` lists exactly 8 names; 1.111 every name resolves via `__getattr__` with one sub-test per branch — sub-tests name the failing branch on regression instead of producing a generic AttributeError; 1.112 unknown attr → `AttributeError`, not silent `None` — typo guard; 1.113 reference-stable repeated access — guards against per-call object reconstruction). **TestCluster01o_KeycloakExclusions** (1.114 the three exclusion constants pinned — `legacy_data` app, `audit_logging.AuditLog` resource, `("historical","metahistorical")` prefix tuple — drift-canary against framework tables silently syncing into a customer Keycloak realm; 1.115 `is_keycloak_syncable_app` over Django built-in / lex internal / site-packages / real user app; 1.116 missing-attr safety on shim AppConfigs — production crash here would mask Keycloak-sync errors; 1.117 `is_keycloak_sync_excluded_model` over every branch incl. case-insensitive `historical`/`metahistorical` prefix; 1.118 `_resource_name` variant over None / empty / bare-name / FQN). **TestCluster01o_SimpleHistoryConfig** (1.119 `Historical*`-prefixed classes blocked — canary against `HistoricalHistorical*` recursion that simple_history's own shadow tables would create if re-tracked; 1.120 Django built-in apps blocked; 1.121 `hasattr(model, "history")` short-circuit prevents double-registration on `ready()` reload; 1.122 vanilla user model passes — silent false here = lost audit trail; 1.123 `get_model_exclusion_reason` returns non-None for every blocked case + None for the tracked case, substring-asserted so doc copy-edits don't break the gate). **TestCluster01o_LexAppPackageAlias** (1.124 `sys.modules["lex.lex_app"] is sys.modules["lex_app"]` — both names must resolve to the same module object; historical regression caused `LookupError: No installed app with label 'lex_app'` because parallel registrations of every model would happen on Django startup). **Run summary:** 15 tests, 0 fail, 0 skip, 0.001s. Suite total scenarios: 435 → **450 planned**, 435 → **450 implemented**, 414 → **429 passing**. Cluster 1 dashboard +1 sub-cluster row (1o).
