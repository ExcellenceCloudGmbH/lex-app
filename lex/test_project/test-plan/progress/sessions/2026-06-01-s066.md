---
date: 2026-06-01
clusters: [8]
tests_added: "2 (8.49 / 8.50) + 8.2 inverted"
suite_tally: "6 pass of 6 (2 new + 4 8a re-runs); +2 net scenarios + 1 behavioural inversion"
---

(migrated session 66)

**Sub-cluster 8m landed — every Calculation runs as a Celery task when broker available (behaviour change, framework feature).** User-directed change: pressing "Calculate" must dispatch to a worker regardless of whether the user's `calculate()` / `update()` is decorated with `@lex_shared_task`. Previously `CalculationModel.should_use_celery()` returned False whenever `lex_func()` lacked `.delay`, so undecorated calcs ran inline on the request thread and hung the UI for the duration — same UI action, two completely different runtime profiles, depending on a decorator users often don't know to add. **Surfaces touched:** (a) `should_use_celery()` — dropped the `hasattr(self.lex_func(), 'delay')` clause; the function now returns True whenever `CELERY_ACTIVE=='true'` and the broker `current_app.control.inspect()` succeeds; (b) `dispatch_calculation_task()` — when `lex_func()` has no `.delay`, the instance is wrapped in the existing generic `calc_and_save` Celery task (`lex/lex_app/celery_tasks.py:743`) which calls `model.lex_func()()` inside the worker, otherwise the user's task `.delay` is called directly (fast path preserved). Nested calcs spawned inside a worker still execute synchronously via the existing `is_celery_worker_process()` branch in `calculate_hook` — re-dispatching child calcs would deadlock the pool, and the user's "first calculation" wording covers entry-point only. **Tests:** new `lex/test_project/tests/celery_async/test_8m_undecorated_dispatch.py` with two scenarios — 8.49 asserts the generic-dispatch path (instance lands as `[self]` in `calc_and_save.delay(...)`, calculation_id propagates via the extracted operation context, returned AsyncResult is from the generic task), 8.50 pins the fast path (decorated `lex_func()` with `.delay` is invoked directly, generic `calc_and_save` is NOT touched — regression that always routed through `calc_and_save` would double-wrap every decorated calc and add a deserialisation hop per call). Existing Scenario 8.2 in `test_8a_sync_fallback.py` was **inverted** in-place (renamed `test_8_2_should_use_celery_true_even_without_delay_attr`) — undecorated calc + `CELERY_ACTIVE=true` + reachable broker (mocked via `patch('celery.current_app.control.inspect', return_value=object())`) now returns True; module-level Intent updated to reflect the new contract. **Run summary:** 6 tests touched in the celery_async cluster (2 new in 8m at 1.21s + 4 re-run in 8a at 2.45s), all pass; full celery_async cluster reruns at 77 tests / 1 pre-existing import error in `test_8t_worker_recovery_production` (missing `redis_keys` symbol — unrelated, pre-existing); full calculations cluster reruns at 151/151 pass in 122s confirming no regression in `CalculationModel` lifecycle. Suite total scenarios: 594 → **596 planned**, 594 → **596 implemented**, 573 → **575 passing**. Cluster 8 dashboard +1 sub-cluster row (8m).
