---
date: 2026-05-04
clusters: [8]
tests_added: "1 + pip-publish gate"
suite_tally: "2 broker-backed passes when Redis is available; env-gated skip otherwise"
---

(migrated session 49)

**Sub-cluster 8k extended — `CalculationModel` + `WaitForTasks` over a real Redis broker, plus pip-publish gate.** Added scenario **8.46** to `test_8k_redis_broker_integration.py`: a persisted `CeleryCalc` row is moved to `IN_PROGRESS`, `CELERY_ACTIVE=true` is scoped to the test body, an in-process Celery worker consumes the decorated `calculate()` task from Redis, `WaitForTasks` captures and drains the real `AsyncResult`, and `CallbackTask.on_success` flips the row to `SUCCESS` while the terminal audit seam is observed. The model-instance task uses the framework's pickle serializer boundary intentionally; the test enables Kombu pickle deserialization only inside the opt-in body. Both 8.45 and 8.46 now publish with an explicit temporary Redis connection so Celery producer-pool caching cannot leak a previous/default broker URL between scenarios. Added `.github/workflows/celery_redis_broker_example.yml` with PostgreSQL + Redis services and wired it into `pip_publish.yml` via `workflow_call`; PyPI publishing now needs both `gate-release` and `gate-celery-broker`. Verification: default no-Redis run = 2 skipped / OK with no DB setup; full opt-in run against temporary Redis on port 6380 = 2 tests OK.
