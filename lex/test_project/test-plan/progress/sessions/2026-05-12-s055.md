---
date: 2026-05-12
clusters: [7]
tests_added: "21"
suite_tally: "21 pass of 21; +21 scenarios on core exceptions + restriction ABC + XLSXField spotter"
---

(migrated session 55)

**Sub-cluster 7k landed — first batch of PR-8 (calculation-edge tier).** Skipped past the marginal 1q init-extension batch (init.py is already heavily covered by 1g/1h/1j/1k/1l/1n) to the higher-value PR-8 work. Three files from the cleanup-and-coverage-plan COMPLETE bucket. **TestCluster07k_HelperPrimitives** (7.122 `ensure_list` over None / list / tuple / scalar plus the Falsy ≠ None gotchas — `0` and `""` must wrap, not collapse; 7.123 `_normalize_string` collapses None / blank / whitespace and coerces non-string via `str(...)`; 7.124 `iter_exception_chain` walks `__cause__` chain with cycle protection — self-cycle terminates after one yield via the seen-set guard; 7.125 falls back to `__context__` when `__cause__` missing — the implicit Python during-handling chain). **TestCluster07k_PreferredSelectors** (7.126 `select_preferred_exception_detail` skips generic `"A server error occurred."` even when listed first — the core "don't silently surface 'server error' to the customer" rule; 7.127 falls back to first generic when only generics present — better than None; 7.128 None when nothing usable; 7.129 `select_preferred_stack_trace` returns first non-blank — earliest is most specific; 7.130 `find_exception_artifacts` walks chain for first carrier with non-empty calc_obj/exception_details/stack_trace — inner exceptions skipped, outer wrapper carrying the artefacts wins; 7.131 returns three empty lists when nothing carries — caller depends on tuple-unpacking, regression returning single None would crash every audit-log writer). **TestCluster07k_ResolveHelpers** (7.132 `resolve_exception_detail` priority order artefact → str(e) → fallback covered for all four shape combos; 7.133 `resolve_exception_traceback` priority artefact → fallback with the deliberate-absence pin: it must NOT fall back to `str(exception)` because `str(e)` is the message not the traceback — pinning the absence so a future "helpful" addition that broke this rule would be caught here). **TestCluster07k_ExceptionClasses** (7.134 `ValidationError` carries `original_exception` + `model_class`; 7.135 `CalculatedModelError` message format `[<model>] <msg> - Context: k=v` over all four shape combos — plain / model only / context only / both; 7.136 subclass extras weaved in — `ModelCombinationError` field_name in parens, `ModelClusteringError` model_count + parallelizable_fields, `CeleryDispatchError` group_index reported 1-based for humans + group_size + task_id; 7.137 every subclass derives from `CalculatedModelError` so `except` clauses catch). **TestCluster07k_ModelModificationRestriction** (7.138 every ABC default returns `True` — the "subclass and override only what you need to restrict" no-op contract that makes partial overrides safe; 7.139 `AdminReportsModificationRestriction` allows read + denies every write — HTMLReport read-only profile pinned, regression here would silently make HTMLReport pages writable; 7.140 `violations` list mutable in overrides — the documented "tell the user WHY" mechanism; 7.141 `ExampleModelModificationRestriction` template instantiates cleanly with placeholder bodies — copy-paste starting point for new restrictions stays usable). **TestCluster07k_XLSXFieldCoverageSpotter** (7.142 `XLSXField` format constants pinned — `cell_format` / `_without_color` / `boolean_format` strings parsed by Excel/Google Sheets/LibreOffice + `max_length=300` migration sensitivity flag; the file already has 378 lines of exhaustive coverage at `lex/tests/unit/api/test_xlsx_field.py` driving real spreadsheet libs — this single scenario is the dashboard hook so a regression deleting the unit-test file would surface here, not silently drop coverage). **Run summary:** 21 tests, 0 fail, 0 skip, 0.001s. Suite total scenarios: 472 → **493 planned**, 472 → **493 implemented**, 451 → **472 passing**. Cluster 7 dashboard +1 sub-cluster row (7k).
