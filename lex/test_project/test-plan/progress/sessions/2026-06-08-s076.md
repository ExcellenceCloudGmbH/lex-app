---
date: 2026-06-08
clusters: [8x]
tests_added: "13 (8.103–8.115) + source in 2 files"
suite_tally: "6 U pass / 0 fail (`tracked_calculation_record_ids`); 7 I real-row scenario bodies pass their assertions, only the shared `TransactionTestCase` teardown flush errors in the borrowed local venv (same `test_db_lex` provisioning gap as 8v/8w) — they gate on the CI Postgres service"
---

(migrated session 76)

**Sub-cluster 8x landed — liveness-aware startup reset (recovery hand-off).** Closes the last state-loss hole left after 8w: when the **backend/web** pod restarts mid-calculation, `_handle_calculation_model_reset` (`lex/process_admin/utils/model_registration.py`, gated by `CALLED_FROM_START_COMMAND`) did a **blind** sweep flipping every `IN_PROGRESS` row → `ABORTED`. In the split web/worker deployment the Celery worker pods survive a backend restart and keep computing, so the sweep aborted live calculations; worse, for a *dead-but-tracked* row the abort then trips PR #603's terminal-outcome guard and the supervisor refuses to requeue it — permanent state loss. **Fix (defer-if-tracked):** new pure helper `tracked_calculation_record_ids()` in `celery_recovery/supervisor.py` returns `(label_lower, pk)` for every calc row owned by a *tracked* recovery task — alive heartbeat **or** expired-but-tracked, no `is_alive` gate (an expired-but-tracked row is exactly what the supervisor resumes). The sweep gained an optional precomputed `tracked_record_ids` set (read once in `register_models`, only on the startup path) and now `continue`s past any owned row, aborting only genuinely **untracked** rows. Degrades to today's blind-abort when recovery is off / Redis unreadable (`list_tracked()==[]` ⇒ empty set). **Cluster 8x opened** (next-free letter after 8w; 8.103 picks up after the cluster max at 8.102): 8.103–8.108 `tracked_calculation_record_ids()` against a mocked registry (alive→owned; expired→still owned & `is_alive` never called; empty registry→empty; non-calc payload→nothing; multi-task→union; pk-None→excluded); **8.109–8.115 the integration surface** — the real sweep over live `CelerySyncCalc` rows via `E2ETestCase` (owned stays `IN_PROGRESS` + no audit written; untracked → `ABORTED` + audit; mixed; empty-ownership → all abort; gate-off no-op; precomputed-set honored; self-computes when kwarg omitted). Source files: `lex/lex_app/celery_recovery/supervisor.py`, `lex/process_admin/utils/model_registration.py`. Test file: `lex/test_project/tests/celery_async/test_8x_startup_reset_recovery_handoff.py`. Stacked on the #603 (`fix/recovery-terminal-outcome-guard`) branch since the design relies on that guard.
