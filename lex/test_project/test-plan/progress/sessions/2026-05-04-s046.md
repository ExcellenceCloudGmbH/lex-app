---
date: 2026-05-04
clusters: [4]
tests_added: "1"
suite_tally: "1 pass; full-read deny now covered across unit, list, pk_only, detail, and export-row filtering paths"
---

(migrated session 46)

**Cluster 4 read-deny detail endpoint gap closed.** The deny matrix already had read-deny coverage at the default-permission unit level (4.8 / 4.8c), list/filter-backend level (4.13 / 4.16 / 4.18), and serializer read-mask level in Cluster 12, but a guessed detail URL (`GET /api/<model>/<id>/`) was not explicitly pinned for a full `PermissionResult.deny`. Added scenario **4.41** to `test_4e_filter_backend.py`: a `deny_all` caller requests a concrete `FilterBackendItem` detail URL and receives `200 {}` — no `name`, no other domain field, and no framework-injected `id`. This complements 4.18: list endpoints drop denied rows before serialization; detail endpoints rely on `LexSerializer.to_representation` returning `{}`.
