---
date: 2026-04-24
clusters: [1]
tests_added: "14"
suite_tally: "13 pass + 1 env-gated skip of 14; closes 1i and Bucket-B of the April-24 coverage push"
---

(migrated session 36)

**Sub-cluster 1i landed — initial-data upload journey end-to-end.** User directive (Cluster B in the A/B/C gap bucket): drive `InitialDataAuditLogger.py` (148 stmts, **12.64%** baseline) + the `ProcessAdminTestCase` seed walker that Cluster 1c never reached. 1c only asserts post-`Init` DB state — it doesn't touch the JSON walker (tag:/datetime: prefix resolvers, subprocess flattening), doesn't exercise the audit-logger create/update/delete methods, and never drives the end-to-end action dispatcher. New `test_1i_initial_data_journey.py` in `lex/test_project/tests/init/` lands 14 scenarios across 5 classes: **TestCluster01i_ReplaceTaggedParameters** (`SimpleTestCase`, no DB) — **1.51a** `tag:foo` resolves to the previously-stored in-memory object (FK-by-reference mechanism; non-prefixed strings and non-string values pass through untouched — the resolver must not mangle literals); **1.51b** `datetime:YYYY-MM-DD` parses through `dateutil.parser` into a real `datetime` (seed files ship dates as strings; models need `datetime` instances). **TestCluster01i_SubprocessFlattening** (`SimpleTestCase`) — **1.52** `{"subprocess": "path"}` entries flatten recursively, preserving the declared parent-file order AND each sub-file's internal order (parent-before-child FK resolution depends on this); patched `PROJECT_ROOT` env var to point at the `lex/` root so the fixture-relative subprocess path `test_project/tests/fixtures/…` resolves. **TestCluster01i_AuditLoggerCRUD** (`E2ETestCase` with `e2e_models = [SimpleItem]`, `e2e_framework_models = [AuditLog, AuditLogStatus]`) — **1.54** `log_object_creation` writes AuditLog + pending AuditLogStatus with `author = "system (initial_data_upload)"` and `_audit_tag` in payload for trace-back; **1.54b** `_logged_ids` tracks every log in declaration order (finalize_batch's sweep is scoped by this list — must not touch earlier sessions); **1.55** `log_object_update` embeds the target instance's pk in payload (compliance view links audit row → DB row); **1.56** `log_object_deletion` snapshots the instance via `generic_instance_payload` (deleted row's fields survive in the audit trail after the main row is gone — payload `value` is stringified by `_serialize_payload` for JSON stability, so the assertion compares `str(...)`); **1.57** `mark_operation_success` flips status to `success`, `mark_operation_failure` to `failure` with error_traceback populated; both idempotent via queryset `.update()` — calling twice does not duplicate status rows; **1.57b** mark_operation_* on `None` audit log is a no-op (the loader passes whatever `log_object_creation` returned, which can be `None` on failure — must not raise). **TestCluster01i_AuditLoggerFinalize** — **1.58a** clean run → `finalize_batch()` with no error resolves lingering pending → success; summary reports `pending_resolved` count (non-zero signals a handler forgot to mark); **1.58b** `finalize_batch(failure_error=…)` collapses pending → failure with the error string on every swept row (outer-driver exception path — audit trail closes out completely even for aborted runs). **TestCluster01i_FullSeedJourney** — **1.59** full create/update/delete drive with audit enabled (env-gated on `INITIAL_DATA_AUDIT_LOGGING`) asserts DB has the correct surviving row AND 4 AuditLog rows in `[create, create, update, delete]` order AND all statuses `success`; **1.59b** full journey without audit still lands the right DB state (2 created − 1 deleted = 1 surviving row with `value=999` from the update — audit is observability, disabling it cannot regress data transitions); **1.60** crash-path simulation — 3 pending ops then `finalize_batch(failure_error=…)` sweeps every row to `failure` with the error string (`pending_resolved == 3`; `len(_logged_ids) == 3`). Harness trick: `ProcessAdminTestCase` is itself a `unittest.TestCase`, so instantiating it as a fixture needs a `runTest = lambda self: None` placeholder; `get_test_data` is monkey-patched to read from our fixture path; `apps.get_app_config(settings.repo_name)` is stubbed (the test project's app label isn't registered as an installed app in the sandbox) so the harness sees `SimpleItem` via `models.values()`. Fixtures all pre-existed: `seed_parent.json`, `seed_child_01.json`, `seed_child_02.json` (subprocess test), `test_seed_journey.json` (full journey). First-run result: **14 tests, 13 pass + 1 env-gated skip, 0 fail, ~5.4s**. Suite total 245 → 259 scenarios; passing 222 → 235.
