---
date: 2026-06-03
clusters: [9e]
tests_added: "8 (9.29–9.36) + source in 5 files"
suite_tally: "8 pass / 0 fail locally (Postgres test DB available)"
---

(migrated session 73)

**Sub-cluster 9e landed — plain CRUD now broadcasts a generic `record_mutation` so open list views refresh live.** Root cause: only `CalculationModel` state transitions broadcast over WebSockets; ordinary create/update/delete on a non-calculation record emitted nothing, so a list view open in another tab/iframe/window went stale until manual refresh. Added `lex/core/signals/ModelMutationSignal.py` (`broadcast_model_mutation` defers a `model_data_update`-group send via `transaction.on_commit`) and `lex/api/consumers/ModelDataUpdateConsumer.py` (joins the group, forwards each message verbatim); routed at `ws/model_data_update` in `routing.py`. Wired the broadcast into the REST mutation entry points: `One.py` create/update/destroy (skips the generic broadcast when `calculate=true`, since `calculation_success` already refreshes) and `Many.py` bulk patch/delete. Added `test_9e_model_mutation_broadcast.py` (scenarios 9.29–9.36, continuing after 9d's 9.28f): helper message-shape + empty-name no-op + on_commit-deferral (U/I), consumer group-join+forward (U), and E2E CRUD-triggers-broadcast for create/update/delete/bulk-delete. Frontend `ModelDataUpdate` listener landing in same change.
