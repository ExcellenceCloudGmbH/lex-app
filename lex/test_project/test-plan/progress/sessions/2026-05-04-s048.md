---
date: 2026-05-04
clusters: [12]
tests_added: "4"
suite_tally: "4 pass; docs PR serializer-history alias behavior now gated"
---

(migrated session 48)

**Cluster 12e extended — serializer alias inheritance from docs PRs #45/#47.** The docs PRs for v2.0.0rc146 describe the new public serializer behavior: when a source model overrides `api_serializers["default"]`, the auto-generated framework serializer is also exposed under the configured alias on the source table and automatically on associated history/meta-history tables. Added scenarios **12.32–12.35** to `test_12e_factory.py`: 12.32 pins the source-model baseline (`default` remains the developer override, `framework_default` exposes the auto serializer, and explicit `detail` remains addressable); 12.33 proves a synthetic `Historical*` model inherits the alias through `instance_type` without copying unrelated source serializers; 12.34 proves `MetaHistorical* → Historical* → Source` chain walking; 12.35 pins the serializer docs option `Meta.hide_actions_column=True` surviving `_wrap_custom_serializer` and surfacing via `get_list_ui_options()`. Also restored expected-failure markers on the two known 12b bugs (BUG-012 timezone-naive DateTime output and BUG-013 FK dict PATCH payload) so the full serializer package is green with tracked expected failures. Verification: focused `test_12e_factory` = 7 tests OK (1 expected failure); full `lex.test_project.tests.serializers` = 35 tests OK (3 expected failures).
