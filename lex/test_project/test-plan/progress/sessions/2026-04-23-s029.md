---
date: 2026-04-23
clusters: [7, 12, 13]
tests_added: "10 (3 new in 12a history/meta + 3 new in 12e factory including 1 xfail + 1 new in 13c + 2 new in 13d + 1 new in 7h) + fixture restores unblocking 9 pre-existing scenarios (12f 3 + 7g 6)"
suite_tally: "Cluster 12  / Cluster 13  / Cluster 7h ; BUG-017 added for `get_serializer_map_for_model` no-cache"
---

(migrated session 29)

**Cluster 12 + Cluster 13 closed; Cluster 7h async-dispatch sub-cluster landed.** Task triple from the user: (1) finish the 6 remaining Cluster-12 scenarios; (2) finish the 3 remaining Cluster-13 scenarios; (3) add a Cluster-7 sub-cluster driving the `CalculatedModelMixin._dispatch_model_processing` Celery branch that 7g's sync path never reached. **Cluster 12** — discovered on entry that the previously-logged session-18 "12f write paths landed" was in fact silently failing to import (`TagItem` / `TaggableItem` referenced but never added to `serializers/models.py`) so 12f's 3 scenarios had been counting as passing on the dashboard while erroring at collection time. Added `TagItem`, `TaggableItem`, and a new `EditScopedItem` (for scenario 12.8's `permission_edit → allow_fields({"x"})` fixture) to `serializers/models.py`. Also disambiguated a scenario-number collision: `test-clusters.md` 12e (factory contract) is canonical for 12.26/12.27/12.28; the 12f write-path scenarios were renumbered 12.26–28 → **12.29–31**. New `test_12a_history_meta_scopes.py` lands **12.6** (history-row GET unwraps to the main model's `permission_read` — `ProtectedWideItem` secret fields must NOT leak through the history endpoint's `snapshot` payload), **12.7** (MetaHistorical instance's `lex_reserved_scopes` is locked at `{"edit": [], "delete": False, "export": False}` regardless of caller; located the MetaHistorical class by walking `django.apps.get_models()` for the `MetaHistorical*` subclass tied to the history model, because the framework registers `meta_history` as a dynamic reverse relation rather than a declared field), and **12.8** (non-admin GET of `EditScopedItem` returns `lex_reserved_scopes.edit == ["x"]` — exactly the one field `permission_edit` allows). New `test_12e_factory.py` lands **12.26** (`model2serializer` always injects `id_field` / `short_description` / `lex_reserved_scopes` in `Meta.fields` — asserted both for the default call and for a narrow-fields caller), **12.27** (`_wrap_custom_serializer` preserves a developer's `Meta.fields = ["name", "amount"]` AND appends the framework internals AND leaves the class inheriting from `LexSerializer` so the row-level permission filter stays active), and **12.28 (xfail)** documenting **BUG-017** — `get_serializer_map_for_model` rebuilds the serializer class on every call with no in-process cache, so the identity assertion fails until the memoization is added. Cluster 12 dashboard: 18 pass + 4 xfail → **24 pass + 4 xfail + 0 unexpected failures** ( Complete). **Cluster 13** — `test_13c_grouped_selected.py` lands **13.9** (AG `selection.groupKeyPaths` with two paths — `[{"field": "category", "key": "<alpha-pk>"}]` and `[{"field": "category", "key": "null"}]` — exercises both the integer-FK string→int coercion branch of `_coerce_group_key` AND the `"null"` → `__isnull=True` sentinel branch; asserts the selected sheet is strictly smaller than the empty-selection baseline, and that exactly 2 group rows land instead of 3 when alpha + null are selected and beta is excluded). `test_13d_auth_edge.py` lands **13.11** (fresh `APIClient` not `force_login`'d POSTs export → 401/403; no streaming body — `HasAPIKey | IsAuthenticated` gate holds) and **13.12** (monkey-patches `ExportMaskedItem.permission_export` so row A returns `allow_fields({"id", "name"})` and row B additionally allows `{"amount"}` — heterogeneous per-row results force `_compute_uniform_export_mask → None` and the slow `filter_and_mask_data_for_export` per-row loop; asserts `amount` is blank for row A and populated for row B in the resulting sheet, which is only possible if the per-row mask actually consulted the per-object `permission_export`). Cluster 13 dashboard: 10 pass + 2 not-started → **12 pass + 0 xfail + 0 unexpected failures** ( Complete). **Cluster 7h** — discovered on entry that 7g had the same silent-import bug as 12f — `CombinatorialCalc` referenced but never defined in `calculations/models.py`, so 7g's 6 "passing" scenarios had been erroring at collection time all along. Added `CombinatorialCalc(CalculatedModelMixin)` with `defining_fields = ["region", "category"]`, `parallelizable_fields = ["region"]`, class-level mutable key rings (`_region_keys` / `_category_keys` / `fail_for_region`), a `get_selected_key_list(key)` that reads those rings, and a `calculate()` that sets `self.name = f"{region}-{category}"` and raises on `fail_for_region` match. With the fixture restored, 7g's 6 scenarios actually execute and pass. New `test_7h_celery_dispatch.py` lands **7.31** (`CELERY_ACTIVE=true` + stub `.delay` attached to `calculate` + `CeleryTaskDispatcher.dispatch_calculation_groups` mocked → `create()` routes through the async branch at lines ~1606-1665 of `CalculatedModelMixin.py` that 7g's sync path never reached; asserts the dispatcher was called with 2 flattened groups totalling 4 models — 2 regions × 2 categories grouped by the `region` parallelizable field — and that zero rows persisted, proving the sync fallback did NOT fire). Full regression across clusters 12 + 13 + 7: **82 tests**, 1 pre-existing unrelated failure in `test_13e_streaming_fast_export.test_13_17_end_row_limit_caps_written_rows` (confirmed pre-existing by git-stash test), 6 xfails (5 prior + 1 new = BUG-017). Net: **+12 passing scenarios** (12.6 / 12.7 / 12.8 + 12.26 / 12.27 + 13.9 / 13.11 / 13.12 + 7.31 + retroactive unblock of 12.29–31 and 7.25–30 that were silently failing to import).
