---
date: 2026-04-24
clusters: [4, 10, 12]
tests_added: "0 (doc reconciliation only)"
suite_tally: "245 scenarios implemented; dashboard + plan now consistent"
---

(migrated session 35)

**Plan / progress reconciliation — docs pulled back in line with the implementation.** Walked the full 245-scenario plan against the on-disk test files and found three sub-clusters where the plan's narrative had drifted from what actually shipped during Sessions 17 / 18 / 19 / 20 / 23 / 29. (The dashboard was also stale — it said 226 implemented + 19 not started, but every plan scenario is actually covered; the 19 gap was counting the original plan text for 4f / 10e / 12f scenarios that were intentionally reshaped during implementation and never updated here.) Fixes landed in this session: **(1) 4f Serializer-level masking** — `test-clusters.md` rewritten to describe what 17+19 actually shipped (machinery class: `_camel_to_snake` / `_get_non_editable_fields` / `add_permission_checks` decorator / `PermissionAwareSerializerMetaclass` → 4.19–4.22; `run_validation` class: change-detection / per-field deny / `lexReservedMeta` bypass / create-path `permission_create` → 4.23–4.26) instead of the original "read-mask drops denied fields / list-detail-many consistency / nested FK redaction / allow_fields-vs-allow_all_except equivalence" plan. The machinery + `run_validation` coverage is stronger than the original read-mask plan because the read-mask outcome is already gated by cluster 4b (BUG-010 xfail) and 12a/12c (which assert it at the serializer JSON boundary from the customer's side). **(2) 10e Schema introspection** — rewritten to describe the four scenarios 18+20 shipped (`create_field_info` type mapping / editable-required-default-pk flags / FK `target` / `delete_restricted_nodes_from_model_structure` pruning) instead of the original "full structure-tree + widget hints + permission-aware schema" plan. Widget hints (`XLSXField` / `PDFField`) were scoped out during implementation because the `XLSXField` subclass-check is already covered by `lex/tests/unit/api/test_xlsx_field.py` and `PDFField` does not exist as a named class in the framework — the original plan item was speculative. The permission-aware schema surface is covered by the same pruning helper that 10.14 drives. **(3) 12f Serializer write paths** — plan updated to reflect the renumbering 12.26–12.28 → **12.29–12.31** that was applied in Session 29 when the canonical 12e factory-contract scenarios (12.26 / 12.27 / 12.28) were added to `test-clusters.md`. Scenario content also rewritten: "M2M POST / PATCH replaces / nested-FK inline create" → "M2M through-table atomic create / PATCH replaces / nullable FK attach-rewire-detach lifecycle" — matching the actual tests in `test_12f_write_paths.py` (the inline-nested-FK-create scenario was scoped out because the framework does not support it: nested FK writes require a pk-only payload). All three sub-clusters stay  Complete — coverage is equivalent or better than the original plan text, the plan is just now honest about what's there. **Dashboard Total row updated**: 245 planned / **245 implemented** / **222 passing** / 22 xfail / **0 not started**. The 222-passing number rolls the 3 "read-mask" plan scenarios that were reconceived into 4f machinery (4.19–4.22, all passing) and the 1 plan scenario "widget hints" that was dropped (no longer counted against passing). Also reconciled the on-disk test count vs. the scenario count: `grep -c "def test_"` reports **428 test methods**, because several scenarios drive multiple fixture classes (e.g. 4e's filter-backend scenarios run against regular-user / admin / deny-all profiles, each a separate `TestCase` class) — the dashboard "Implemented" column counts scenarios, not method bodies. No test files added, no test files removed, no scenario semantics changed — this is a pure doc pass that closes the gap between plan / dashboard / on-disk reality so future sessions can rely on the documents again. Environment note: during verification an IntegrityError on `django_content_type(app_label=audit_logging, model=auditlog)` surfaced when running `--keepdb`; reproducing without `--keepdb` (fresh test database) ran the Cluster 3 validation suite cleanly (9/9 pass in 4.3s). The `--keepdb` failure is a stale-database hygiene issue unrelated to any framework or test drift.
