---
date: 2026-07-02
clusters: [8ad (7r withdrawn, 7q restored)]
tests_added: "3 (8.142–8.144) + source in 1 file"
suite_tally: "8ad 3 pass / 0 fail; regression: full `calculations` + `celery_async` = 325 pass / 7 skip / 0 fail"
---

(migrated session 91)

**Batch 8ad landed — nested dispatch restored; the Report 1 (abort→resume) fix moved from the withdrawn inline guard to the restart-surviving cluster cancel marker.** Live verification of Session 89's inline guard on a real two-worker setup (worker1/worker2) showed it broke the core parallelism contract: a nested per-instance calc (`CalculateNAV`, a `@lex_shared_task`-decorated CalculationModel inside an `InvestorTrackRecord` calculation) logged "Dispatching…" then immediately ran INLINE on the parent's worker; only wrapping the caller in an explicit `WaitForTasks` restored dispatch. Per the developer's explicit requirement ("calculation can dispatch calculations… it worked only when I put waitfortasks, otherwise it is doing it inline"), the guard, the 7q 7.199 flip, and draft batch 7r (test file included) were **withdrawn** — `CalculationModel.calculate_hook` is back to the committed `4eafd0fa` always-dispatch behaviour on both paths, with the dispatch-branch comment rewritten to document where abort-safety actually lives. **The real Report 1 hole and its fix:** `cancel()` already persists a Redis cancelled marker per calculation_id (`cluster_cancel_index.mark_cancelled`, TTL-bounded, survives server+worker restarts — unlike the in-memory revoke), and `calc_and_save` already self-aborts on it at task start; but a *decorated* calculate method dispatches directly via `func.delay` and never consulted the marker, so a broker-redelivered child of an aborted calc silently resumed. The generic `lex_shared_task` wrapper now performs the same cooperative check: a dispatched execution (a `context` kwarg carrying a calculation_id) whose marker is set raises `CalculationCancelled` before running anything → the redelivered task lands CANCELLED. Sync calls carry no context kwarg and never touch the cancel index (zero Redis dependency; without Redis there is no broker to redeliver anyway). **Cluster 8ad** (next free letter after 8ac; 8.142 picks up after cluster-8 max 8.141), Type U (module-level `@lex_shared_task` probe; `_celery_is_active` patched False so calling the descriptor executes the task body in-process — the same code path a worker runs; `cluster_cancel_index.is_cancelled` mocked at the module boundary): 8.142 dispatched run + marker set → raises `CalculationCancelled` before the wrapped function runs; 8.143 dispatched run + no marker → marker consulted, runs exactly once; 8.144 synchronous run (no context) → cancel index never consulted. Withdrawal cleanup: `test_7q_worker_default_dispatch.py` restored to committed dispatch assertions; `test_7r_nested_worker_inline_abort_safe.py` deleted; 7r marked withdrawn across test-clusters.md / dashboard.md / test-writing-plan.md. Source: `lex/lex_app/celery_tasks.py` (`lex_shared_task` wrapper), `lex/core/models/CalculationModel.py` (comment only — code reverted to committed state). Test: `lex/test_project/tests/celery_async/test_8ad_dispatched_task_cancel_marker.py`.
