---
date: 2026-05-12
clusters: [6]
tests_added: "4 (6.156–6.159)"
suite_tally: "4 pass of 4; +4 scenarios on the bulk-write payload normalisation seam"
---

(migrated session 64)

**Sub-cluster 6o landed — `BulkAuditLogMixin._normalize_bulk_payloads` four-branch matrix (coverage-driven extension to 6e).** 6e (Session 51) only drove the API-level happy DELETE-many path — `BulkAuditLogMixin.py` 47.06% baseline (89 stmts, 42 missed) still had every other branch of `_normalize_bulk_payloads` unexercised. This static helper sits between DRF's bulk serializer and the per-row audit-write loop; its job is to take an arbitrary `payloads` shape (None / dict / list of varying length) and produce **exactly one normalised payload per target** so the per-row audit-write loop never has to special-case shape mismatches. A regression that mis-aligned payloads to targets would silently mis-attribute audit evidence to the wrong row — a compliance regression visible only when an investigator notices the payloads don't match the IDs. **`SimpleTestCase`-only** — `_attach_related_instance_id` patched to a transparent identity (`lambda payload, target: {**payload, "_attached_to_pk": target.pk}` for dicts, wrapped as `{"_p": payload}` for non-dicts) so we observe which payload landed on which target without depending on the attacher's internal contract (already covered in 6d). **TestCluster06o_NormalizeBulkPayloads** (6.156 `len(payloads) == len(targets)` → strict 1-to-1 zip alignment, the dominant happy path — DRF's bulk serializer produces one payload per instance, helper preserves mapping; a regression that swapped to broadcast or single-serialize would silently apply the same payload to every target; 6.157 `len(payloads) == 1` and `len(targets) > 1` → broadcast — uniform "delete-with-reason" bulk ops, helper replicates the one payload across every target; a regression that zip-truncated would leave N-1 audit rows with empty payloads; 6.158 dict / scalar payload (not a list) → `_serialize_payload(...) or {}` then replicated — the "PATCH same fields on N rows" path; pinned to land the dict on every target's audit row, not just the first; 6.159 falsy serialised payload → `{}` fallback at line 34 fires for both empty dict (`_serialize_payload({}) → {}` falsy) and empty list (`_serialize_payload([])` enters list-branch with len 0, falls through to bottom, `[]` falsy) — without the `or {}` guard the audit row's `payload` column would be `None`, masking bulk-write evidence; **plus pinned the documented quirk that `None` is NOT rewritten to `{}`** — `_serialize_payload(None)` falls through to `str(None)` returning the string `"None"` (truthy) so the `or {}` is skipped and the literal string lands replicated; this is the real behaviour and a future "fix" that silently coerced None → {} would change observable behaviour for downstream consumers, so the test pins reality. Plus mismatched-length list (3 entries / 2 targets) → fall-through to single-serialize semantics, replicating the whole list across every target — pin so a regression that silently truncated to `targets[:len(payloads)]` would surface here). **One mid-session fix:** initial draft of 6.159 asserted None coerced to `{}` based on the docstring intent, but the actual behaviour disagreed. Verified the bug-or-feature question by tracing `_serialize_payload(None)`: no isinstance branch matches, falls through to the bottom `try: return str(data) except: return f"<Unserializable: {type(data).__name__}>"` — `str(None) → "None"` which is truthy. Reframed scenario 6.159 as a four-part assertion that pins (a) empty dict triggers fallback, (b) empty list triggers fallback, (c) None lands as the string `"None"` (the documented quirk), (d) mismatched-length list falls through. Each assertion message names the failure mode in production terms ("masking bulk-write evidence", "audit row's payload column", "downstream consumers"). **Run summary:** 4 tests, 0 fail, 0 skip, 0.044s combined with 8l. Suite total scenarios: 580 → **584 planned**, 580 → **584 implemented**, 559 → **563 passing**. Cluster 6 dashboard +1 sub-cluster row (6o).
