# Test-Suite Dashboard

> **Back to:** [Progress index](../progress.md) | [Test Plan Index](../index.md)
> **Audience:** Engineering leadership, QA supervisors, anyone scanning suite health at a glance.
> **Update cadence:** after every work session — both the per-cluster table AND the Known Bugs Tracker. The Copilot test-bot updates only the cluster row(s) it touched and (in modes B/C) appends a Known-Bugs row.

---

## At a glance

| Cluster | Scenarios | Implemented | Passing | Expected Failures | Not Started | Status |
|---------|-----------|-------------|---------|-------------------|-------------|--------|
| 1. Init — Project Bootstrap | 26 | 26 | 26 | 0 | 0 |  Complete (scenario **1.6b** added April 22 as the CI showcase test — asserts detect-changes / makemigrations / migrate / Keycloak sync all fire in one `lex Init` run) |
| 1g. `KeycloakSyncManager` admin-facing methods (coverage-driven — April 24) | 10 | 10 | 10 | 0 | 0 |  Complete — covers `get_all_django_models` (app-registry walk + repo_name scoping + abstract/proxy + exclusion rules), `get_client_roles` (ignore-list filter + lazy default-role creation + missing-id fail-fast), and `ensure_client_role_policies` (add-new + normalize-in-place + extra-role ordering). All 10 driven through the `_make_sync_manager()` fixture from 1e — no Keycloak, no DB. |
| 1h. Bootstrap flow — URL + HTTP polling (coverage-driven — April 24) | 15 | 15 | 15 | 0 | 0 |  Complete — covers `build_instance_controller_url` (query-string shape + missing-base fail-fast), `fetch_bootstrap_state` (done/cancelled/pending + non-2xx + non-JSON), `poll_bootstrap_status` (loop-until-done + cancel/timeout/missing-payload), `wait_for_keycloak_setup_v2` (env-already-set fast path + controller-payload passthrough + cancel + timeout). `requests.get` patched — network-free. |
| 1i. Initial-data upload journey (coverage-driven — April 24) | 14 | 14 | 13 | 0 | 0 |  Complete (1 env-gated skip — 1.59 requires `INITIAL_DATA_AUDIT_LOGGING=true`). Drives `ProcessAdminTestCase` seed walker (`replace_tagged_parameters` tag:/datetime: resolution, `get_test_data_from_path` subprocess flattening, action dispatcher) + `InitialDataAuditLogger` end-to-end (log_object_create/update/delete → AuditLog + pending AuditLogStatus, mark_operation_success/failure idempotent, finalize_batch sweeps lingering pending → success on clean run / failure on crash path). Full create/update/delete journey runs against real `SimpleItem` + `AuditLog` tables via `E2ETestCase`. Closes `InitialDataAuditLogger.py` (12.64% baseline) + the JSON-walker contracts in `ProcessAdminTestCase` that Cluster 1c never reached. |
| 1o. Lazy imports + sync-exclusion + history-config helpers (coverage-driven — May 12) | 15 | 15 | 15 | 0 | 0 |  Complete — 15 pass in 0.001s. `SimpleTestCase`-only batch covering 4 files from the cleanup-and-coverage-plan COMPLETE bucket: `lex/process_admin/__init__.py` (lazy `__getattr__` — every `__all__` name resolves, unknown raises `AttributeError`, reference-stable across calls), `lex/lex_app/keycloak_exclusions.py` (constants pinned + `is_keycloak_syncable_app` over Django/lex/site-packages/user-app + `is_keycloak_sync_excluded_model` and `_resource_name` over every branch incl. case-insensitive `historical`/`metahistorical` prefix), `lex/lex_app/simple_history_config.py` (`should_track_model` blocks `Historical*`/Django apps/already-tracked, passes vanilla; `get_model_exclusion_reason` table — non-None reason for every blocked case), `lex/lex_app/__init__.py` (sys.modules alias proves `lex.lex_app` and `lex_app` resolve to the same module object — historical regression caused `LookupError: No installed app with label 'lex_app'`). Scenarios 1.110–1.124. |
| 1p. Settings / URLs / health view / config singletons (coverage-driven — May 12) | 22 | 22 | 22 | 0 | 0 |  Complete — 22 pass in 0.016s. Five files from the COMPLETE bucket whose contracts are user-visible the moment Django starts: `lex/lex_app/settings.py` (ROOT_URLCONF + the four Celery resilience flags + pickle in CELERY_ACCEPT_CONTENT + per-tenant default queue + REACT_APP_BUILD_PATH), `lex/lex_app/urls.py` (`reverse()` over health_view / api_health_view / current-user + NoReverseMatch on unknown), `lex/lex_app/views.py` (HealthCheck — both `/health` and `/api/health` for proxy-stripped deployments), `lex/core/config.py` (`LexProjectConfig.load` over no-config / new-config / legacy `_authentication_settings.py` / user-raises paths + the cached `get_configured_default_serializer_name` and `get_tab_display_names_for_model` accessors with their reset helpers), `lex/utilities/config/generic_app_config.py` (autodiscovery helpers — `_is_structure_yaml_file`, `_is_structure_file`, `_dir_filter`, `_is_valid_module`, idempotent `add_model`). Scenarios 1.125–1.146. |
| 1r. Fast-health query-tolerant path matching (coverage-driven — Jun 1) | 3 | 3 | 3 | 0 | 0 |  Complete — 3 pass / 0 fail. Covers `lex/lex_app/fast_health.py` query-tolerant matcher (`match_health_request_path`) while pinning strict matcher behavior (`is_fast_health_path`) to prevent accidental broadening. Scenarios 1.151–1.153. |
| 7k. Core exceptions + `ModelModificationRestriction` ABC + XLSXField spotter (coverage-driven — May 12) | 21 | 21 | 21 | 0 | 0 |  Complete — 21 pass in 0.001s. Three files from the COMPLETE bucket. **`lex/core/exceptions.py`** (246 lines, dark before this batch): all 5 custom exception classes (`ValidationError`, `CalculatedModelError`, `ModelCreationError`, `ModelCombinationError`, `ModelClusteringError`, `CeleryDispatchError`) + 8 helper functions (`ensure_list`, `iter_exception_chain`, `select_preferred_exception_detail`, `select_preferred_stack_trace`, `find_exception_artifacts`, `resolve_exception_detail`, `resolve_exception_traceback`, `_normalize_string`). Pins the audit-dashboard-parsed message formats `[<model>] <msg> - Context: k=v` + the `group_index`-is-1-based-for-humans contract + the documented `resolve_exception_traceback` rule that it must NOT fall back to `str(exception)` (which is the message, not the traceback). **`lex/core/mixins/ModelModificationRestriction.py`** (111 lines): the ABC contract (defaults allow everything so partial-override is safe) + `AdminReportsModificationRestriction` (read-only profile shipped with HTMLReport — every write denied) + `violations`-list mutability + `ExampleModelModificationRestriction` template instantiation. **`lex/core/fields/XLSX_field.py`** coverage spotter (the file already has 378 lines of exhaustive test battery at `lex/tests/unit/api/test_xlsx_field.py` driving real openpyxl/pandas/xlsxwriter; the spotter pins the documented format-constant tuples + `max_length=300` migration sensitivity at the cluster-7 dashboard level). Scenarios 7.122–7.142. |
| 2. CRUD via REST API | 23 | 23 | 23 | 0 | 0 |  Complete (Many endpoint bulk-write contract reconciled to DELETE-only; BUG-004 timestamp assertions removed per product decision; BUG-005 fixed at framework level. Scenario **2.1** is the CRUD showcase test in CI) |
| 3. Validation Hooks | 8 | 9 | 9 | 0 | 0 |  Complete |
| 4. Permissions | 14 | 14 | 12 | 2 | 0 |  Complete (BUG-008/010 tracked; 4.40 added April 28 — `permission_export` full deny at the export endpoint; 4.41 added May 4 — full `permission_read` deny at detail endpoint leaks no domain fields / id) |
| 4e. Read-restriction filter backend (planned — April 21) | 7 | 7 | 5 | 0 | 0 |  Complete (5 pass + 2 skipped — 4.14/4.15 AuditLog DB-filter path deferred; needs Keycloak user_permissions + seeded AuditLog fixture; 4.41 pins detail GET read-deny serialization) |
| 4f. Serializer-level masking (planned — April 21) | 8 | 8 | 8 | 0 | 0 |  Complete — 4.19–4.22 structural (mixin machinery) + 4.23–4.26 `run_validation` end-to-end (change-detection, per-field deny, `lexReserved*` bypass, create-path `permission_create`) |
| 5. History & Bitemporal | 10 | 10 | 10 | 0 | 0 |  Complete |
| 5.11. History fallback-snapshot path (planned — April 21) | 1 | 1 | 1 | 0 | 0 |  Complete — 1 scenario drives `_get_snapshot(serializer_class=None)` directly; covers CONTROL_FIELDS filter + datetime/date `isoformat` + non-primitive `str()` coercion + primitive/list/dict passthrough |
| 5g. History `valid_to` chaining contract (Session 51 — May 5) | 2 | 2 | 2 | 0 | 0 |  Complete — 5.61 three-saves chain `valid_to → valid_from` end-to-end with latest row open-ended; 5.61b delete closes the chain with NULL `valid_to`. Closes the half of 5.4's docstring that the original test never asserted. |
| 5h. History suppression toolkit (Session 51 — May 5) | 6 | 6 | 4 | 1 | 1 |  Complete — 5.62 `save_without_historical_record()` (single-save toggle), 5.63 `untrack()` / `track()` (sticky between calls), 5.64 `bulk_create(skip_history=True)`, 5.65 default `bulk_create` still tracks. 5.66 (`suspend_bitemporal()` CM) marked `expectedFailure` — docs reference it but the public CM is not yet exposed; only the lower-level guards (`suppress_main_table_sync` etc., already covered by 9.7-9.10) ship today. 5.67 (`untracked_models` model_structure.yaml opt-out) deferred — needs a fresh test-project fixture. |
| 5i. History API contract (Session 51 — May 5) | 4 | 4 | 2 | 0 | 2 |  Complete — 5.71 endpoint response shape (every documented key: `history_id` / `valid_from` / `valid_to` / `history_type` / `user` / `snapshot` / `system_history` + snapshot carries domain fields), 5.72 `get_queryset_as_of(Model, t)` valid-time slice. 5.73 + 5.74 auto-skip when MetaHistorical* isn't wired (test_project's HistSimpleItem skips `register_standard_model()` so the L2 chain is not attached); production-registered models exercise the system-time branch in integration tests. |
| 5j. History snapshot completeness + `history_user` actor (Session 51 — May 5) | 2 | 2 | 2 | 0 | 0 |  Complete — 5.75 history rows carry every field's value (full snapshot, not a diff — pre-update row matches pre-update state, post-update row matches post-update state); 5.76 API-driven save stamps `history_user` to the authenticated user, `history_change_reason` defaults to falsy. |
| 5k. MetaHistory positive contract (Session 51 — May 5) | 4 | 4 | 0 | 1 | 3 |  Complete (auto-skip) — 5.81/5.82/5.84 + 5.83 expectedFailure landed as live tests but skip cleanly when MetaHistorical* isn't wired on the test fixture. The contract is real (docs explicitly enumerate `sys_from/sys_to` chaining, `history_object` FK, `meta_task_status` defaults), but the test_project's HistSimpleItem is declared as a private test-only model and skips the framework-boot `register_standard_model()` step that wires the L2 manager. Production registration covered at unit level by `lex.tests.unit.api.test_history_endpoint` + `lex.tests.unit.infra.test_bitemporal_service` (Mock-backed L2 manager). |
| 5l. Future-dated bitemporal saves — scheduled activation contract (planned — May 7) | 7 | 0 | 0 | 0 | 7 |  Not started — closes the **save side** of the future-activation handoff that 8.43 pins on the worker side. Concrete user scenario surfaced the gap: rename `test → test1 → test2` with `test2.valid_from = now + 1h` — main row must still read "test1" until activation fires, then catches up to "test2". Seven scenarios cover (5.91) main table NOT updated for future saves, (5.92) PeriodicTask + ClockedSchedule registered with correct args / local-scheduler call when CELERY_ACTIVE=false, (5.93) the `> now + 5s` grace boundary, (5.94) reschedule-on-edit cleanup, (5.95) end-to-end run of `activate_history_version` flips main row + meta status, (5.96) delete cancels queued task and flips meta to `CANCELLED` (`on_history_pre_delete__cancel_schedules`), (5.97) multiple future-dated rows queue independently. No new fixtures — reuses `HistSimpleItem`. |
| 6. Audit Logging | 10 | 10 | 7 | 2 | 0 |  Complete (1 still skipped — 6.4 retired in favour of 6.45's reachable-via-`pre_validation` path; BUG-001 family tracked on 6.10; BUG-007 tracked on 6.7) |
| 6d. Audit-log payload + GenericForeignKey (Session 51 — May 5) | 8 | 8 | 6 | 0 | 2 |  Complete — 6.41 `content_type` + `object_id` populated on create (the GFK that lets the per-record Audit Log Tab UI find rows); 6.42 create payload carries the full request body + post-save `id`; 6.43 update payload refreshed to full post-save state (not a diff); 6.44 delete payload preserves pre-delete state; 6.45 `pre_validation` raise → failure audit row with status='failure' + traceback (the framework already implements this — replaces the previously-skipped 6.4); 6.46 failure traceback round-trips with the exception class name preserved. 6.47 (atomic-block failure queues replacement) and 6.48 (pending state observable mid-flight) skipped — need failure-injection seam / pre_save signal-spy fixture. |
| 6e. Bulk audit (`BulkAuditLogMixin`) (Session 51 — May 5) | 3 | 3 | 2 | 0 | 1 |  Complete — 6.51 `DELETE /many/?ids=…` of 3 records produces 3 audit rows all with action='delete' + status='success'; 6.53 each bulk-delete audit row's `content_type` + `object_id` points back at its own pre-delete instance (per-record Audit Log Tab still works after the bulk op). 6.52 partial-success contract skipped — needs a per-row `permission_delete` deny fixture / DB error injection. |
| 6f. Audit-log resilience (Session 51 — May 5) | 3 | 3 | 3 | 0 | 0 |  Complete — 6.61 retryable PG `40P01` retries 2× with exponential backoff (BASE × 2⁰, BASE × 2¹) then succeeds; 6.62 exhaustion re-raises original exception with pgcode preserved + non-retryable error propagates immediately; 6.63 split into input-validation half (`safe_get_content_type(None)` raises `ValueError`) + recovery half (first `get_for_model` raises `DoesNotExist`, helper clears cache and retries with the real ContentType). Pins `RETRYABLE_SQLSTATE_CODES = {"40P01", "40001"}` + `MAX_UPDATE_RETRIES = 3` constants so a regression that drops a code is caught at this level. |
| 6g. Audit-log immutability (Session 51 — May 5) | 3 | 3 | 3 | 0 | 0 |  Complete — 6.71 `AuditLog.permission_create` returns `False` (system-created only); 6.72 `permission_delete` returns `False` even for admin (the only way to remove rows is direct DB access — compliance-team operation, not a customer one); 6.73 `permission_edit` returns `PermissionResult(allowed=False)` with the documented "read-only" reason substring. Plus 6.73b sub-pin: `AuditLogStatus` permission methods, where exposed, also deny — a regression that opens write access there would let an attacker flip `failure → success` and hide errors. |
| 6j. Audit-utils singletons & data-model contracts (coverage-driven — May 12) | 16 | 16 | 16 | 0 | 0 |  Complete — 16 pass in 0.022s. `SimpleTestCase`-only batch covering two small audit-pipeline files. `lex/audit_logging/utils/config.py` (`AuditLoggingConfig` env parser + global singleton): unset → default True, blank/whitespace → default, every TRUE_VALUES / FALSE_VALUES entry case-insensitive, gibberish raises with helpful message naming the env var + accepted values, constants pinned (drift canary against `INITIAL_DATA_AUDIT_LOGGING` rename or synonym drop), summary-dict shape, singleton memoized + `reset_audit_logging_config` forces re-read, `is_audit_logging_enabled` passes through, `create_with_validation` returns validated instance. `lex/audit_logging/utils/DataModels.py` (dataclasses + exception hierarchy): `ContextInfo` minimal construction (only `calculation_id` + `audit_log` required, all 7 optional fields default to None — promoting any to required would crash every audit writer), `CacheCleanupResult.__post_init__` normalises None → empty list (callers iterate directly, would `TypeError` mid-cleanup), exception hierarchy `CalculationLogError` → `CacheOperationError` (+`cache_key`) / `ContextResolutionError` (+`stack_length`) — operator dashboards parse these diagnostic fields for triage, silent drops would lose signal. Scenarios 6.80–6.95. |
| 6n. `AuditLogSerializer` + `AuditLogMixinSerializer` surface (coverage-driven — May 12) | 15 | 15 | 15 | 0 | 0 |  Complete — 16 pass in 0.009s (15 scenarios; 6.142 + 6.142b are sub-letter siblings). ROI batch #3 of the May 12 sweep — `AuditLogSerializer.py` 37.65% + `AuditLogMixinSerializer.py` 53.73% baselines. **`AuditLogReadOnlySerializerMixin`** (6.141 lock `{edit:[], delete:False, export:True}` regardless of caller — drift here would silently let a non-system caller PATCH/DELETE audit rows and erase compliance evidence). **`AuditLogDefaultSerializer` status-record SerializerMethodFields** (6.142 `_get_latest_status_record` uses `_prefetched_objects_cache` when present, returns LAST entry as newest, no DB hit — pins the AG Grid list-view fast path; 6.142b empty prefetch list → None still no DB hit; 6.143 fallback to `status_records.order_by('-created_at').first()`; 6.144 all 5 computed fields return None for the mid-flight zero-status-records case operators see right after audit row creation; 6.145 live record values flow through with isoformat for datetimes). **`get_calculation_record` builder** (6.146 None when `object_id` is None — line 81 early-return; 6.147 None when `safe_get_content_type` raises — defensive try/except at lines 90-93 prevents 500 on stale ContentType; 6.148 full output shape + display-name fallback chain `short_description > name > display > "<model> #<pk>"`; 6.149 `details` only includes `is_calculated` + `error_message` when present in payload — pin so other payload keys (potentially PII) cannot leak into the drill-down preview; 6.150 non-dict payload (None/list/int/str) treated as `{}` — defensive against older bulk-op audit rows). **Module wiring** (6.151 `AuditLog.api_serializers` registry default+reference + `_lex_skip_serializer_alias` + `_lex_hidden_serializers` set — frontend reads these for AG Grid + per-record link widget). **`AuditLogMixinSerializer` helpers** (6.152 `_iso_seconds` over None / tz-naive / tz-aware non-UTC / UTC; 6.153 `_serialize_file_reference` shape + `.url`-raises path + missing-name short-circuit; 6.154 `generic_instance_payload` round-trip via real `AuditLog` row, pins editable-concrete-fields surface and pk → `id` injection; 6.155 `_serialize_payload` walks every coercion branch — dict/list recursion, datetime/date/time isoformat, Decimal/UUID/Promise to str, Model → `{id, display}`, QuerySet/set → list, fallback `str(obj)` and the unserializable last-resort placeholder). Scenarios 6.141–6.155. |
| 7. Calculation State Machine | 94 | 94 | 93 | 1 | 0 |  Complete (BUG-009 tracked; 7.32–7.47 = 7i 2-level atomicity matrix, 16 scenarios; 7.48–7.111 = 7j 3-level grandparent/parent/child matrix, 64 scenarios — every combination of gp/p/c atomicity × pass/fail) |
| 7g. `CalculatedModel.create()` pipeline (coverage-driven — April 21) | 6 | 6 | 6 | 0 | 0 |  Complete — `CombinatorialCalc.create()` walks combination generator → prepare → cluster → dispatch (sync) → `calc_and_save_sync` → `delete_models_with_same_defining_fields` end-to-end. Drove `CalculatedModelMixin.py` from **33.74% → 64.75%** (+31 pts / +170 lines) in one pass. Covers cartesian expansion, `get_selected_key_list` fallback, partial-failure accumulation, idempotent rerun, empty-key-list branch pruning, and pk-reset on missing duplicate. **Note (April 23):** the 7g import was broken — `CombinatorialCalc` was referenced but missing from `calculations/models.py`. Fixture added in the April 23 session so the 6 scenarios actually execute (they had been counting as passing in the dashboard while silently failing to import). |
| 7h. `_dispatch_model_processing` Celery branch (coverage-driven — April 23) | 1 | 1 | 1 | 0 | 0 |  Complete — drives the `CELERY_ACTIVE=true` + `hasattr(calculate, 'delay')` async branch of `_dispatch_model_processing` that 7g's synchronous path never reached. Stubs `.delay` onto `calculate` and patches `CeleryTaskDispatcher.dispatch_calculation_groups` so no broker is required; asserts the dispatcher is invoked with 2 flattened groups (4 models) and that no rows leak through to the sync fallback. |
| 8. Celery & Async | 6 | 6 | 6 | 0 | 0 |  Complete |
| 8g. Task infrastructure (planned — April 24) | 9 | 9 | 9 | 0 | 0 |  Complete — drives the 958-line `lex/lex_app/celery_tasks.py` (CallbackTask on_success/on_failure, CeleryCalculationContext, FireAndForget + WaitForTasks dispatch routing, lex_shared_task wrapper). **Redis-free**: patches `.delay` to return MagicMock AsyncResult; swaps `allow_join_result` for `nullcontext` on the WFT join path; instantiates `CallbackTask` directly and drives real ORM + real signals without running Celery itself. |
| 8h. Celery **eager-mode** end-to-end (planned — April 24) | 6 | 6 | 6 | 0 | 0 |  Complete — closes the gap 8g couldn't: the real `.delay()` → `@lex_shared_task` wrapper → task body → `CallbackTask.on_success`/`on_failure` sequence under `CELERY_ACTIVE=true`. **Broker-free**: uses Celery's own `app.conf.task_always_eager=True` + `task_eager_propagates` knobs — `.delay()` runs inline, real `EagerResult` returned, real Celery trace machinery invokes the callbacks. Covers: 8.16 success round-trip, 8.17 failure round-trip (propagate=False so on_failure actually fires), 8.18 `EnhancedBoundTaskMethod` + `WaitForTasks` drains real EagerResult on scope exit, 8.19 `CeleryTaskDispatcher.dispatch_calculation_groups` runs 2 groups via real `calc_and_save.delay`, 8.20 `is_celery_worker_process()` returns True inside an eager task body, 8.21 `register_task_with_context` FF-over-WFT priority. Coverage: `celery_tasks.py` 46% → 55%; `CeleryTaskDispatcher.py` ~0% → 45% (first real coverage). |
| 8i. `WaitForTasks` / `FireAndForget` scope contracts (planned — April 24) | 9 | 9 | 9 | 0 | 0 |  Complete — exhaustive coverage of the customer-facing dispatch primitives documented in `docs/lex_topics/12-celery-async-dispatch.md`: priority hierarchy, nesting, filters, no-op behaviour, exception propagation. 8.22 FF nested inside WFT partitions results (outer WFT sees parent tasks, inner FF sees its own, never crossed — real eager). 8.23 nested WFT scopes — inner exit drains only its own results, outer's result untouched across the nested scope boundary (real eager). 8.24 `WaitForTasks(include_tasks=…)` dispatches only the matching task name, non-matches run sync. 8.25 `WaitForTasks(exclude_tasks=…)` keeps excluded task names sync inside an otherwise-async block. 8.26 `FireAndForget(force_tasks=…)` inside an outer WFT — only the named subset is FF-forced, non-matches fall through to WFT rules. 8.27 **No-op when `CELERY_ACTIVE` unset** — entering WFT/FF pushes nothing onto the ContextVars, `.delay` is never called, task body runs sync (the "your code doesn't change" contract). 8.28 exception propagation — a task raising inside WFT surfaces the exception on scope exit with `propagate=False` so Celery's trace machinery invokes on_failure first, then re-raises via `.get()`. 8.29 multiple dispatches in one WFT block all drained on exit — every row reaches SUCCESS. 8.30 FF exit does NOT drain `dispatched_results` — that is the behavioural difference from WFT, only explicit `wait_for_completion()` drains. |
| 8j. Celery task bodies — `load_data` / `calc_and_save` / `activate_history_version` (coverage-driven — April 24) | 14 | 14 | 14 | 0 | 0 |  Complete — closes Cluster-A/B/C bucket C. Drives the three task bodies in `lex/lex_app/celery_tasks.py` (lines 696–957) that 8g/8h/8i never reached. **load_data** (6): early-return gate, LEGACY+worker direct setUp, LEGACY+sync asyncio bridge, non-LEGACY setUpCloudStorage, exception → re-raise + `finalize_batch(failure_error=…)`, None-audit-logger `finally` guard. **calc_and_save** (3): happy path summary, IntegrityError → `delete_models_with_same_defining_fields` → pk-rewire → retry save (conflicts_resolved bump), generic exception re-raise. **activate_history_version** (5): `apps.get_model` LookupError → `"failed_model_lookup"`, history `DoesNotExist` → `"skipped_missing_record"`, valid_from > now+5s → `"failed_too_early"`, happy path → `sync_record_for_model` + Meta `SCHEDULED→DONE` → `"success"`, sync-blowup re-raise. Broker-free: `@lex_shared_task` descriptor routes to `self.task(...)` with `CELERY_ACTIVE` unset, invoking real bodies synchronously. |
| 8k. Redis broker integration examples (opt-in — May 4) | 2 | 2 | 2 | 0 | 0 |  Complete — two real Redis-backed round trips: 8.45 JSON-safe producer → Redis queue → worker → Redis result backend, and 8.46 `CalculationModel` + `WaitForTasks` dispatch over Redis with `CallbackTask.on_success` flipping the row to SUCCESS. Env-gated by default (`LEX_RUN_REDIS_CELERY_TESTS=true`, optional `LEX_CELERY_REDIS_TEST_URL`) so local runs without Redis skip cleanly; verified passing against a temporary local Redis on port 6380 and wired into reusable `celery_redis_broker_example.yml`, which `pip_publish.yml` calls before publishing. |
| 9. Signals & WebSocket | 6 | 6 | 6 | 0 | 0 |  Complete |
| 9.7–9.10. Bitemporal signal branches (planned — April 21) | 4 | 4 | 4 | 0 | 0 |  Complete — guard lifecycle, nested stacking, cross-guard independence, thread-local isolation; covers the suppression primitives at the heart of `bitemporal_signals.py` |
| 9d. `ActiveCalculationStateStore` full surface (coverage-driven — May 12) | 24 | 24 | 24 | 0 | 0 |  Complete — 24 pass in 0.009s. `SimpleTestCase`-only batch covering the in-memory authoritative store of active calculations: mutators (`mark_in_progress` / `clear` / `clear_all`) including empty-id no-ops + payload defaults + `int → str` pk normalisation + idempotent re-marking; accessors (`get_calculation_id` / `get_entry`) including the defensive `dict(entry)` copy that stops snapshot consumers from mutating the store under the lock; private helpers (`_split_record_id` rsplit semantics for model names containing underscores + malformed-input rejection, `_find_model_by_name` registry walk, `_resolve_model_and_pk` over `model_label`-preferred path + `record_id`-fallback path + `non-CalculationModel` reject + total-failure path); `snapshot()` reconciliation including empty-store fast path + live-vs-stale pruning + DB-exception keep-on-error + unresolvable-entry passthrough; `validate_and_prune()` startup sweep including empty-store fast path + only-IN_PROGRESS-survives + DB-exception drop (documented behavioural difference from `snapshot()` — startup sweep is conservative-rebuild). Mocks DB at the `_resolve_model_and_pk` boundary so the test stays SimpleTestCase. **Coverage:** 27.03% → ~95%+. Scenarios 9.11–9.28 (with sub-letters 9.27b / 9.28b–f). |
| 10. API Layer | 9 | 9 | 8 | 0 | 0 |  Complete (10.7 gates supported Many bulk DELETE; 10.9 validation duplicate skipped for now; 10.8 retired — see BUG-009 tracked at 7.14 only) |
| 10e. Schema introspection (planned — April 21) | 4 | 4 | 4 | 0 | 0 |  Complete (`create_field_info` type mapping + pk/required/default flags + FK target; structure-tree pruning collapses denied-model folders. **BUG-015 surfaced** — CharField without explicit default reports `required=False` because Django's `get_default()` returns `""` — documented in-test, not xfailed) |
| 10f. Global search (planned — April 21) | 4 | 4 | 4 | 0 | 0 |  Complete — 10.15 hit shape, 10.15b sentinel string on zero matches, 10.16 EXCLUDED_MODELS enforcement, 10.16b EXCLUDED_TYPES regression gate |
| 10h. `LexAPI` outbound-client SDK (coverage-driven — May 12) | 7 | 7 | 7 | 0 | 0 |  Complete — 7 pass in 0.005s. ROI batch #5 of the May 12 sweep — `lex/api/views/lex_api/LexAPI.py` 14.58% baseline (38 stmts, 31 missed). Tiny file, easy +0.3% project-wide. `SimpleTestCase`-only — `requests` patched with `MagicMock`. **`build_attachments_from_paths`** (10.17 known mimetype `.pdf` → `application/pdf` + base64 round-trip + missing extension → `application/octet-stream` documented fallback — silent drop to `None` would crash every downstream MIME parser). **`send_email`** (10.18 `DEPLOYMENT_ENVIRONMENT` blank → silent no-op never POSTs — the documented "test runs don't blast real emails" guard, regression here means every CI run sends production-ish email; 10.19 happy path: URL `https://<DOMAIN>/api/send_email/` + `Authorization: Api-Key <key>` + JSON body with `subject`/`emails`/`body`, returns response.json(), and pin: NO `attachments` key when none supplied so the platform service doesn't reject a payload with a stray empty list; 10.20 `attachments` only included when truthy — empty list still absent, populated list passes through verbatim; 10.21 non-200 raises so callers route through their error handler instead of silently believing the email landed). **`get_client_roles`** (10.22 happy path: URL + `params={"keycloak_client_id": ...}` querystring placement — the platform's GET handler ignores body data, drift here breaks every Keycloak-roles read; 10.23 non-200 raises — a regression that returned `[]` on 403 would silently grant zero-role access, locking customers out without any visible error). |
| 11. Stress & Performance | 20 | 20 | 15 | 2 | 0 |  Complete (3 skipped — 11.12 concurrent-writes, 11.15 audit throughput, 11.19 at SMALL only; BUG-011 tracked) |
| 12. Serializer Contract | 32 | 32 | 29 | 3 | 0 |  Complete — 12a read contract (5 pass) + **12a history/meta scopes (3 pass — 12.6 history-row unwraps to main model's `permission_read`, 12.7 MetaHistorical scopes immutable `{"edit": [], "delete": False, "export": False}`, 12.8 `lex_reserved_scopes.edit` mirrors `permission_edit` `allow_fields`)** + 12b type round-trip (9 pass + 2 xfail — BUG-012 timezone-naive DateTime output, BUG-013 FK dict PATCH payload) + 12c list & many read contract (3 pass) + 12d AuditLog payload (3 pass) + **12e factory contract (6 pass + 1 xfail — 12.26 `model2serializer` internals, 12.27 custom fields + internals, 12.28 serializer cache xfail / BUG-017, 12.32 source default override alias, 12.33 history alias inheritance, 12.34 meta-history alias chain, 12.35 `hide_actions_column` metadata)**. |
| 12f. Serializer write paths — M2M & nested FK (planned — April 21) | 3 | 3 | 3 | 0 | 0 |  Complete — 12.29 M2M create-with-set, 12.30 PATCH-replace, 12.31 FK attach/rewire/detach lifecycle. **Note (April 23):** scenario numbers renumbered from 12.26/12.27/12.28 → 12.29/12.30/12.31 to free up 12.26–28 for the canonical 12e factory-contract scenarios in ../test-clusters.md. `TagItem` / `TaggableItem` / `EditScopedItem` fixtures added to `serializers/models.py` (the originals were missing so 12f was silently failing to import). |
| 13. Export Endpoint | 12 | 12 | 12 | 0 | 0 |  Complete — 13a legacy (4 pass) + 13b AG Grid flat (5 pass) + **13c grouped & selected (1 pass — 13.9 `groupKeyPaths` filter + `_coerce_group_key` string→int FK coercion + `"null"` → `__isnull=True` sentinel; asserts selected subset is strictly smaller than baseline and hits the expected group count)** + **13d auth & edge (2 pass — 13.11 unauthenticated POST → 401/403 with no streamed bytes, 13.12 non-uniform per-object `permission_export` forces `_compute_uniform_export_mask → None` and the slow per-row mask correctly allows `amount` on row B while blanking it on row A)**. BUG-014 fixed in 13b session. |
| 14. AG Grid Query Endpoint | 25 | 25 | 24 | 0 | 0 |  Complete — baseline 14a/14b/14c (20 scenarios) landed earlier; 14e extended with 4 pass + 1 skip (BUG-016 deferred) covering text/number filter op variants, legacy `condition1`/`condition2` shape, multi-field CSV ordering, and the `blank`/`notBlank` unreachable-branch bug |
| **Total** | **555** | **555** | **534** | **3** | **9** | All cluster 5/6 doc-vs-test gap sub-clusters (5g/5h/5i/5j/5k + 6d/6e/6f/6g) implemented in Session 51. 33 of 35 newly-planned scenarios landed; 2 documented for later (5.67 model_structure.yaml fixture, 5.83 retroactive valid_from). 5.67/5.66/5.83/5.74 + 5.73 + 5k all auto-skip cleanly when their fixture prerequisites aren't met (suspend_bitemporal CM not exposed; MetaHistorical wired only via process_admin's register_standard_model bootstrap, which the test-only HistSimpleItem fixture skips). 6.47/6.48 deferred (need failure-injection seam / pre_save signal-spy fixture). 6.45/6.46 documented as expectedFailure during planning but landed as live passes — the framework already writes the failure audit row through the validation-hook path. Re-enabled the BUG-007 expectedFailure marker on 6.7 (was commented out). Session 50 added 1j/1k/1l (Keycloak client safety pre-flight — 20 mocked + 11 env-gated integration skips, plus the framework-side `verify_client_is_safe_for_init` + `--skip-client-preflight` flag). Session 39 consolidated 1c/1f/1i under `tests/init/initial_data/` (no test changes — pure folder move + package-level docstring). Session 40 added 1m (PyCharm `.run.xml` ↔ `lex` CLI cross-file contract — 8 pass). Session 41 reconciled the Many endpoint contract to DELETE-only, retired BUG-006, restored missing scenario-numbered methods for 8.5/8.6 and 9.1/9.2/9.3/9.6, removed BUG-004 timestamp assertions, fixed BUG-005 at framework level, and skipped BUG-016. Session 42 added 7i's exhaustive calculation atomicity matrix (16 pass), including the atomic-parent rollback cases. Session 44 retired scenario 10.8 (BUG-009 API-trigger xfail) and added scenario 4.40 — sub-cluster 4h: `permission_export` full deny at the export endpoint, closing the last reachable cell in the Cluster 4 deny matrix. Session 45 added 7j (3-level grandparent/parent/child atomicity matrix, **64** parametrically-generated scenarios, all pass) — surfaced two previously-undocumented framework rules (failing level only wiped by *outermost* atomic rollback; successful descendant wiped by *any* atomic ancestor). Session 46 added 4.41 — detail-endpoint full `permission_read` deny returns `{}` with no domain-field or `id` leak. Session 47 added 8k — opt-in Redis broker integration smoke test for the real Celery producer/worker/result-backend round trip. Session 48 added Cluster 12e serializer alias inheritance tests for source/history/meta-history tables plus `hide_actions_column` metadata preservation. Session 49 extended 8k with `CalculationModel` + `WaitForTasks` over Redis and wired the broker workflow into `pip_publish.yml` as a release gate. Session 50 walked the docs-vs-tests audit and queued the gap sub-clusters that Session 51 just implemented. |

**Status legend:**
-  Not started — no tests written yet
-  In progress — some scenarios implemented, not all passing
-  Complete — all scenarios implemented and passing (or marked as expected failure with tracked bug)
-  Blocked — depends on a framework fix before tests can pass

---

## What Counts as "Done" for a Cluster

A cluster is ** Complete** when:

1. ✅ Every scenario from [test-clusters.md](../test-clusters.md) has a corresponding test method
2. ✅ All tests either **pass** or are marked `@unittest.expectedFailure` with a tracked bug reference
3. ✅ Tests use the canonical customer code path (no `skip_hooks`, no `calculate_hook()` in E2E tests)
4. ✅ Tests run in CI without flakiness (3 consecutive green runs)
5. ✅ No mocks on the class under test or the ORM

A cluster is ** In progress** when some but not all of the above are true.

---

## Known Bugs Tracker

Bugs discovered by the new test suite. Each has a corresponding test marked `@unittest.expectedFailure`.

| # | Bug | Severity | Cluster | Test | Status |
|---|-----|----------|---------|------|--------|
| BUG-001 | `LexModel.save()` wraps IN_PROGRESS + hooks in single `transaction.atomic()` — rollback erases IN_PROGRESS history | High | 5, 7 | `test_05_08`, `test_07_02` | **Resolved** (5.8 passes — IN_PROGRESS row persists. The audit-log-survives-rollback half is split out as **BUG-001b**.) |
| BUG-001b | When a calculation fails inside its own `transaction.atomic()` and an outer atomic block also rolls back, the audit-log entry written by `AuditLogMixin` is wiped along with the calculation state — **even though the audit log is supposed to be the durable evidence that the operation was attempted**. The audit row needs a separate-connection write (or `transaction.on_commit`-style escape) so it survives the rollback. Sibling of BUG-001; surfaced when 6.10 was re-enabled in Session 50. | High | 6 | `test_6_10` | Open |
| BUG-002 | Startup reset uses `queryset.update()` instead of `.save()` — bypasses history signals, no ABORTED history row | Medium | 7 | `test_07_xx` | Open |
| BUG-003 | `reconcile_changes_since()` sets `synced_count` but never returns it | Low | 5 | `test_05_xx` | Open |
| BUG-004 | `edited_at` not populated on create/update timestamp path | Low | 2 | retired (`test_2_2`, `test_2_14` removed per product decision) | **Retired** |
| BUG-005 | Validation errors return **500** instead of **400** — missing required fields, wrong field types, and invalid PATCH payloads all bypass DRF's standard ValidationError-→-400 conversion | High | 2 | `test_2_3`, `test_2_4`, `test_2_15` | **Resolved** (framework now preserves DRF `ValidationError` as HTTP 400; duplicate API-layer scenario 10.9 skipped for now) |
| BUG-006 | `model-many-entries` endpoint rejects POST with 405 — no bulk-insert via API | Medium | 2, 10 | retired (`POST /many/` and `PATCH /many/` are not supported contracts; bulk write support is DELETE-only) | **Retired** |
| BUG-007 | Actor resolution on API create path may not populate `created_by` with the authenticated user's email — sibling of BUG-004; revisit once the authenticated-context stamping step is reviewed. | Medium | 6 | `test_6_7` | Open |
| BUG-008 | Authorization failures return **400** with a JSON `message` body instead of **401/403**. Customer intent: unauthorized actions get a proper HTTP auth status so frontends/integrations can route them through the standard error path. | High | 4 | `test_4_5`, `test_4_6` | Open |
| BUG-009 | `CalculationModel.is_calculated` is `editable=False`, so API PATCH requests that set it to `IN_PROGRESS` are silently dropped by the serializer — the documented "API triggers calculation" path (scenario 7.14) cannot work. Fix: expose `is_calculated` as a write-through state field for PATCH while keeping it read-only elsewhere, OR document a dedicated trigger endpoint. The previous API-layer companion test (10.8) was retired; coverage stays at the calculation-state-machine layer (7.14). | High | 7 | `test_7_14` | Open |
| BUG-010 | ``permission_edit`` field-level restriction is not enforced on PATCH. A non-admin PATCHing a field outside the permitted set (e.g. ``secret`` on ``ProtectedItem``, where only ``name`` is allowed) receives 200 and the restricted field IS mutated. The serializer's permission-aware field filtering runs on READ but not on WRITE. | High | 4 | `test_4_4` | Open |
| BUG-011 | List endpoint evaluates ``permission_read`` **once per row** instead of once per request. Observed: a 100-row page produces **500 calls** (5× per row) to the model's ``permission_read``, and the endpoint issues ~``n + k`` SQL queries per page where ``n`` is the page size. At 20k rows this turns every list/export into a minutes-long stall. The documented contract is one permission check per user per request; the serializer's permission-aware field filtering should reuse a single ``PermissionResult`` per request instead of re-invoking per row. | High | 11 | `test_11_3`, `test_11_14` | Open |
| BUG-012 | ``DateTimeField`` is serialized as a **timezone-naive** ISO-8601 string (``"2026-04-21T15:30:45"``) even when the stored value is tz-aware UTC. The UI therefore shows the value in the server's local timezone instead of UTC, and PATCH round-trips silently drift. DRF's default contract is tz-aware output — something in the serializer chain is stripping ``tzinfo``. | High | 12 | `test_12_10` | Open |
| BUG-013 | PATCH rejects the documented ``{"related": {"id": X}}`` dict payload for ``ForeignKey`` fields with DRF's ``Incorrect type. Expected pk value, received dict.`` error — returned as 500 because BUG-005 is also in play. The ``LexSerializer._parse_value_for_field`` FK-dict handling only runs on the audit-log shadow-instance path; the real PATCH-on-main-model path never gets it. Contract (from the frontend's save flow + ``get_lex_reserved_scopes`` round-trip shape): the same dict shape that comes out on GET must be accepted on PATCH. | High | 12 | `test_12_15` | Open |
| BUG-014 | **Every flat Excel export shipped with only the last row populated.** ``ModelExportView.post`` enabled ``xlsxwriter`` ``constant_memory`` mode whenever there was no hierarchy depth (i.e. on every flat export) and called ``df.to_excel(..., index=True, freeze_panes=(1, 1))``. ``constant_memory`` flushes each sheet row as soon as any write advances to a later row; pandas writes the index column (column A) **after** the data columns B..N for each row, which is a backward write. Earlier rows lost every cell, including ``id``. Surfaced on first run of Cluster 13 with a 3-line isolated repro. | **Critical** | 13 | `test_13_2a`, `test_13_2b`, `test_13_3`, `test_13_4` | **Resolved** (fixed in-session; all four regression tests now pass and stand as permanent gates against re-introducing ``constant_memory`` + ``index=True``) |
| BUG-015 | ``create_field_info`` reports ``required=False`` for any ``CharField`` without an explicit ``default=`` argument because Django's ``Field.get_default()`` returns the empty string ``""``, so the framework's ``required = not (field.null or default is not None)`` short-circuits to False. A POST missing ``name`` still fails at the DRF serializer layer (HTTP 400), so the frontend schema and the backend validation disagree on whether the field is mandatory — the UI renders the field as optional and the user only sees the error after submitting. Low severity (contract mismatch, not a data-integrity issue). Documented in ``test_10_12`` with an explicit code comment; the test currently asserts the observed behaviour rather than xfailing. | Low | 10 (10.12) | `test_10_12_field_info_flags_editable_required_default_pk` | Open |
| BUG-016 | **AG Grid ``blank`` and ``notBlank`` filter operations are silently unreachable in `_build_filter_q`.** The text branch's early-return `if value in (None, ""): return None` fires before the `blank` / `notBlank` dispatch can run. The number branch's guard `if value in (None, "") and operation_type != "blank"` omits `notBlank` from the bypass. The date branch's guard `if operation_type != "blank" and not date_from_raw` also excludes `notBlank`. Customer symptom: clicking the "Blank" or "Not Blank" option in the grid's filter header does nothing — every row is returned. The date branch does special-case `blank` correctly, proving the fix pattern; text and number branches need the same treatment, and all three branches must add `notBlank` to the bypass. | Medium | 14 (14e) | `test_14_25_blank_and_not_blank_ops_do_not_work_bug016` | Open — skipped/deferred |
| BUG-017 | **`get_serializer_map_for_model` has no in-process cache.** Every call rebuilds the DRF serializer class via `model2serializer` + `_wrap_custom_serializer`, so two successive calls on the same model return two distinct class objects (`first["default"] is not second["default"]`). Customer impact: every REST request rebuilds the serializer class tree, burning CPU on DRF field-registration work that is pure function of the model class; identity-based callers (DRF's internal field registration, metadata endpoints that compare class references) cannot rely on reference stability. The `ModelContainer.get_serializers_map` layer already caches by a signature hash, so the leak is at the factory itself, not at the container. Fix: memoize the factory by `(model_class, tuple(default_fields or ()))`. | Medium | 12 (12e) | `test_12_28_get_serializer_map_returns_same_class_per_model` | Open |
| BUG-018 | **`ModelExportView._normalize_ag_request` silently overwrites the caller's `endRow` with `MAX_AG_EXPORT_ROWS` (1,000,000).** Line ~151 of `lex/api/views/file_operations/ModelExport.py` does `request_payload["endRow"] = MAX_AG_EXPORT_ROWS` unconditionally, before any downstream path reads it. The streaming cap loop at line ~1229 (`if written >= end_row: break`) is therefore dead code — `end_row` is always 1M. Customer symptom: every "export only the first N rows" UI affordance (AG Grid's pagination window, `startRow`/`endRow` for "export current page") silently falls through to a full-table dump. Bandwidth + spreadsheet-size blowup on large tables; no data-integrity risk. Side finding: scenario 13.7 ("`endRow` over `MAX_AG_EXPORT_ROWS` → clamped silently") passes for the wrong reason — the same line happens to clamp the over-cap direction correctly via clobber rather than `min()`. Fix: `request_payload["endRow"] = max(1, min(int(raw or MAX), MAX))` — preserves user values ≤ MAX, clamps above. | High | 13 (13e) | `test_13_17_end_row_limit_caps_written_rows` | Open |
| BUG-019 | **`FireAndForget(exclude_tasks=…)` — doc vs code disagreement, no test pre-commits us.** Surfaced during 8i design (April 24). `FireAndForget.__init__` docstring (`celery_tasks.py` ~line 295) promises: *"exclude_tasks: Task names that should follow WaitForTasks rules."* The code at `EnhancedBoundTaskMethod.__call__` lines ~527–529 does **not** follow WFT rules — when a task name is in `ff_ctx.exclude_tasks` it `return self.task(self.instance, *args, **kwargs)` **before** `await_ctx.should_dispatch(task_name)` is consulted, forcing synchronous execution even when an enclosing `WaitForTasks` would otherwise have dispatched it. Two defensible interpretations: (a) docstring is right — excluded tasks should fall through to WFT and honour its include/exclude rules; (b) code is right — excluded tasks are pinned to sync regardless of WFT, as a "force sync" escape hatch. 8i covers `FF(force_tasks=…)` (scenario 8.26) but **deliberately does not test `FF(exclude_tasks=…)`** — writing a test either way pre-commits the framework to one interpretation before product has decided. Zero customer reports either way. **Action: raise with product; pick an interpretation; then add the scenario and update the docstring OR the code to match.** Severity Low because no production symptom has been reported, but the ambiguity is a latent footgun for any customer who reads the docstring and relies on WFT-style fall-through. | Low | 8 (8i) | *(no test — deliberately unpinned)* | **Open — design decision pending** |
| BUG-020 | **`suspend_bitemporal()` context manager documented but not implemented.** `docs/features/tracking/bitemporal history.md` references a customer-facing CM (`with suspend_bitemporal(): obj.save()` → 1 query, 0 history rows, 0 meta rows) but the framework only ships the lower-level ContextVar guards (`_suppress_history_valid_to_chaining`, `_suppress_main_table_sync`) which are covered by Cluster 9.7–9.10. The composed CM that wraps both guards into a single user-facing API does not exist. Customer impact: anyone reading the docs and trying `from lex.core.services.bitemporal_signals import suspend_bitemporal` gets `ImportError`; the documented workflow for "rebuild this record's history without re-firing chaining + sync" silently does not work. Fix: ship a `@contextmanager` that enters both guards, plus a public re-export. | Medium | 5 (5h) | `test_5_66` | Open |
| BUG-021 | **Retroactive `valid_from` correction on user `save()` is overwritten with `now()`.** Surfaced when the 5k probe was fixed in Session 53 — `test_5_83` now runs against a wired L2 manager instead of silently skipping. Documented intent (`docs/features/tracking/bitemporal history.md` "retroactive corrections" section): setting `obj.valid_from = earlier; obj.save()` should land the L1 row at `earlier` while the L2 row's `sys_from` reflects the clock time of the correction. Observed: the L1 row's `valid_from` is silently rewritten to `now()` — the customer-supplied earlier date is dropped on the floor. Root cause: the user-save path does not honour `instance.valid_from` (only `_history_date`, the framework-internal hook used by `backfill_bitemporal_history` + scheduled activations). Fix: either (a) propagate `instance.valid_from` → `_history_date` in `LexModel.save()` for user saves, OR (b) document that retroactive corrections require the `_history_date` private hook and rewrite the docs accordingly. | Medium | 5 (5k) | `test_5_83` | Open |
| BUG-022 | **Stale source-comment in `on_history_saved__chain_valid_to` claimed the L2 layer collapses `valid_to`-refinement saves into an in-place update.** Surfaced when the 5k probe was fixed in Session 53 — `test_5_82` ran for the first time and saw 5 L2 rows for 3 saves instead of the comment-promised 3. After tracing the data flow (Session 53b discussion): the 5-row outcome is **correct system-time semantics**, not a bug. Each closing of a previous L1 row's `valid_to` is itself a save() against that L1 row, and the L2 layer's job is to record "what did the system know at instant T?" — so the system genuinely needs separate L2 rows for the open-ended view (before the chaining save) and the closed view (after). Without the extra rows, system-time `?as_of=...` queries lose fidelity. The never-wired `_strict_chaining_update` flag was an aspiration that would have *broken* system-time correctness if it had shipped. Fix landed: source comment rewritten to match the real contract; `test_5_82` rewritten to assert `2N − 1` L2 rows (3 saves → 5 rows) plus a contiguous `sys_to → sys_from` chain across all five — promoted from `@expectedFailure` to a live regression gate. | Low (docs only) | 5 (5k) | `test_5_82` | **Resolved** (Session 53b — docs+test fix; no framework code change needed; the framework was already correct) |

---

> **Back to:** [Progress index](../progress.md) | **See also:** [Conventions](conventions.md) | [Session Log](session-log.md)
