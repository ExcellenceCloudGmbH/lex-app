# Progress & Organization

> **Back to:** [Test Plan Index](index.md)  
> **Audience:** Engineering leadership, QA supervisors, developers

---

## How We Measure Progress

### The Dashboard: One Glance

At any point, the state of the test suite is summarized by this table. Update it after each work session.

| Cluster | Scenarios | Implemented | Passing | Expected Failures | Not Started | Status |
|---------|-----------|-------------|---------|-------------------|-------------|--------|
| 1. Init — Project Bootstrap | 23 | 23 | 23 | 0 | 0 |  Complete (scenario **1.6b** added April 22 as the CI showcase test — asserts detect-changes / makemigrations / migrate / Keycloak sync all fire in one `lex Init` run) |
| 1g. `KeycloakSyncManager` admin-facing methods (coverage-driven — April 24) | 10 | 10 | 10 | 0 | 0 |  Complete — covers `get_all_django_models` (app-registry walk + repo_name scoping + abstract/proxy + exclusion rules), `get_client_roles` (ignore-list filter + lazy default-role creation + missing-id fail-fast), and `ensure_client_role_policies` (add-new + normalize-in-place + extra-role ordering). All 10 driven through the `_make_sync_manager()` fixture from 1e — no Keycloak, no DB. |
| 1h. Bootstrap flow — URL + HTTP polling (coverage-driven — April 24) | 15 | 15 | 15 | 0 | 0 |  Complete — covers `build_instance_controller_url` (query-string shape + missing-base fail-fast), `fetch_bootstrap_state` (done/cancelled/pending + non-2xx + non-JSON), `poll_bootstrap_status` (loop-until-done + cancel/timeout/missing-payload), `wait_for_keycloak_setup_v2` (env-already-set fast path + controller-payload passthrough + cancel + timeout). `requests.get` patched — network-free. |
| 1i. Initial-data upload journey (coverage-driven — April 24) | 14 | 14 | 13 | 0 | 0 |  Complete (1 env-gated skip — 1.59 requires `INITIAL_DATA_AUDIT_LOGGING=true`). Drives `ProcessAdminTestCase` seed walker (`replace_tagged_parameters` tag:/datetime: resolution, `get_test_data_from_path` subprocess flattening, action dispatcher) + `InitialDataAuditLogger` end-to-end (log_object_create/update/delete → AuditLog + pending AuditLogStatus, mark_operation_success/failure idempotent, finalize_batch sweeps lingering pending → success on clean run / failure on crash path). Full create/update/delete journey runs against real `SimpleItem` + `AuditLog` tables via `E2ETestCase`. Closes `InitialDataAuditLogger.py` (12.64% baseline) + the JSON-walker contracts in `ProcessAdminTestCase` that Cluster 1c never reached. |
| 2. CRUD via REST API | 23 | 23 | 23 | 0 | 0 |  Complete (Many endpoint bulk-write contract reconciled to DELETE-only; BUG-004 timestamp assertions removed per product decision; BUG-005 fixed at framework level. Scenario **2.1** is the CRUD showcase test in CI) |
| 3. Validation Hooks | 8 | 9 | 9 | 0 | 0 |  Complete |
| 4. Permissions | 12 | 12 | 10 | 2 | 0 |  Complete (BUG-008/010 tracked) |
| 4e. Read-restriction filter backend (planned — April 21) | 6 | 6 | 4 | 0 | 0 |  Complete (4 pass + 2 skipped — 4.14/4.15 AuditLog DB-filter path deferred; needs Keycloak user_permissions + seeded AuditLog fixture) |
| 4f. Serializer-level masking (planned — April 21) | 8 | 8 | 8 | 0 | 0 |  Complete — 4.19–4.22 structural (mixin machinery) + 4.23–4.26 `run_validation` end-to-end (change-detection, per-field deny, `lexReserved*` bypass, create-path `permission_create`) |
| 5. History & Bitemporal | 10 | 10 | 10 | 0 | 0 |  Complete |
| 5.11. History fallback-snapshot path (planned — April 21) | 1 | 1 | 1 | 0 | 0 |  Complete — 1 scenario drives `_get_snapshot(serializer_class=None)` directly; covers CONTROL_FIELDS filter + datetime/date `isoformat` + non-primitive `str()` coercion + primitive/list/dict passthrough |
| 6. Audit Logging | 10 | 10 | 8 | 1 | 0 |  Complete (1 skipped — 6.4 needs middleware-level audit hook; BUG-001 family tracked) |
| 7. Calculation State Machine | 30 | 30 | 29 | 1 | 0 |  Complete (BUG-009 tracked; scenarios 7.32-7.47 add the exhaustive parent/child atomicity matrix: atomic/non-atomic parent × atomic/non-atomic child × parent pass/fail × child pass/fail) |
| 7g. `CalculatedModel.create()` pipeline (coverage-driven — April 21) | 6 | 6 | 6 | 0 | 0 |  Complete — `CombinatorialCalc.create()` walks combination generator → prepare → cluster → dispatch (sync) → `calc_and_save_sync` → `delete_models_with_same_defining_fields` end-to-end. Drove `CalculatedModelMixin.py` from **33.74% → 64.75%** (+31 pts / +170 lines) in one pass. Covers cartesian expansion, `get_selected_key_list` fallback, partial-failure accumulation, idempotent rerun, empty-key-list branch pruning, and pk-reset on missing duplicate. **Note (April 23):** the 7g import was broken — `CombinatorialCalc` was referenced but missing from `calculations/models.py`. Fixture added in the April 23 session so the 6 scenarios actually execute (they had been counting as passing in the dashboard while silently failing to import). |
| 7h. `_dispatch_model_processing` Celery branch (coverage-driven — April 23) | 1 | 1 | 1 | 0 | 0 |  Complete — drives the `CELERY_ACTIVE=true` + `hasattr(calculate, 'delay')` async branch of `_dispatch_model_processing` that 7g's synchronous path never reached. Stubs `.delay` onto `calculate` and patches `CeleryTaskDispatcher.dispatch_calculation_groups` so no broker is required; asserts the dispatcher is invoked with 2 flattened groups (4 models) and that no rows leak through to the sync fallback. |
| 8. Celery & Async | 6 | 6 | 6 | 0 | 0 |  Complete |
| 8g. Task infrastructure (planned — April 24) | 9 | 9 | 9 | 0 | 0 |  Complete — drives the 958-line `lex/lex_app/celery_tasks.py` (CallbackTask on_success/on_failure, CeleryCalculationContext, FireAndForget + WaitForTasks dispatch routing, lex_shared_task wrapper). **Redis-free**: patches `.delay` to return MagicMock AsyncResult; swaps `allow_join_result` for `nullcontext` on the WFT join path; instantiates `CallbackTask` directly and drives real ORM + real signals without running Celery itself. |
| 8h. Celery **eager-mode** end-to-end (planned — April 24) | 6 | 6 | 6 | 0 | 0 |  Complete — closes the gap 8g couldn't: the real `.delay()` → `@lex_shared_task` wrapper → task body → `CallbackTask.on_success`/`on_failure` sequence under `CELERY_ACTIVE=true`. **Broker-free**: uses Celery's own `app.conf.task_always_eager=True` + `task_eager_propagates` knobs — `.delay()` runs inline, real `EagerResult` returned, real Celery trace machinery invokes the callbacks. Covers: 8.16 success round-trip, 8.17 failure round-trip (propagate=False so on_failure actually fires), 8.18 `EnhancedBoundTaskMethod` + `WaitForTasks` drains real EagerResult on scope exit, 8.19 `CeleryTaskDispatcher.dispatch_calculation_groups` runs 2 groups via real `calc_and_save.delay`, 8.20 `is_celery_worker_process()` returns True inside an eager task body, 8.21 `register_task_with_context` FF-over-WFT priority. Coverage: `celery_tasks.py` 46% → 55%; `CeleryTaskDispatcher.py` ~0% → 45% (first real coverage). |
| 8i. `WaitForTasks` / `FireAndForget` scope contracts (planned — April 24) | 9 | 9 | 9 | 0 | 0 |  Complete — exhaustive coverage of the customer-facing dispatch primitives documented in `docs/lex_topics/12-celery-async-dispatch.md`: priority hierarchy, nesting, filters, no-op behaviour, exception propagation. 8.22 FF nested inside WFT partitions results (outer WFT sees parent tasks, inner FF sees its own, never crossed — real eager). 8.23 nested WFT scopes — inner exit drains only its own results, outer's result untouched across the nested scope boundary (real eager). 8.24 `WaitForTasks(include_tasks=…)` dispatches only the matching task name, non-matches run sync. 8.25 `WaitForTasks(exclude_tasks=…)` keeps excluded task names sync inside an otherwise-async block. 8.26 `FireAndForget(force_tasks=…)` inside an outer WFT — only the named subset is FF-forced, non-matches fall through to WFT rules. 8.27 **No-op when `CELERY_ACTIVE` unset** — entering WFT/FF pushes nothing onto the ContextVars, `.delay` is never called, task body runs sync (the "your code doesn't change" contract). 8.28 exception propagation — a task raising inside WFT surfaces the exception on scope exit with `propagate=False` so Celery's trace machinery invokes on_failure first, then re-raises via `.get()`. 8.29 multiple dispatches in one WFT block all drained on exit — every row reaches SUCCESS. 8.30 FF exit does NOT drain `dispatched_results` — that is the behavioural difference from WFT, only explicit `wait_for_completion()` drains. |
| 8j. Celery task bodies — `load_data` / `calc_and_save` / `activate_history_version` (coverage-driven — April 24) | 14 | 14 | 14 | 0 | 0 |  Complete — closes Cluster-A/B/C bucket C. Drives the three task bodies in `lex/lex_app/celery_tasks.py` (lines 696–957) that 8g/8h/8i never reached. **load_data** (6): early-return gate, LEGACY+worker direct setUp, LEGACY+sync asyncio bridge, non-LEGACY setUpCloudStorage, exception → re-raise + `finalize_batch(failure_error=…)`, None-audit-logger `finally` guard. **calc_and_save** (3): happy path summary, IntegrityError → `delete_models_with_same_defining_fields` → pk-rewire → retry save (conflicts_resolved bump), generic exception re-raise. **activate_history_version** (5): `apps.get_model` LookupError → `"failed_model_lookup"`, history `DoesNotExist` → `"skipped_missing_record"`, valid_from > now+5s → `"failed_too_early"`, happy path → `sync_record_for_model` + Meta `SCHEDULED→DONE` → `"success"`, sync-blowup re-raise. Broker-free: `@lex_shared_task` descriptor routes to `self.task(...)` with `CELERY_ACTIVE` unset, invoking real bodies synchronously. |
| 9. Signals & WebSocket | 6 | 6 | 6 | 0 | 0 |  Complete |
| 9.7–9.10. Bitemporal signal branches (planned — April 21) | 4 | 4 | 4 | 0 | 0 |  Complete — guard lifecycle, nested stacking, cross-guard independence, thread-local isolation; covers the suppression primitives at the heart of `bitemporal_signals.py` |
| 10. API Layer | 10 | 10 | 8 | 1 | 0 |  Complete (10.7 now gates supported Many bulk DELETE; 10.9 validation duplicate skipped for now; BUG-009 tracked) |
| 10e. Schema introspection (planned — April 21) | 4 | 4 | 4 | 0 | 0 |  Complete (`create_field_info` type mapping + pk/required/default flags + FK target; structure-tree pruning collapses denied-model folders. **BUG-015 surfaced** — CharField without explicit default reports `required=False` because Django's `get_default()` returns `""` — documented in-test, not xfailed) |
| 10f. Global search (planned — April 21) | 4 | 4 | 4 | 0 | 0 |  Complete — 10.15 hit shape, 10.15b sentinel string on zero matches, 10.16 EXCLUDED_MODELS enforcement, 10.16b EXCLUDED_TYPES regression gate |
| 11. Stress & Performance | 20 | 20 | 15 | 2 | 0 |  Complete (3 skipped — 11.12 concurrent-writes, 11.15 audit throughput, 11.19 at SMALL only; BUG-011 tracked) |
| 12. Serializer Contract | 28 | 28 | 25 | 3 | 0 |  Complete — 12a read contract (5 pass) + **12a history/meta scopes (3 pass — 12.6 history-row unwraps to main model's `permission_read`, 12.7 MetaHistorical scopes immutable `{"edit": [], "delete": False, "export": False}`, 12.8 `lex_reserved_scopes.edit` mirrors `permission_edit` `allow_fields`)** + 12b type round-trip (7 pass + 3 xfail) + 12c list & many read contract (3 pass) + 12d AuditLog payload (3 pass) + **12e factory contract (2 pass + 1 xfail — 12.26 `model2serializer` always injects framework internals, 12.27 `_wrap_custom_serializer` preserves user fields AND appends internals, 12.28 serializer cached per model — xfail: `get_serializer_map_for_model` has no in-process cache, documented as BUG-017)**. |
| 12f. Serializer write paths — M2M & nested FK (planned — April 21) | 3 | 3 | 3 | 0 | 0 |  Complete — 12.29 M2M create-with-set, 12.30 PATCH-replace, 12.31 FK attach/rewire/detach lifecycle. **Note (April 23):** scenario numbers renumbered from 12.26/12.27/12.28 → 12.29/12.30/12.31 to free up 12.26–28 for the canonical 12e factory-contract scenarios in test-clusters.md. `TagItem` / `TaggableItem` / `EditScopedItem` fixtures added to `serializers/models.py` (the originals were missing so 12f was silently failing to import). |
| 13. Export Endpoint | 12 | 12 | 12 | 0 | 0 |  Complete — 13a legacy (4 pass) + 13b AG Grid flat (5 pass) + **13c grouped & selected (1 pass — 13.9 `groupKeyPaths` filter + `_coerce_group_key` string→int FK coercion + `"null"` → `__isnull=True` sentinel; asserts selected subset is strictly smaller than baseline and hits the expected group count)** + **13d auth & edge (2 pass — 13.11 unauthenticated POST → 401/403 with no streamed bytes, 13.12 non-uniform per-object `permission_export` forces `_compute_uniform_export_mask → None` and the slow per-row mask correctly allows `amount` on row B while blanking it on row A)**. BUG-014 fixed in 13b session. |
| 14. AG Grid Query Endpoint | 25 | 25 | 24 | 0 | 0 |  Complete — baseline 14a/14b/14c (20 scenarios) landed earlier; 14e extended with 4 pass + 1 skip (BUG-016 deferred) covering text/number filter op variants, legacy `condition1`/`condition2` shape, multi-field CSV ordering, and the `blank`/`notBlank` unreachable-branch bug |
| **Total** | **324** | **324** | **304** | **1** | **0** | All planned scenarios implemented. Session 38 added 1j/1k/1l (Keycloak client safety pre-flight — 20 mocked + 11 env-gated integration skips, plus the framework-side `verify_client_is_safe_for_init` + `--skip-client-preflight` flag). Session 39 consolidated 1c/1f/1i under `tests/init/initial_data/` (no test changes — pure folder move + package-level docstring). Session 40 added 1m (PyCharm `.run.xml` ↔ `lex` CLI cross-file contract — 8 pass). Session 41 reconciled the Many endpoint contract to DELETE-only, retired BUG-006, restored missing scenario-numbered methods for 8.5/8.6 and 9.1/9.2/9.3/9.6, removed BUG-004 timestamp assertions, fixed BUG-005 at framework level, and skipped BUG-016. Session 42 added 7i's exhaustive calculation atomicity matrix (16 pass), including the atomic-parent rollback cases. On-disk `def test_` method count is now 534. |

**Status legend:**
-  Not started — no tests written yet
-  In progress — some scenarios implemented, not all passing
-  Complete — all scenarios implemented and passing (or marked as expected failure with tracked bug)
-  Blocked — depends on a framework fix before tests can pass

---

### What Counts as "Done" for a Cluster

A cluster is ** Complete** when:

1. ✅ Every scenario from [test-clusters.md](test-clusters.md) has a corresponding test method
2. ✅ All tests either **pass** or are marked `@unittest.expectedFailure` with a tracked bug reference
3. ✅ Tests use the canonical customer code path (no `skip_hooks`, no `calculate_hook()` in E2E tests)
4. ✅ Tests run in CI without flakiness (3 consecutive green runs)
5. ✅ No mocks on the class under test or the ORM

A cluster is ** In progress** when some but not all of the above are true.

---

## How We Organize the Work

### Rule: Work in Cluster Order

Clusters are ordered by the user journey (see [test-clusters.md](test-clusters.md#ordering-the-user-journey)). We implement them **in order** because each cluster builds on the one before it:

```
1. Initial Data → 2. CRUD → 3. Validation → 4. Permissions → 5. History → ...
```

**Exception:** If a cluster is blocked by a framework bug, skip it (mark ) and move to the next. Come back after the bug is fixed.

### Rule: One Cluster at a Time

Don't start Cluster N+1 until Cluster N is  or . This keeps work focused and progress visible.

### Rule: Test Intent, Never Overfit to Source Code

> **The source code is an incomplete story.** It has bugs, shortcuts, and workarounds.
>
> Tests must assert what the framework is **trying to achieve** — derived from the docs, the public API, and what a customer would reasonably expect. Never write a test by reading the current implementation and asserting "what the code does today."
>
> **If the code is buggy, the test fails. That is correct.** Mark the failing test `@unittest.expectedFailure`, add the bug to the [Known Bugs Tracker](#known-bugs-tracker), and move on. The test now guards against the bug being forgotten.
>
> **Never** weaken a test to make it pass on broken code. That is how the old suite ended up with 2,000 green tests that missed real bugs. See [Golden Rule + red flags in test-clusters.md](test-clusters.md#testing-philosophy).

### Rule: Test First, Then Fix

When a test exposes a framework bug:
1. Write the test asserting the **correct** behavior (from docs / intent)
2. Mark it `@unittest.expectedFailure` with a comment explaining the bug and linking the tracker entry
3. Add the bug to [Known Bugs Tracker](#known-bugs-tracker)
4. Move on — don't block the test suite on the fix
5. When the bug is fixed, remove `@unittest.expectedFailure` — the test should now pass naturally

---

## User Experience: Making Tests Readable

Tests are documentation. A new developer reading a test should understand:
- **What** is being tested (the scenario)
- **Why** it matters (the risk)
- **How** to reproduce it (the setup)

### Naming Convention

```python
def test_<cluster_number>_<short_description>(self):
    """
    Scenario X.Y: <one-line description from the cluster table>
    
    Given: <setup>
    When: <action>
    Then: <expected outcome>
    """
```

**Example:**
```python
def test_02_01_create_sets_timestamps(self):
    """
    Scenario 2.1: Create a record via ORM
    
    Given: A SimpleItem model
    When: We create and save a new instance
    Then: created_at and edited_at are set, created_by = resolved actor
    """
    item = SimpleItem(name="Test", value=42)
    item.save()
    
    self.assertIsNotNone(item.created_at)
    self.assertIsNotNone(item.edited_at)
    self.assertEqual(item.created_by, "Initial Data Upload")
```

### Assertion Messages

Every assertion has a human-readable failure message:

```python
# Bad
self.assertEqual(item.is_calculated, "SUCCESS")

# Good
self.assertEqual(
    item.is_calculated, "SUCCESS",
    "After successful calculation, state must be SUCCESS"
)
```

### Test Class Organization

One test class per cluster, named clearly:

```python
class TestCluster02_CRUDLifecycle(E2ETestCase):
    """Cluster 2: CRUD & Lifecycle — tests the basic LexModel contract"""
    e2e_models = [SimpleItem, TrackedItem]
```

---

## How to Run Tests

### Run all clusters
```bash
source ~/LUND_IT/ArmiraCashflowDB/.venv/bin/activate
lex test lex.test_project.tests --noinput
```

### Run a single cluster
```bash
lex test lex.test_project.tests.test_02_crud_lifecycle --noinput
```

### Run with coverage
```bash
coverage run --source=lex manage.py test lex.test_project.tests --noinput
coverage report
```

---

## Known Bugs Tracker

Bugs discovered by the new test suite. Each has a corresponding test marked `@unittest.expectedFailure`.

| # | Bug | Severity | Cluster | Test | Status |
|---|-----|----------|---------|------|--------|
| BUG-001 | `LexModel.save()` wraps IN_PROGRESS + hooks in single `transaction.atomic()` — rollback erases IN_PROGRESS history | High | 5, 7 | `test_05_08`, `test_07_02` | **Resolved** (5.8 passes — IN_PROGRESS row persists. Repro may have been two-phase save refactor; re-open if specific repro resurfaces.) |
| BUG-002 | Startup reset uses `queryset.update()` instead of `.save()` — bypasses history signals, no ABORTED history row | Medium | 7 | `test_07_xx` | Open |
| BUG-003 | `reconcile_changes_since()` sets `synced_count` but never returns it | Low | 5 | `test_05_xx` | Open |
| BUG-004 | `edited_at` not populated on create/update timestamp path | Low | 2 | retired (`test_2_2`, `test_2_14` removed per product decision) | **Retired** |
| BUG-005 | Validation errors return **500** instead of **400** — missing required fields, wrong field types, and invalid PATCH payloads all bypass DRF's standard ValidationError-→-400 conversion | High | 2 | `test_2_3`, `test_2_4`, `test_2_15` | **Resolved** (framework now preserves DRF `ValidationError` as HTTP 400; duplicate API-layer scenario 10.9 skipped for now) |
| BUG-006 | `model-many-entries` endpoint rejects POST with 405 — no bulk-insert via API | Medium | 2, 10 | retired (`POST /many/` and `PATCH /many/` are not supported contracts; bulk write support is DELETE-only) | **Retired** |
| BUG-007 | Actor resolution on API create path may not populate `created_by` with the authenticated user's email — sibling of BUG-004; revisit once the authenticated-context stamping step is reviewed. | Medium | 6 | `test_6_7` | Open |
| BUG-008 | Authorization failures return **400** with a JSON `message` body instead of **401/403**. Customer intent: unauthorized actions get a proper HTTP auth status so frontends/integrations can route them through the standard error path. | High | 4 | `test_4_5`, `test_4_6` | Open |
| BUG-009 | `CalculationModel.is_calculated` is `editable=False`, so API PATCH requests that set it to `IN_PROGRESS` are silently dropped by the serializer — the documented "API triggers calculation" path (scenarios 7.14 / 10.8) cannot work. Fix: expose `is_calculated` as a write-through state field for PATCH while keeping it read-only elsewhere, OR document a dedicated trigger endpoint. | High | 7, 10 | `test_7_14`, `test_10_8` | Open |
| BUG-010 | ``permission_edit`` field-level restriction is not enforced on PATCH. A non-admin PATCHing a field outside the permitted set (e.g. ``secret`` on ``ProtectedItem``, where only ``name`` is allowed) receives 200 and the restricted field IS mutated. The serializer's permission-aware field filtering runs on READ but not on WRITE. | High | 4 | `test_4_4` | Open |
| BUG-011 | List endpoint evaluates ``permission_read`` **once per row** instead of once per request. Observed: a 100-row page produces **500 calls** (5× per row) to the model's ``permission_read``, and the endpoint issues ~``n + k`` SQL queries per page where ``n`` is the page size. At 20k rows this turns every list/export into a minutes-long stall. The documented contract is one permission check per user per request; the serializer's permission-aware field filtering should reuse a single ``PermissionResult`` per request instead of re-invoking per row. | High | 11 | `test_11_3`, `test_11_14` | Open |
| BUG-012 | ``DateTimeField`` is serialized as a **timezone-naive** ISO-8601 string (``"2026-04-21T15:30:45"``) even when the stored value is tz-aware UTC. The UI therefore shows the value in the server's local timezone instead of UTC, and PATCH round-trips silently drift. DRF's default contract is tz-aware output — something in the serializer chain is stripping ``tzinfo``. | High | 12 | `test_12_10` | Open |
| BUG-013 | PATCH rejects the documented ``{"related": {"id": X}}`` dict payload for ``ForeignKey`` fields with DRF's ``Incorrect type. Expected pk value, received dict.`` error — returned as 500 because BUG-005 is also in play. The ``LexSerializer._parse_value_for_field`` FK-dict handling only runs on the audit-log shadow-instance path; the real PATCH-on-main-model path never gets it. Contract (from the frontend's save flow + ``get_lex_reserved_scopes`` round-trip shape): the same dict shape that comes out on GET must be accepted on PATCH. | High | 12 | `test_12_15` | Open |
| BUG-014 | **Every flat Excel export shipped with only the last row populated.** ``ModelExportView.post`` enabled ``xlsxwriter`` ``constant_memory`` mode whenever there was no hierarchy depth (i.e. on every flat export) and called ``df.to_excel(..., index=True, freeze_panes=(1, 1))``. ``constant_memory`` flushes each sheet row as soon as any write advances to a later row; pandas writes the index column (column A) **after** the data columns B..N for each row, which is a backward write. Earlier rows lost every cell, including ``id``. Surfaced on first run of Cluster 13 with a 3-line isolated repro. | **Critical** | 13 | `test_13_2a`, `test_13_2b`, `test_13_3`, `test_13_4` | **Resolved** (fixed in-session; all four regression tests now pass and stand as permanent gates against re-introducing ``constant_memory`` + ``index=True``) |
| BUG-015 | ``create_field_info`` reports ``required=False`` for any ``CharField`` without an explicit ``default=`` argument because Django's ``Field.get_default()`` returns the empty string ``""``, so the framework's ``required = not (field.null or default is not None)`` short-circuits to False. A POST missing ``name`` still fails at the DRF serializer layer (HTTP 400), so the frontend schema and the backend validation disagree on whether the field is mandatory — the UI renders the field as optional and the user only sees the error after submitting. Low severity (contract mismatch, not a data-integrity issue). Documented in ``test_10_12`` with an explicit code comment; the test currently asserts the observed behaviour rather than xfailing. | Low | 10 (10.12) | `test_10_12_field_info_flags_editable_required_default_pk` | Open |
| BUG-016 | **AG Grid ``blank`` and ``notBlank`` filter operations are silently unreachable in `_build_filter_q`.** The text branch's early-return `if value in (None, ""): return None` fires before the `blank` / `notBlank` dispatch can run. The number branch's guard `if value in (None, "") and operation_type != "blank"` omits `notBlank` from the bypass. The date branch's guard `if operation_type != "blank" and not date_from_raw` also excludes `notBlank`. Customer symptom: clicking the "Blank" or "Not Blank" option in the grid's filter header does nothing — every row is returned. The date branch does special-case `blank` correctly, proving the fix pattern; text and number branches need the same treatment, and all three branches must add `notBlank` to the bypass. | Medium | 14 (14e) | `test_14_25_blank_and_not_blank_ops_do_not_work_bug016` | Open — skipped/deferred |
| BUG-017 | **`get_serializer_map_for_model` has no in-process cache.** Every call rebuilds the DRF serializer class via `model2serializer` + `_wrap_custom_serializer`, so two successive calls on the same model return two distinct class objects (`first["default"] is not second["default"]`). Customer impact: every REST request rebuilds the serializer class tree, burning CPU on DRF field-registration work that is pure function of the model class; identity-based callers (DRF's internal field registration, metadata endpoints that compare class references) cannot rely on reference stability. The `ModelContainer.get_serializers_map` layer already caches by a signature hash, so the leak is at the factory itself, not at the container. Fix: memoize the factory by `(model_class, tuple(default_fields or ()))`. | Medium | 12 (12e) | `test_12_28_get_serializer_map_returns_same_class_per_model` | Open |
| BUG-018 | **`ModelExportView._normalize_ag_request` silently overwrites the caller's `endRow` with `MAX_AG_EXPORT_ROWS` (1,000,000).** Line ~151 of `lex/api/views/file_operations/ModelExport.py` does `request_payload["endRow"] = MAX_AG_EXPORT_ROWS` unconditionally, before any downstream path reads it. The streaming cap loop at line ~1229 (`if written >= end_row: break`) is therefore dead code — `end_row` is always 1M. Customer symptom: every "export only the first N rows" UI affordance (AG Grid's pagination window, `startRow`/`endRow` for "export current page") silently falls through to a full-table dump. Bandwidth + spreadsheet-size blowup on large tables; no data-integrity risk. Side finding: scenario 13.7 ("`endRow` over `MAX_AG_EXPORT_ROWS` → clamped silently") passes for the wrong reason — the same line happens to clamp the over-cap direction correctly via clobber rather than `min()`. Fix: `request_payload["endRow"] = max(1, min(int(raw or MAX), MAX))` — preserves user values ≤ MAX, clamps above. | High | 13 (13e) | `test_13_17_end_row_limit_caps_written_rows` | Open |
| BUG-019 | **`FireAndForget(exclude_tasks=…)` — doc vs code disagreement, no test pre-commits us.** Surfaced during 8i design (April 24). `FireAndForget.__init__` docstring (`celery_tasks.py` ~line 295) promises: *"exclude_tasks: Task names that should follow WaitForTasks rules."* The code at `EnhancedBoundTaskMethod.__call__` lines ~527–529 does **not** follow WFT rules — when a task name is in `ff_ctx.exclude_tasks` it `return self.task(self.instance, *args, **kwargs)` **before** `await_ctx.should_dispatch(task_name)` is consulted, forcing synchronous execution even when an enclosing `WaitForTasks` would otherwise have dispatched it. Two defensible interpretations: (a) docstring is right — excluded tasks should fall through to WFT and honour its include/exclude rules; (b) code is right — excluded tasks are pinned to sync regardless of WFT, as a "force sync" escape hatch. 8i covers `FF(force_tasks=…)` (scenario 8.26) but **deliberately does not test `FF(exclude_tasks=…)`** — writing a test either way pre-commits the framework to one interpretation before product has decided. Zero customer reports either way. **Action: raise with product; pick an interpretation; then add the scenario and update the docstring OR the code to match.** Severity Low because no production symptom has been reported, but the ambiguity is a latent footgun for any customer who reads the docstring and relies on WFT-style fall-through. | Low | 8 (8i) | *(no test — deliberately unpinned)* | **Open — design decision pending** |

---

## Session Log

Record each work session here so progress is traceable.

| Date | Session | What Was Done | Clusters Affected | Tests Added | Tests Passing |
|------|---------|---------------|-------------------|-------------|---------------|
| 2026-04-17 | 1 | Created test plan documentation (index, why-the-shift, clusters, expected-results, progress) | All | 0 | 0 |
| 2026-04-19 | 2 | Scaffolded `lex/test_project/` (config fixtures, fixtures dir, seed JSON). Implemented Cluster 1 (17 tests) and Cluster 2 (20 tests). Exposed 3 new framework bugs (BUG-004, 005, 006) via intent-driven assertions — all marked `expectedFailure` with tracker references. | 1, 2 | 37 | 31 (6 expected failures) |
| 2026-04-19 | 3 | Implemented Cluster 3 (Validation Hooks) — 9 tests covering pre_validation cancel, post_validation rollback, hook execution ordering (create/update), and recursion guard. All passing — no new bugs exposed, framework behaves per documented intent. | 3 | 9 | 9 |
| 2026-04-20 | 4 | Scaffolded Clusters 4–10 (permissions, history, audit_logging, calculations, celery_async, signals_ws, api_layer). Each cluster has its own `models.py` + per-sub-cluster test files. Tests that need the Keycloak/AuditLog/Celery/signals fixtures are `@unittest.skip`'d with explicit reasons; ORM-path scenarios (history, calc state machine, hierarchy propagation) assert real intent. BUG-007 added (actor resolution via API create path). | 4, 5, 6, 7, 8, 9, 10 | 68 | TBD |
| 2026-04-20 | 5 | **Pass 1f** — Closed Cluster 1 to . Added `test_1f_keycloak_drift.py` (scenarios 1.8 / 1.9 / 1.10 / 1.15 — drives `process_model_changes` end-to-end with a stubbed kc_manager, asserts new-resource default scopes, rename preserves permissions, delete removes resource + referencing policies, state-file durability across processes) and `test_1f_seed_idempotency.py` (1.18 / 1.20 — gate returns False when any referenced model has rows, `load_data` is a no-op when `initial_data_load` is falsy, seed JSON declaration order is preserved verbatim). New `test_seed_with_fk.json` fixture added. | 1 | 14 | 14 |
| 2026-04-20 | 6 | **Pass 2f + Journeys D & E** — Closed Cluster 2 to . `test_2f_missing_scenarios.py` adds the last four planned scenarios (2.7 skipped — API-key middleware dep, matches 4.11 pattern; 2.16 PUT full-replace contract; 2.24 / 2.25 bulk PATCH + all-or-nothing bulk POST — both `expectedFailure` against BUG-006). `test_journey_d_audit_trail.py` (Cluster 5 focus) walks a full create→patch→patch→delete cycle via REST and reconstructs the whole history trail — row count, `history_type` sequence `[+, ~, ~, -]`, stable/unique `history_id` ordering, change-row amounts match PATCH payloads, multi-invoice strand isolation. `test_journey_e_validation_gate.py` (Cluster 3 focus) proves the pre/post validation gates in combination: pre-hook rejection leaves zero rows AND zero history rows; post-hook rejection rolls back to pre-save snapshot AND leaves no dangling history row; mixed good/bad/good batch has only the valid rows in DB. New `ValidatedInvoice` model added to `journeys/models.py`. | 2, 3, 5 | 9 | 9 (6 pass + 1 skip + 2 expected failures) |
| 2026-04-20 | 7 | **Pass B2 — E2ETestCase fixture extensions**. Closed Clusters 6, 8, 9 to . Added three composable hooks to `lex/tests/e2e/_e2e_test_case.py`: `e2e_framework_models` (creates AuditLog/AuditLogStatus tables alongside e2e_models), `e2e_unpatch` (opts out of individual default mocks — the gate that was blocking 14 skipped tests), and `spy_on()` (installs a recording MagicMock over an opted-out default). Rewrote `test_6a_api_audit.py` (6.1/6.2/6.3 pass against live AuditLog table; 6.4 stays `@skip` — needs middleware-level audit hook, documented), `test_6b_calculation_audit.py` (6.5/6.6 drive `ensure_terminal_calculation_audit` with real context_data; 6.10 `@expectedFailure` against BUG-001 family), `test_9a_state_store.py` (9.1/9.2 spy on `mark_in_progress` + `ActiveCalculationStateStore.clear`), `test_9b_notifier.py` (9.3/9.6 spy on `sync_channel_group_send` — the actual broadcast seam — asserting payload shape and "no success broadcast on failure"), `test_9c_cache_cleanup.py` (9.4 skipped — needs `operation_context` helper; 9.5 skipped — needs nested-calc fixture), `test_8b_dispatch.py` (8.5 pickles the dispatched context and asserts it round-trips; 8.6 skipped — needs nested-calc + worker fixture). Skip pile dropped from 22 → 10; passing from 85 → 94. | 6, 8, 9 | 14 (skip → pass/xfail) | 94 pass + 10 skip + 16 xfail of 234 |
| 2026-04-20 | 8 | **Unblock-pass — skipped → passing**. Added an ``operation_context(calc_id)`` helper to ``E2ETestCase`` (wraps ``OperationContext`` from ``lex.api.utils.Context`` so tests can set a ``calculation_id`` on the context var without importing framework internals directly). Used it to rewrite the last four skipped scenarios as live tests. **9.4 / 9.5**: stack ``model_logging_context`` under ``operation_context`` + pre-seed an ``AuditLog`` row so ``ContextResolver.resolve()`` finds a calculation to clean up against; patch ``CacheManager.cleanup_calculation`` directly and assert (9.4) root calc invokes it once with the right calc_id and (9.5) child calc — where a parent is still on the stack — does NOT invoke it. **7.7**: added ``NonAtomicParentCalc`` model (non-atomic parent that spawns atomic ``ChildCalc``), asserts error propagation up the tree regardless of the parent's ``is_atomic`` flag. **7.10**: exercises ``CalculationModel.persist_error_state`` idempotency directly — two calls, second must not add a history row — verifying the ``_has_persisted_terminal_state`` fast-path. Clusters 4, 7, 10 now . Skip pile 10 → 2 (only 6.4 middleware-level audit and 10.7 which is a duplicate of the 2.24/2.25 BUG-006 xfail remain). Passing 94 → 108 of 234 in the test_project suite. | 7, 9 | 4 (skip → pass) | 216 pass + 2 skip + 16 xfail of 234 |
| 2026-04-20 | 9 | **Added Cluster 11 — Stress & Performance** to the test plan. 15 planned scenarios covering `bulk_create` baseline, single-row `.save()` at 5k, paginated list reads at 20k (p95 + query-count budgets), filter/sort (with `EXPLAIN Index Scan` assertions), full-table export (streaming + memory ceiling), Excel/CSV writer loop (N+1 detection), `PeriodAggregateCalc` single period and all-12-period runs, `DependentPeriodCalc` 3-period dependency chain across 12 periods, bulk POST/PATCH at volume, 10-client concurrent writes, 20k-revision history growth, permission filtering at scale, and audit-log write throughput. Specified the `StressTestCase` harness (`bulk_seed`, `assert_runtime_under`, `assert_query_count_at_most`, `measure(label)` CM writing JSON trend data), volume tiers (SMALL 500 / MEDIUM 5k / LARGE 20k), five new models (`StressInvoice`, `StressCounterparty`, `StressPeriod`, `PeriodAggregateCalc`, `DependentPeriodCalc`), hard-gate budgets with a 90-day moving-average drift check, and the "budgets tighten, never loosen" release discipline. Cluster status  — implementation tracked for a later pass. | 11 | 0 (documentation only) | 216 pass + 2 skip + 16 xfail of 234 (unchanged) |
| 2026-04-20 | 10 | **Implemented Cluster 11 — Stress & Performance**. Scaffolded `lex/test_project/tests/stress/` with 5 models (`StressCounterparty`, `StressPeriod`, `StressInvoice`, `PeriodAggregateCalc`, `DependentPeriodCalc`), the `StressTestCase` base class (`bulk_seed` with `skip_history=True` fast-path, `assert_runtime_under`, `assert_query_count_at_most`, `measure()` writing JSON trend data to `test_reports/stress/`, volume tiers via `LEX_STRESS_VOLUME` env var), 6 test files and all 15 scenarios. **Two real framework bugs surfaced on the very first run** and are now tracked as **BUG-011** with concrete repros: (a) scenario 11.3 — list endpoint issues ~504 SQL queries for a 100-row page at SMALL volume; (b) scenario 11.14 — the root cause: the serializer calls the model's ``permission_read`` **500 times** for that same page (5× per row). Both are xfail against BUG-011. Other xfail: 11.10 tied to BUG-006 (bulk POST 405). Skipped with documented reasons: 11.12 (threaded-concurrency fixture), 11.15 (middleware-level audit). Ran 15 scenarios at SMALL tier; 10 pass, 3 xfail, 2 skip, zero unexpected failures. Note: a stale ``StressPeriod`` field pair named ``valid_from``/``valid_to`` collided with simple_history's internal columns on first run — renamed to ``period_from``/``period_to`` (an unplanned guard-rail discovery: simple_history reserves those names). | 11 | 15 (incl. xfail) | 226 pass + 4 skip + 19 xfail of 249 |
| 2026-04-20 | 11 | **Cluster 11 — FK-heavy export sub-cluster (11.16 – 11.20)**. Added 3 new models (`FKHeavyCategory`, `FKHeavyCurrency`, `FKHeavyInvoice` with 4 FKs: counterparty/period/category/currency) and `test_11g_fk_heavy_export.py` with 5 scenarios targeting **25k rows × 4 FKs**: (11.16) REST list export; (11.17) documented CSV path with ``select_related`` + ``.iterator()``; (11.18) anti-baseline naive path that exposes the N+1; (11.19) ``tracemalloc``-measured peak-memory delta between list materialization and ``.iterator()``; (11.20) aggregated JOIN ``Sum(invoices__amount_net)`` grouped by counterparty. **Concrete measurement** from the first run: the documented path does the full export in **1 SQL query**; the naive path does the same work in **8 001 queries** (2 000 rows × 4 FK round-trips). That 8 000× delta is now a hard CI gate. Scenarios run at 25k by default on MEDIUM/LARGE and scale to 2.5k on SMALL. 11.19's memory assertion skips at SMALL (delta too small to distinguish reliably with ``tracemalloc`` at that volume). Full stress suite now 20 tests; full test_project suite 254 tests, 231 pass, 4 skip, 19 xfail, 0 unexpected failures. | 11 | 5 | 231 pass + 4 skip + 19 xfail of 254 |
| 2026-04-21 | 12 | **Added Cluster 12 — Serializer Contract** to the plan and landed the first two sub-clusters. Added the cluster spec to `test-clusters.md` (28 scenarios across 12a read contract / 12b type round-trip / 12c list & many / 12d AuditLog payload / 12e factory contract), updated `index.md` and the progress dashboard. Scaffolded `lex/test_project/tests/serializers/` with 3 dedicated models (`RelatedItem`, `WideItem` — one field per interesting type: Decimal/DateTime/Date/Time/UUID/JSON/choices/FK, `ProtectedWideItem` — field-level restrictive `permission_read`). Implemented **12a** (5 scenarios — framework-managed key presence, `short_description = str(instance)`, `lex_reserved_scopes` shape, field filtering round-trip, `FilteredListSerializer` drops denied rows) and **12b** (11 scenarios — Decimal precision, DateTime tz round-trip, Date ISO format, UUID as string, nullable FK is null, FK set shape, FK-dict PATCH, invalid choice rejected, TextField unicode, JSONField structure, unknown field ignored). First run: **13 pass, 3 xfail, 0 unexpected failures** — and **two new framework bugs surfaced immediately**: **BUG-012** (`DateTimeField` returns a tz-naive ISO string — UI shows server-local time instead of UTC, PATCH round-trips silently drift) and **BUG-013** (PATCH rejects the documented `{"related": {"id": X}}` dict payload with DRF's `Incorrect type` error — `LexSerializer._parse_value_for_field` FK-dict handling only runs on the audit-log shadow path, never on real PATCHes). 12.16 xfail against BUG-005 as expected. | 12 | 16 | 13 pass + 3 xfail of 16 new (cumulative ~244 pass + 22 xfail) |
| 2026-04-21 | 13 | **Cluster 12 — 12c list/many + 12d AuditLog payload filtering landed.** 12c (3 scenarios): `FilteredListSerializer` drops denied rows (not blank dicts), list row shape matches detail shape exactly (no key drift between endpoints), `/many/` POST invalid-choice rejection (xfail against BUG-006). 12d (3 scenarios) — instantiates `model2serializer(AuditLog)` directly against a `TransactionTestCase` with history tables (inherits `E2ETestCase` just for the fixture; does NOT use the APIClient) and calls `to_representation` with a request-factory context: (12.23) an FK dict whose target row is denied by `permission_read` is stripped from the payload, non-FK fields survive; (12.24) target-model fields the caller cannot read are pruned from both the top-level payload AND `payload.updates`; (12.25) when the target's `permission_read` denies entirely, the payload collapses — only pinned `id` / `id_field` / `short_description` keys may survive. All 6 pass on first run. Full cluster 12 now 22 tests, **18 pass + 4 xfail + 0 unexpected failures**. Remaining for : 12.6–12.8 (history/meta scopes) + 12e factory-contract sub-cluster. | 12 | 6 | 18 pass + 4 xfail of 22 (cumulative ~249 pass + 23 xfail of 276) |
| 2026-04-21 | 14 | **Added Cluster 13 — Export Endpoint** to close the 17-methods-untouched gap in `ModelExportView`. Plan (12 scenarios across 4 sub-clusters — 13a legacy, 13b AG flat, 13c AG grouped/selected, 13d auth/edge) added to `test-clusters.md`; `index.md` + progress dashboard updated. Scaffolded `lex/test_project/tests/exports/` with 3 dedicated models (`ExportCategory` with distinctive `__str__`, `ExportItem` with default perms opted into export, `ExportMaskedItem` with field-level `allow_fields({"id","name"})`) and landed **10 scenarios** (13.1 / 13.2a / 13.2b / 13.3 / 13.4 / 13.5 / 13.6 / 13.7 / 13.8 / 13.10) driving the real `POST /api/<model>/export` endpoint and asserting on `pd.read_excel` of the returned `FileResponse`. **Found and fixed BUG-014** on first run: a 3-line isolated repro proved that `xlsxwriter` `constant_memory=True` + `df.to_excel(..., index=True)` drops every row except the last — production-critical because that's the dominant flat-export config. User fixed the framework mid-session; 4 tests that were xfailed against BUG-014 now pass as permanent regression gates. Added `url_export()` helper to `E2ETestCase`. Cluster 13 state: **10 pass + 0 xfail + 0 unexpected failures** across 10 tests. Remaining for : 13.9 (`groupKeyPaths` selection + `_coerce_group_key`), 13.11 (unauthenticated POST), 13.12 (non-uniform per-object mask forcing the slow `_apply_export_mask_to_ag_rows` path). | 13 | 10 | 10 pass of 10 |
| 2026-04-21 | 15 | **Coverage-gap remap — no new top-level clusters.** After the 42.63% coverage baseline, audited every low-coverage customer-visible file and **mapped each gap into an existing cluster** rather than inventing Cluster 15/16/17/…. Added a new "Planned Expansions — Coverage-Driven Sub-Clusters" section to `test-clusters.md` with six new sub-clusters totalling **24 scenarios**: **4e** (read-restriction filter backend, 6 scenarios — targets `UserReadRestrictionFilterBackend` at 28.97%), **4f** (serializer-level masking, 4 scenarios — targets `PermissionAwareSerializerMixin` at 9.33%), **12f** (M2M & nested-FK write paths, 3 scenarios — closes `base_serializers.py` write-branch holes), **10e** (schema introspection, 4 scenarios — `ModelStructureObtainView` at 21.54% + `Fields.py` at 22.35%), **10f** (global search, 2 scenarios — `Search.py` at 34.21%), **5.11** (history × permission intersection, 1 scenario — `History.py` 180–201), **9.7–9.10** (bitemporal-signal branches, 4 scenarios — `bitemporal_signals.py` 170–340). Rationale documented in the new section: every gap is a *facet* of an existing user-journey concern, so splitting them out as standalone clusters would fragment the story and duplicate model setup. Progress dashboard updated with per-sub-cluster rows. Sequencing prioritised: 4e → 4f → 12f → 10e → 10f → cleanup. | 4, 5, 9, 10, 12 | 0 (plan-only) | unchanged |
| 2026-04-21 | 16 | **Sub-cluster 4e landed — `UserReadRestrictionFilterBackend`**. Added `FilterBackendItem` to `permissions/models.py` (per-row `permission_read` branching on `is_secret` + a `deny_all` group profile). Landed 4 live scenarios in `test_4e_filter_backend.py` driving the real `GET /api/<model>/` endpoint through three `AuthenticatedE2ETestCase` subclasses (regular-user, admin, deny-all profile): **4.13** mixed-row list returns public rows only (iterate + exclude path), **4.16** `?pk_only=true` fast path excludes denied pks and reports the correct `count` (proves bulk-selection can't leak denied ids), **4.17** admin `allow_all` returns every row (no-exclusion branch), **4.18** deny-all profile collapses to zero rows. **4.14 / 4.15** (AuditLog DB-visibility + residual-permission path) are marked `@skip` with documented reasons — need the audit-log fixture + a Keycloak-shaped `request.user_permissions` payload. First run: **4 pass + 2 skip + 0 unexpected failures**. Minor learning: the List endpoint returns a raw list (not a paginated dict) when no `perPage` query-param is set — added a tiny `_rows()` normalizer rather than forcing pagination, so tests match what the frontend actually receives. | 4 | 4 | 4 pass + 2 skip of 6 |
| 2026-04-21 | 17 | **Sub-cluster 4f landed — `PermissionAwareSerializerMixin` machinery.** Scoped to the mixin's **infrastructure contracts** (not field-level denial, which is already BUG-010-xfailed in 4b — those tests cover the customer-visible outcome; 4f covers the mechanics the fix will rely on). Four scenarios in `test_4f_serializer_mixin.py` using `SimpleTestCase` + one `TestCase`: **4.19** `_camel_to_snake` table-driven over 9 shapes (incl. acronyms `URLPath`→`url_path`, already-snake no-op, empty string) — the translation every PATCH depends on; **4.20** `_get_non_editable_fields` contains pk + every `editable=False` column and NOT editable ones (wrong set → false 403s on `id`); **4.21** `add_permission_checks` decorator preserves `__name__` / `__module__` while injecting the mixin (wrong → error traces show a wrapper class); **4.22** `PermissionAwareSerializerMetaclass` auto-injects the mixin on LexModel-backed serializers and leaves plain Django-model serializers untouched. Two synthetic unmanaged models (`FakeNonEditableModel`, `PlainDjangoModel`) + a `MiniLexModel` inline, all `managed=False` — no migration impact. One learning: Django's E023 check rejects model class names starting with `_`, so synthetic models drop the underscore prefix. **4 pass + 0 xfail + 0 unexpected failures** in 0.003s. Cluster 4 remains  (now +10 coverage-driven scenarios across 4e + 4f closing most of the 28.97% → ~70% gap in the read-side filter backend and the 9.33% → substantially higher gap in the write-side mixin). | 4 | 4 | 4 pass of 4 |
| 2026-04-21 | 18 | **Sub-clusters 12f + 10e landed.** Two coverage-gap sub-clusters in one pass, 7 new scenarios. **12f** (M2M + nested-FK write paths) — added `TagItem` + `TaggableItem` (M2M `tags` + nullable FK `primary_tag`) to `serializers/models.py`. Three end-to-end scenarios in `test_12f_write_paths.py` driving real POST/PATCH against the One endpoints: **12.26** POST creates M2M through rows atomically (through-table read back via ORM, not trusting the serializer echo); **12.27** PATCH with a different tag set **replaces** (not merges — guards the frontend`s deselect UX from silently regressing); **12.28** nullable FK lifecycle (attach-on-create → rewire via PATCH → detach to NULL). All 3 pass first run. **10e** (schema introspection) — added `SchemaFKTarget`, `SchemaItem` (one field per interesting type), `SchemaHiddenItem` (`permission_list` returns False) to `api_layer/models.py`. Four scenarios in `test_10e_schema_introspection.py`: **10.11** Django field → API-type mapping table (string/int/float/boolean/date/date_time/json/foreign_key); **10.12** editable/required/default/is_pk flags — **surfaced BUG-015** (CharField reports `required=False` because Django`s `get_default()` returns `""` — frontend/backend disagree on required-ness; documented in-test, not xfailed since the value is stable and the fix needs a framework decision); **10.13** FK exposes `target` pointing at the related model`s `_meta.model_name`; **10.14** `delete_restricted_nodes_from_model_structure` prunes denied models AND collapses folders that become empty (patched `UserContext.from_request` so the test doesn`t need a full middleware-built request). Two learnings: (a) the structure tree`s folder-collapse side-effect deletes Model leaves that carry an empty `children: {}` dict — production tree doesn`t include `children` on leaves, test trees must match; (b) `SimpleTestCase` works well for pure-helper coverage. Session cumulative: 4e + 4f + 12f + 10e = 15 new scenarios, 15 pass + 2 skip + 0 unexpected failures + 1 new bug (BUG-015). | 4, 10, 12 | 7 | 7 pass of 7 |
| 2026-04-21 | 19 | **Sub-cluster 4f extended — `run_validation` end-to-end.** The original 4f (session 17) proved the mixin's structural guarantees with `SimpleTestCase` but never ran the `run_validation` body — the actual customer-facing hook (lines 62–163, the bulk of the uncovered code). Added `TestCluster04f_RunValidation` (extends `E2ETestCase` so `FieldLevelItem` + `ProtectedItem` tables are materialized) with four new scenarios: **4.23** change-detection — PATCH with same value as stored on a denied field (`sensitive_salary=100→100`, `pii_ssn='111-22-3333'→same`) passes validation, so the frontend's "send the whole form back" pattern doesn't false-403; **4.24** changed denied field raises `PermissionDenied` with the field name in the message (`sensitive_salary` for non-superuser); **4.25** `lexReservedMeta` key bypasses the check while a real `public_name` change still goes through; **4.26** create path — regular user POSTing `ProtectedItem` gets `PermissionDenied` (model name in the message); admin passes. Uses `APIRequestFactory` + `req.user_permissions = ()` to exercise the anonymous-scopes branch of `UserContext.from_request`. Cluster 4f dashboard row bumped from 4→8 scenarios. All 8 pass (0 fail, 0 xfail) in 2.7s. One notable learning: the `TestCase` baseline doesn't create `lex_app_fieldlevelitem` / `lex_app_protecteditem` tables — **always extend `E2ETestCase`** when a scenario needs to persist test-project models, even if no HTTP call is involved. | 4 | 4 | 4 pass of 4 |
| 2026-04-21 | 20 | **Sub-cluster 10f landed — global search.** Session 18's log claimed 10e + 12f landed, but its edit to `api_layer/models.py` never actually persisted — both 10e (which imports `SchemaFKTarget` / `SchemaItem` / `SchemaHiddenItem`) and the newly-authored 10f were broken at import time (`ImportError: cannot import name 'SchemaItem'`). Re-added the three models to `api_layer/models.py` with a varied-field `SchemaItem` (one field per Django type for 10.11), a `SchemaFKTarget` (for the FK mapping at 10.13), and a `SchemaHiddenItem` that returns `permission_list=False` (for the pruning proof at 10.14). With the models restored, 10e's 4 scenarios pass and 10f's 4 scenarios also pass: **10.15** query matches a text field and returns the `{data, total}` shape (`hit.model`, `hit.content.description`, `hit.url`) the frontend routes on; **10.15b** zero matches returns the documented `"No match found"` sentinel string — the frontend branches on response type, so a silent contract change here would break the nav bar; **10.16** containers whose `id` is in `EXCLUDED_MODELS` (`user`, `permission`, …) short-circuit *before* `SearchVector` runs, so PII in system rows can never leak through global search; **10.16b** regression gate on `EXCLUDED_TYPES` — if a non-text field type (`FloatField`, `BooleanField`, `IntegerField`, `FileField`, `ForeignKey`) ever slips out, `SearchVector` 500s at query time. The search view depends only on `self.model_collection` + `self.kwargs['query']`, so 10f drives `Search.get` directly with a `SimpleNamespace(all_containers=[...])` stand-in rather than wiring the full process-admin URL — keeps the scenario fast and avoids coupling to unrelated plumbing. `UserPermission` is patched open; the exclusion-list constants are asserted independently so a drift produces a named failure instead of a runtime error. Full Cluster 10 run: 22 tests, 19 pass + 1 skip + 2 xfail. | 10 | 4 (+retroactive-fix for 10e) | 8 pass of 8 (10e + 10f) |
| 2026-04-21 | 21 | **Sub-cluster 5.11 landed — History fallback-snapshot path.** Retargeted the plan entry during implementation: the original "5.11 = as_of × permission intersection" scenario would have required either a fresh end-to-end fixture or reopening a model container, whereas the **actual** uncovered lines at `History.py` 180–201 are the *fallback manual serialization* branch inside `_get_snapshot(serializer_class=None)` — a branch the existing 5c tests cannot hit because every test-project LexModel registers a default serializer, so the view always short-circuits at line 176. New `test_5_11_fallback_snapshot.py` uses `SimpleTestCase` + a dynamically-built fake history class (real `type()` call — Python forbids setting `__class__` to a `SimpleNamespace` so the first iteration failed with `TypeError: __class__ must be set to a class`; switched to `type("FakeHistoryRecord", (), {"_meta": _Meta})` and it passed immediately). One scenario, five named sub-assertions: **(a)** every `CONTROL_FIELDS` entry (`history_id`, `valid_from`, `sys_from`, `meta_task_status`, `history_user_id`, `history_type`) is filtered out even when populated, so the frontend never sees bitemporal plumbing inside the business snapshot; **(b)** `datetime` → `isoformat()`; **(c)** `date` → `isoformat()`; **(d)** a non-JSON-safe object (`_CustomValue` with `__str__ → "<custom-value>"`) is coerced via `str()` so DRF's encoder doesn't crash at render time; **(e)** primitives (`int` / `bool` / `None`) and containers (`list` / `dict`) pass through unchanged. All five branches exercise in a single test (each sub-assertion carries a message that names the failing branch, so a regression surfaces the exact drift rather than a generic `AssertionError`). Pass in 0.001s. Full history cluster run: 11 tests, 0 fail. | 5 | 1 | 1 pass of 1 |
| 2026-04-21 | 22 | **Sub-cluster 9.7–9.10 landed — bitemporal suppression guards.** Retargeted during implementation: the original plan items named a `suspend_bitemporal_chaining` context manager and a "signal carries correct `(sender, instance, chain_hint)`" scenario, but the **actual** code has three `ContextVar`-backed guards — `suppress_main_table_sync` (consulted at line 274), `suppress_history_valid_to_chaining` (consulted at line 118), and `suppress_meta_sys_to_chaining` (consulted by the Level-2 meta-chaining handler) — and the signal payload is whatever Django's signal plumbing delivers, not a framework-owned hint. Retargeted all four scenarios to the guard contract the handlers actually rely on. New `test_9_7_suppression_guards.py` (`SimpleTestCase`, pure Python, no DB, 1ms total): **9.7** each guard flips `True` on enter and resets `False` on exit for all three variables — a leaked `True` across request boundaries is how the BUG-011 chaining bottleneck would compound; **9.8** nested suspension stacks — inner `with` exit does NOT deactivate the outer context, which is exactly what the handlers depend on when they themselves wrap internal `record.save(...)` calls in `with suppress_*():` to avoid unbounded recursion; **9.9** the three guards are orthogonal — suspending `main_table_sync` must not silently suspend `valid_to_chaining` or `meta_sys_to_chaining`, because `on_history_saved__sync_main_table` enters only one of them and a cross-contaminating impl would silently disable chain maintenance; **9.10** suspension is thread-local — a background thread launched while the parent thread holds a suspension sees `False`, guaranteeing a Celery worker running parallel calculations cannot silently share suspension state. Each assertion message names the failure mode in production terms ("signal handler's internal save re-triggers the very signal it's inside, causing unbounded recursion") so a drift produces an actionable diagnostic, not a generic `AssertionError`. Full Cluster 9 run: 6 tests, 0 fail, 1s. | 9 | 4 | 4 pass of 4 |
| 2026-04-21 | 23 | **Cluster 14 dashboard sync + 14e extension + BUG-016 surfaced.** Investigating "Cluster 14 — AG Grid Query Endpoint" discovered the 20-scenario baseline (14a/14b/14c) was actually landed in an earlier session but the progress-dashboard row had never been bumped from "Not started" — all 20 were green on re-run. Corrected the dashboard row and then extended with **14e** (4 scenarios + 1 xfail) to close the long tail of `_build_filter_q` branches the baseline didn't reach: **14.21** text ops (`startsWith` / `endsWith` / `equals` / `notEqual` / `notContains`) via one `subTest` per op so a regression names the failing branch; **14.22** number ops (`lessThan` / `lessThanOrEqual` / `greaterThanOrEqual` / `notEqual` / `inRange` + date `blank` which works correctly); **14.23** legacy `condition1` / `condition2` shape for both AND and OR (older AG Grid clients still emit this — endpoint must serve both shapes from one deploy); **14.24** multi-field CSV ordering `-amount,name` + proof that unknown tokens are silently dropped instead of 500ing the grid. **14.25 (xfail)** documents **BUG-016**: AG Grid `blank` / `notBlank` ops are silently unreachable — the text branch's early-return `if value in (None, ""): return None` fires before the per-op dispatch; the number branch's `if value in (None, "") and operation_type != "blank"` omits `notBlank` from the bypass; the date branch's `if operation_type != "blank" and not date_from_raw` does the same. Customer symptom: clicking the "Blank"/"Not Blank" option in the grid's filter header does nothing — every row is returned. The date branch's `blank` special-case already shows the fix pattern (pre-bypass, then dispatch). When the framework is fixed, the xfail on 14.25 drops naturally and the test passes. Full Cluster 14 run: **25 tests, 24 pass + 1 xfail** in 14s. | 14 | 4 (+retroactive dashboard sync for 20 baseline) | 24 pass + 1 xfail of 25; BUG-016 added |
| 2026-04-21 | 24 | **Sub-cluster 7g landed — `CalculatedModel.create()` pipeline end-to-end.** Target was `CalculatedModelMixin.py` — 550 stmts at **33.74%** baseline after 7a–7f, the largest single uncovered file in the framework. Discovery during setup: `CalculationModel` extends `LexModel` only — NOT `CalculatedModelMixin` — so the existing Cluster 7 test models never reach the `create()` pipeline at all. Added a new dedicated model `CombinatorialCalc(CalculatedModelMixin)` with `defining_fields = ["region", "category"]`, `parallelizable_fields = ["region"]`, and a `get_selected_key_list(field_name)` that returns class-level key rings so tests can toggle the combinatorial surface per-scenario. Six scenarios in `test_7g_create_pipeline.py` — each drives one `Model.create(...)` call that walks `_generate_model_combinations` → `_prepare_models_for_processing` → `_create_processing_clusters` → `_dispatch_model_processing` (sync branch, CI default) → `calc_and_save_sync` → `delete_models_with_same_defining_fields` all in one path: **7.25** cartesian expansion (3 regions × 2 categories = 6 rows, all persisted with `name` set by `calculate()`); **7.26** `create()` with no kwargs falls back to `get_selected_key_list` for every field; **7.27** partial failure — one region's `calculate()` raises, `calc_and_save_sync` catches the `CalculatedModelError`, accumulates into `errors`, and keeps going; successful rows persist, failed rows do NOT (save comes *after* calculate in the sync loop); the "processed_count > 0" warning branch fires without re-raising; **7.28** idempotent rerun — `delete_models_with_same_defining_fields` detects existing rows on the second `create()` call, pk set unchanged between runs; **7.29** empty-key-list pruning — if `get_selected_key_list("region")` returns `[]`, the whole combinatorial branch is pruned, zero rows, no exception; **7.30** pk-reset branch — direct call to `delete_models_with_same_defining_fields` on an un-saved instance carrying a stale pk returns `self` with pk reset to `None` so the caller can INSERT. **Coverage delta: `CalculatedModelMixin.py` 33.74% → 64.75% (+31 pts, +170 lines) on 6 new scenarios.** Full Cluster 7 run: 28 tests, 0 fail, ~14s. | 7 | 6 | 6 pass of 6; coverage +31 pts on the largest-gap file |
| 2026-04-22 | 25 | **Showcase CI landed + new scenario 1.6b.** Business requirement: deploy a second CI workflow that runs **two hand-picked tests** from `lex/test_project/` and renders a plain-English pass/fail summary non-engineers can read. Added new **scenario 1.6b** (`test_1_6b_init_runs_full_pipeline`) in `test_1b_lex_init.py` because the existing 1.6 only asserted *one* of the four pieces the business wanted covered (`migrate OR makemigrations`), whereas the stakeholder ask is "Init must detect model changes → makemigrations → migrate → sync to Keycloak, all in one run". 1.6b patches `MigrationAutodetector`, `MigrationLoader`, `ProjectState.from_apps`, `KeycloakSyncManager`, and `call_command` at their import sites (never inside the command) and — after running `cmd.handle(dry_run=False, no_makemigrations=False)` with `check_unapplied_migrations` forced to `True` so `execute_migrations` actually reaches the `migrate` call — asserts all four customer-visible steps fired: (a) `autodetector.changes` was called; (b) `call_command("makemigrations", …)` was invoked; (c) `call_command("migrate", …)` was invoked; (d) `KeycloakSyncManager.process_model_changes` was invoked. Cluster 1 stays  Complete, scenario count 22 → 23. **CI plumbing**: new workflow `.github/workflows/showcase_tests.yml` runs the two showcase tests (1.6b init + 2.1 crud-create) as separate steps with `continue-on-error: true`, then invokes a new helper `.github/scripts/showcase_summary.py` that writes a rich markdown block to `$GITHUB_STEP_SUMMARY` — title + overall verdict + 2-row results table ("Capability / Result / What it proves") + "What the red checks mean" narrative on failure + collapsible `<details>` block with dotted test ids for engineers. Triggers: `workflow_dispatch` (stakeholders click "Run workflow") + push on `lex-app-v2` (keeps the latest completed run fresh); deliberately no `pull_request` trigger — PR gating is `django_tests.yml`'s job. The final step re-raises if either showcase failed so the run badge still goes red. Business labels chosen generic (not project-specific): "Project initialization — migrations & access sync" + "Create record via REST API". Stakeholder-facing doc added at `docs/ci-cd/showcase-ci.md` covering what the workflow is, what each capability proves, when it runs, how to read the summary, how to run it on demand, and what to do when a check is red. Local verification: both tests pass in 0.798s together; summary script smoke-tested in `success success` and `failure success` modes, markdown renders cleanly in both. | 1 (infra) | 1 (new scenario 1.6b) + CI workflow + summary script + stakeholder doc | 2 pass of 2 (showcase set) |
| 2026-04-22 | 26 | **Showcase report upgraded to customer-grade HTML + PDF + SendGrid email.** Scoping shift: the markdown step-summary from Session 25 was a fine engineer view but it isn't what a business user / customer reads — they read inboxes. This session built a proper customer-facing artefact pipeline and wired it in. New `build_showcase_report.py` renders a **branded, single-page Platform Health Report** — deep-navy header band, coloured verdict panel (green ✓ / red ✗), one card per capability with plain-English "what this proves" / "what it means if broken" paragraphs, runtime per test, a "what we test and why" glossary, and a traceability footer (build id / branch / run link). The HTML is 1998-style table-layout with inline CSS so it renders identically in Gmail, Outlook and Apple Mail; the same HTML is rendered to PDF via **WeasyPrint** (`libpango-1.0-0` + `libpangoft2-1.0-0` added to the CI apt step alongside the existing `libcairo2-dev` — pango is the only new system dep). New `send_showcase_email.py` uses the SendGrid SDK to send the HTML body with the PDF attached as `platform-health-report-<date>.pdf`; subject line adapts to the verdict (`[✓ all green]` / `[✗ action required]`); gracefully no-ops (exit 0 with a clear log line) when any of `SENDGRID_API_KEY` / `SHOWCASE_REPORT_RECIPIENTS` / `SHOWCASE_REPORT_FROM` is missing, so the pipeline can ship before the secrets land without failing the workflow. Workflow rewritten: per-test duration captured via `date +%s.%N` bookends (`awk` computes the diff, written to `$GITHUB_OUTPUT`) so the report can show "Runtime: 450 ms" per card; `actions/upload-artifact@v4` uploads both `report.html` + `report.pdf` as the `platform-health-report` artefact; the engineer `showcase_summary.py` step is kept but reverted to file-only output (removed the stdout echo + banner from session 25's last turn — the user's explicit ask was to keep the step log quiet and push the real output to the report). Brand defaults to `"LEX Platform"` but is overrideable via a `SHOWCASE_BRAND` repo variable. Stakeholder doc at `docs/ci-cd/showcase-ci.md` rewritten to describe the new flow (HTML email + PDF attachment + run artefact) and list required secrets. Local verification: HTML renders with correctly-nested quotes (one CSS quoting bug surfaced on first render — multi-word font names wrapped in `"..."` broke the outer `style="..."` attribute; switched to single-quote font names throughout); WeasyPrint installs cleanly on local (PDF 1.7, ~14 KB); email script dry-run with no secrets prints `"Skipping email: missing required env vars: SENDGRID_API_KEY, SHOWCASE_REPORT_RECIPIENTS, SHOWCASE_REPORT_FROM. The HTML + PDF artefact has still been produced."` and exits 0. YAML validates. | 1 (infra) | 0 new test scenarios; 2 new scripts (`build_showcase_report.py`, `send_showcase_email.py`) + workflow rewrite + stakeholder doc rewrite | HTML + PDF + email pipeline green end-to-end locally; CI will activate on next push once SendGrid secrets are set |
| 2026-04-22 | 27 | **Showcase report — dual-template rewrite (branded + scalable).** Three focused upgrades to the report pipeline, all at the builder layer — no change to the CI contract or secrets list. (1) **Brand**: palette rebased on the Excellence Cloud logo — primary navy `#283067` (hexagon) and accent teal `#24b6bb` (the "=" band + "CLOUD" wordmark), with a 4-px teal divider between the navy header and the body. Default brand string flipped from `"LEX Platform"` to `"Excellence Cloud"`; `SHOWCASE_BRAND` repo-variable override unchanged. The header embeds `images/dark-lex-logo.svg` via **cairosvg** (new dep, added to the CI pip install alongside weasyprint/sendgrid) — SVG is rasterised to a 520-px-wide PNG, base64-encoded, and inlined as a data URI so Outlook and corporate gateways that strip `<svg>` still render the wordmark; falls back to a text wordmark if cairosvg is unavailable or the file is missing. WeasyPrint picks the PNG up for the PDF from the same data URI, giving byte-identical rendering. (2) **"What we test and why" → PDF only**: per user's explicit instruction, the glossary section is removed from the email body entirely and lives only in the PDF. The email footer carries a one-line hint ("The attached PDF contains the full report including the 'What we test and why' glossary.") so the reader knows where to find it. (3) **Scalable email layout**: the email was one fat card per test, which would explode on the inbox if we ever add more tests. Split into two functions — `render_email_html` (compact) and `render_pdf_html` (detailed). Email strategy: **failures first as expanded cards** under a red "Needs attention (N)" heading, then **passes as one-line rows** under a green "Working (N)" heading — 14px per pass, not a 200px card. Verdict band now shows a counts line ("2 of 2 capabilities passing") so scannable at a glance. PDF keeps the detailed per-test card + glossary. Both layouts share the header band / verdict band / footer, so the brand is consistent. Local verification: 2 tests all-green → email 14.6 KB, PDF 24.6 KB; 1 failure → email 15.9 KB (tiny bump from the expanded failure card), PDF 24.9 KB. Logo base64 embed confirmed (1 data URI in both). Glossary appears in PDF only — email's sole "What we test and why" occurrence is the footer hint pointing at the PDF (grep-verified, line 129). CI YAML updated to `pip install "weasyprint>=61" "cairosvg>=2.7" "sendgrid>=6.11"`. | 1 (infra) | 0 new test scenarios; `build_showcase_report.py` rewritten into a dual-template module + workflow pip-install bumped + stakeholder doc updated | Email & PDF both render correctly with logo and new palette in both all-green and 1-failure scenarios |
| 2026-04-22 | 28 | **Showcase report — CID logo + tabular layout.** Two user-reported issues. (1) **Logo invisible in delivered email**: Session 27 used a base64 `data:image/png` URI which WeasyPrint renders fine for PDFs but which SendGrid and most major email clients (Gmail, Outlook, corporate relays) strip from `<img src>` to prevent tracking/spam — so the wordmark rendered in the PDF but came through empty in the actual inbox. Confirmed the two excellence-cloud.com data URIs the user pulled (light + dark mode) match the committed SVGs at `images/lex-logo.svg` / `images/dark-lex-logo.svg` — the dark-mode white-text wordmark is the right pick for our navy header, no asset change needed. **Fix**: dual-source the logo. The builder now emits a separate `logo.png` alongside `report.html` / `report.pdf`; the email HTML uses `<img src="cid:logo">` (a Content-ID reference that points at an inline MIME attachment); the sender attaches `logo.png` via SendGrid's `Attachment(..., Disposition("inline"))` + `ContentId("logo")` so the `cid:` reference resolves at the recipient's end. The PDF continues to embed the PNG as a `data:` URI (PDFs don't have CIDs). Email size dropped 14.6 KB → 8.9 KB as a side-effect because the logo bytes no longer inline in the HTML body. (2) **Both layouts redesigned as proper data tables**: the previous "card per test" / "one-line row per pass" split worked but didn't feel like an executive report. Collapsed both into a **single three-column table** — `Status | Capability | Runtime` — with an uppercase-letterspaced header row tinted in the brand background and subtle alternating-row zebra striping (`#fafbfd` on odds). Each failing row gets a `#fff5f6` red tint plus a secondary full-width row directly underneath carrying the "what it means if broken" paragraph in `#b71c1c`, so the eye is dragged straight to the problem without breaking the scan pattern. PDF variant extends the expansion to every row (success → "what this proves", failure → "what it means if broken") and still renders the "What we test and why" glossary below the table — unchanged from Session 27. Infra updates: CI workflow `Build customer report` step passes `--out-logo logo.png`, the `Upload report artefact` step uploads all three (`report.html` + `report.pdf` + `logo.png`), the `Send customer email` step passes `--logo-path logo.png --logo-cid logo` through to the sender. Default brand on both build + send steps flipped from `"LEX Platform"` to `"Excellence Cloud"`. Local verification: `cairosvg` rasterises to 5,791-byte PNG (verified file sizes); email HTML `<img src>` extracted is literally `cid:logo`; PDF HTML contains `data:image/png;base64` and zero `cid:` — so the CID reference is correctly scoped to the email template only. Both all-green and 1-failure runs produce clean output; YAML still validates. | 1 (infra) | 0 new test scenarios; `build_showcase_report.py` + `send_showcase_email.py` rewritten; workflow updated to carry the logo through the artefact + send pipeline | Email logo rendering fixed (CID inline attachment); both layouts converted to tabular; ready for the next run on `lex-app-v2` |
| 2026-04-23 | 29 | **Cluster 12 + Cluster 13 closed; Cluster 7h async-dispatch sub-cluster landed.** Task triple from the user: (1) finish the 6 remaining Cluster-12 scenarios; (2) finish the 3 remaining Cluster-13 scenarios; (3) add a Cluster-7 sub-cluster driving the `CalculatedModelMixin._dispatch_model_processing` Celery branch that 7g's sync path never reached. **Cluster 12** — discovered on entry that the previously-logged session-18 "12f write paths landed" was in fact silently failing to import (`TagItem` / `TaggableItem` referenced but never added to `serializers/models.py`) so 12f's 3 scenarios had been counting as passing on the dashboard while erroring at collection time. Added `TagItem`, `TaggableItem`, and a new `EditScopedItem` (for scenario 12.8's `permission_edit → allow_fields({"x"})` fixture) to `serializers/models.py`. Also disambiguated a scenario-number collision: `test-clusters.md` 12e (factory contract) is canonical for 12.26/12.27/12.28; the 12f write-path scenarios were renumbered 12.26–28 → **12.29–31**. New `test_12a_history_meta_scopes.py` lands **12.6** (history-row GET unwraps to the main model's `permission_read` — `ProtectedWideItem` secret fields must NOT leak through the history endpoint's `snapshot` payload), **12.7** (MetaHistorical instance's `lex_reserved_scopes` is locked at `{"edit": [], "delete": False, "export": False}` regardless of caller; located the MetaHistorical class by walking `django.apps.get_models()` for the `MetaHistorical*` subclass tied to the history model, because the framework registers `meta_history` as a dynamic reverse relation rather than a declared field), and **12.8** (non-admin GET of `EditScopedItem` returns `lex_reserved_scopes.edit == ["x"]` — exactly the one field `permission_edit` allows). New `test_12e_factory.py` lands **12.26** (`model2serializer` always injects `id_field` / `short_description` / `lex_reserved_scopes` in `Meta.fields` — asserted both for the default call and for a narrow-fields caller), **12.27** (`_wrap_custom_serializer` preserves a developer's `Meta.fields = ["name", "amount"]` AND appends the framework internals AND leaves the class inheriting from `LexSerializer` so the row-level permission filter stays active), and **12.28 (xfail)** documenting **BUG-017** — `get_serializer_map_for_model` rebuilds the serializer class on every call with no in-process cache, so the identity assertion fails until the memoization is added. Cluster 12 dashboard: 18 pass + 4 xfail → **24 pass + 4 xfail + 0 unexpected failures** ( Complete). **Cluster 13** — `test_13c_grouped_selected.py` lands **13.9** (AG `selection.groupKeyPaths` with two paths — `[{"field": "category", "key": "<alpha-pk>"}]` and `[{"field": "category", "key": "null"}]` — exercises both the integer-FK string→int coercion branch of `_coerce_group_key` AND the `"null"` → `__isnull=True` sentinel branch; asserts the selected sheet is strictly smaller than the empty-selection baseline, and that exactly 2 group rows land instead of 3 when alpha + null are selected and beta is excluded). `test_13d_auth_edge.py` lands **13.11** (fresh `APIClient` not `force_login`'d POSTs export → 401/403; no streaming body — `HasAPIKey | IsAuthenticated` gate holds) and **13.12** (monkey-patches `ExportMaskedItem.permission_export` so row A returns `allow_fields({"id", "name"})` and row B additionally allows `{"amount"}` — heterogeneous per-row results force `_compute_uniform_export_mask → None` and the slow `filter_and_mask_data_for_export` per-row loop; asserts `amount` is blank for row A and populated for row B in the resulting sheet, which is only possible if the per-row mask actually consulted the per-object `permission_export`). Cluster 13 dashboard: 10 pass + 2 not-started → **12 pass + 0 xfail + 0 unexpected failures** ( Complete). **Cluster 7h** — discovered on entry that 7g had the same silent-import bug as 12f — `CombinatorialCalc` referenced but never defined in `calculations/models.py`, so 7g's 6 "passing" scenarios had been erroring at collection time all along. Added `CombinatorialCalc(CalculatedModelMixin)` with `defining_fields = ["region", "category"]`, `parallelizable_fields = ["region"]`, class-level mutable key rings (`_region_keys` / `_category_keys` / `fail_for_region`), a `get_selected_key_list(key)` that reads those rings, and a `calculate()` that sets `self.name = f"{region}-{category}"` and raises on `fail_for_region` match. With the fixture restored, 7g's 6 scenarios actually execute and pass. New `test_7h_celery_dispatch.py` lands **7.31** (`CELERY_ACTIVE=true` + stub `.delay` attached to `calculate` + `CeleryTaskDispatcher.dispatch_calculation_groups` mocked → `create()` routes through the async branch at lines ~1606-1665 of `CalculatedModelMixin.py` that 7g's sync path never reached; asserts the dispatcher was called with 2 flattened groups totalling 4 models — 2 regions × 2 categories grouped by the `region` parallelizable field — and that zero rows persisted, proving the sync fallback did NOT fire). Full regression across clusters 12 + 13 + 7: **82 tests**, 1 pre-existing unrelated failure in `test_13e_streaming_fast_export.test_13_17_end_row_limit_caps_written_rows` (confirmed pre-existing by git-stash test), 6 xfails (5 prior + 1 new = BUG-017). Net: **+12 passing scenarios** (12.6 / 12.7 / 12.8 + 12.26 / 12.27 + 13.9 / 13.11 / 13.12 + 7.31 + retroactive unblock of 12.29–31 and 7.25–30 that were silently failing to import). | 7, 12, 13 | 10 (3 new in 12a history/meta + 3 new in 12e factory including 1 xfail + 1 new in 13c + 2 new in 13d + 1 new in 7h) + fixture restores unblocking 9 pre-existing scenarios (12f 3 + 7g 6) | Cluster 12  / Cluster 13  / Cluster 7h ; BUG-017 added for `get_serializer_map_for_model` no-cache |
| 2026-04-24 | 31 | **Sub-cluster 8g landed — `lex/lex_app/celery_tasks.py` customer-visible surface.** Target was the 958-line Celery plumbing module (`CallbackTask`, `CeleryCalculationContext`, `FireAndForget` / `WaitForTasks`, `EnhancedBoundTaskMethod`, `lex_shared_task` decorator) — 21.5% baseline, the largest dark surface among production-critical files after 7g/7h. **No Redis required** across all 9 scenarios, by design: (a) `CELERY_ACTIVE=true` branches driven by `patch.dict(os.environ, ...)` so the framework never opens a broker connection; (b) `.delay(...)` return values patched onto fake tasks to return `MagicMock` stand-ins for `AsyncResult`; (c) `allow_join_result` swapped for `contextlib.nullcontext` on the `WaitForTasks` join path so the blocking seam executes end-to-end without talking to anyone; (d) `CallbackTask` instantiated directly with real ORM + real signals — Celery itself never runs, no task registration, no broker lookup. New `test_8g_task_infrastructure.py` in `lex/test_project/tests/celery_async/` with four `TestCase` classes matching the mental-model of the file: **TestCluster08g_CallbackTask** lands **8.7** (`on_success` → `is_calculated=SUCCESS` via direct `.update()` — not `.save()`, so the stale-snapshot branch of `_persist_status_fields` cannot overwrite unrelated columns; `ensure_terminal_calculation_audit` spy called with `audit_status="success"`), **8.8** (`on_failure` → `is_calculated=ERROR` + audit `audit_status="failure"` + exception message + stack-trace string forwarded verbatim from `einfo`), and **8.9** (`initial_data_upload` task short-circuits both callbacks — regression gate against the opt-out ever being silently removed, proves no `.update()` / no audit with nonsense args). **TestCluster08g_CeleryContext** lands **8.10** (`CeleryCalculationContext(context=<incoming>)` stamps the worker-side `operation_context` — calculation_id preserved, `celery_task=True`, `task_name="calc_and_save"`, fresh `operation_id` minted — and restores the prior token on exit; the seam that lets `CalculationLog.log()` inside a worker reach back to the originating calc row) and **8.11** (`CeleryCalculationContext(context=None, ...)` is a safe no-op — `operation_context` is NOT clobbered, matching the sync-mode bypass in `lex_shared_task`). **TestCluster08g_DispatchRouting** lands the three-branch priority hierarchy — **8.12** (no FF/WFT scope, CELERY_ACTIVE=true → runs sync, `.delay` NOT called, task body invoked with `(instance, *args, **kwargs)`); **8.13** (FireAndForget **wins** over an enclosing WaitForTasks — `.delay` called, result lands on the FF scope not the WFT scope; the sync body did NOT run); **8.14** (WaitForTasks alone → `.delay` called, result registered on WFT, scope exit calls `.get()` on the fake `AsyncResult` with `allow_join_result` patched to `nullcontext`; `dispatched_results` cleared after the blocking join). **TestCluster08g_LexSharedTaskWrapper** lands **8.15** (`lex_shared_task` strips `context`/`model_context` from kwargs that reach the inner function, enters `CeleryCalculationContext` when truthy context is supplied, returns the `(inner_result, args)` tuple shape `CallbackTask._extract_model_instances` depends on — a regression in this tuple would silently stop callbacks from seeing their model instances). Implementation learnings: (a) the audit spy must patch `lex.lex_app.celery_tasks.ensure_terminal_calculation_audit` at the import site, not the source module, because `celery_tasks.py` does `from … import ensure_terminal_calculation_audit` at module load — patching too late misses the binding; added `e2e_unpatch = {"ensure_terminal_calculation_audit"}` to opt out of the default mock in those three scenarios. (b) `WaitForTasks.__init__` snapshots `_celery_is_active()` once at construction time, so tests force `_active=True` explicitly instead of depending on env-var timing inside the `with` block. (c) `lex_shared_task`'s wrapper only pops `model_context` when it is truthy — passing `model_context=None` leaks the kwarg through to the inner body, causing `unexpected keyword argument`; scenario 8.15 exercises the context-only branch by omitting `model_context` entirely. **Coverage delta: `celery_tasks.py` 21.5% → 46% (+24.5 pts, +128 lines) on 9 new scenarios.** Full Cluster 8 run: **13 tests (8a 4 + 8g 9), 0 fail, 0 xfail, ~14s.** Cluster 8g dashboard row already tracked at the 9/9/9 target state. Remaining dark code on the file (46% → ~100%): `load_data` / `calc_and_save` / `activate_history_version` task bodies (need real Django fixtures with STORAGE_TYPE branching), `is_celery_worker_process` / `is_running_in_celery` env-detection variants, `print_context_state`, `register_task_with_context`, `respect_fire_and_forget` decorator — all candidates for a follow-up 8h sub-cluster. | 8 | 9 | 9 pass of 9; coverage +24.5 pts on `celery_tasks.py` |
| 2026-04-24 | 32 | **Sub-clusters 1g + 1h landed — `init.py` admin-API & bootstrap surfaces.** User flagged four uncovered areas in `lex/lex_app/management/commands/init.py`: `KeycloakSyncManager.get_all_django_models`, `get_client_roles`, `ensure_client_role_policies`, and the bootstrap flow. All four were testable without Keycloak / Redis / network by extending the `_make_sync_manager()` pattern from 1e (bypass `__init__`, stub `kc_manager` as `MagicMock`) and patching `init_module.requests.get` + `init_module.apps.get_app_configs` + `init_module.settings` at the import site. **Sub-cluster 1g** (`test_1g_sync_manager_admin.py`) — 10 scenarios split across three classes: **TestCluster01g_GetAllDjangoModels** (1.44 concrete models across apps, 1.44b abstract/proxy skipped, 1.44c KEYCLOAK_SYNC_EXCLUDED rules honoured for legacy_data + audit tables + Historical*/MetaHistorical* prefixes, 1.44d `settings.repo_name` scopes to single app); **TestCluster01g_GetClientRoles** (1.45 IGNORED_CLIENT_ROLES dropped + no create call when defaults present, 1.45b missing defaults created via `create_client_role` with `clientRole=True` then re-fetched, 1.45c fail-fast when re-fetch returns no `id`); **TestCluster01g_EnsureClientRolePolicies** (1.46 one `Policy - <role>` per managed role in canonical order with UUID JSON, 1.46b existing drifted policy normalized in place — wrong type/logic/strategy rewritten, legacy top-level `roles` stripped, stale `config.roles` refreshed, no duplicate, 1.46c extra customer role gets `Policy - hr` ordered after defaults). **Sub-cluster 1h** (`test_1h_bootstrap_flow.py`) — 15 scenarios across four classes: **TestCluster01h_BuildControllerUrl** (1.47 URL carries state / callback / flow=keycloak-client-bootstrap / project, trailing slash stripped from base; 1.47b missing base raises); **TestCluster01h_FetchBootstrapState** (1.48 done+payload returns payload, 1.48b cancelled returns `{"cancelled": True}` sentinel, 1.48c pending returns None, 1.48d non-2xx raises, 1.48e non-JSON body raises — critical because silent None on non-JSON would be indistinguishable from pending and produce an infinite wait); **TestCluster01h_PollBootstrapStatus** (1.49 loop yields payload after pending responses, 1.49b cancelled raises, 1.49c done-without-payload raises, 1.49d timeout raises — patched `time.time` + zero `time.sleep` for sub-second runs); **TestCluster01h_WaitForKeycloakSetupV2** (1.50 env-already-set fast path returns env dict without touching controller, 1.50b controller-cancelled raises, 1.50c controller-payload returned as-is, 1.50d timeout raises). All 25 scenarios pass in **8 ms total** (network-free, DB-free, broker-free). Full run against `lex.test_project.tests.init.test_1g_sync_manager_admin` + `lex.test_project.tests.init.test_1h_bootstrap_flow`: **25 tests, 0 fail, 0 xfail, 0 skip**. Suite total 205 → 230 scenarios, 186 → 211 implemented, 159 → 184 passing. | 1 | 25 | 25 pass of 25; +25 scenarios on init.py admin + bootstrap surface |
| 2026-04-24 | 33 | **Sub-cluster 8h landed — Celery eager-mode end-to-end (broker-free real dispatch).** User flagged that the `CELERY_ACTIVE=true` production code paths in `celery_tasks.py`, `CeleryTaskDispatcher`, and `CalculatedModelMixin._dispatch_model_processing` were **never exercised end-to-end** — 8g had proven the routing logic with `.delay` patched to a MagicMock but nothing ever ran the real `.delay()` → `@lex_shared_task` wrapper → task body → `CallbackTask.on_success`/`on_failure` sequence, and 7h had mocked `CeleryTaskDispatcher.dispatch_calculation_groups` rather than running it. This sub-cluster closes that gap without Redis / RabbitMQ / a running worker by using **Celery's own test harness** — `app.conf.task_always_eager=True` + `task_eager_propagates` — which makes `.delay(...)` run the task body **inline in the calling thread** and return a real `EagerResult`. The broker is faked by Celery itself, not by us; every other moving part (the `@lex_shared_task` wrapper, the `CallbackTask` base class, the `task_postrun` signal, the ORM writes, the audit seam) is the real production code. New `test_8h_eager_end_to_end.py` with a shared `_celery_eager(propagate=True)` context manager that flips the three knobs and restores them on exit. Six scenarios across five classes: **TestCluster08h_DelayRoundTrip** — **8.16** (`CeleryCalc.calculate.delay(instance)` in eager mode → Celery executes the wrapped body → real `CallbackTask.on_success` flips `is_calculated=SUCCESS` on the persisted row via direct `.update()` + `ensure_terminal_calculation_audit` fires with `audit_status="success"`; asserts `result.ready()` + `result.successful()`); **8.17** (`should_fail=True` + `propagate=False` → `EagerResult.failed()` is True, `result.result` carries the `RuntimeError`, real `CallbackTask.on_failure` fires → row flipped to ERROR, audit logged with `audit_status="failure"` and the exception message; learning: with `task_eager_propagates=True` the exception re-raises inline *before* Celery's trace machinery calls on_failure, so the failure scenario must use `propagate=False` to exercise the callback path — documented in the fixture docstring). **TestCluster08h_EnhancedBoundRoundTrip** — **8.18** (inside a `WaitForTasks` scope, `EnhancedBoundTaskMethod` routes to real `.delay()` → eager execution → `EagerResult` registered on the WFT scope → scope exit drains it via `wait_for_completion` → row reaches SUCCESS; closest thing to a production dispatch we can simulate in-process). **TestCluster08h_DispatcherEndToEnd** — **8.19** (real `CeleryTaskDispatcher.dispatch_calculation_groups` with 2 groups of 3 total models → dispatcher imports `calc_and_save`, calls `.delay(group, group_index, ..., context=..., model_context=...)` on each group, each task runs inline, every row saved with `is_calculated=SUCCESS`; learning: the dispatcher dereferences `context['request_obj']` at line 218 so tests **must** pass a context dict — previously undocumented invariant, now captured in-test). **TestCluster08h_IsCeleryWorkerProcess** — **8.20** (`is_celery_worker_process()` returns False from the main thread but True inside an eager task body, because Celery's `current_task` is set identically in both eager and remote modes — proves the recursive-dispatch guard works without actually spinning a worker process). **TestCluster08h_RegisterTaskWithContext** — **8.21** (`register_task_with_context(result)` routes to the FireAndForget scope when both FF and WFT are active — FF priority, documented contract — result does NOT land on the outer WFT). **Coverage delta: `celery_tasks.py` 46% → 55% (+9 pts on top of 8g's +24.5); `CeleryTaskDispatcher.py` ~0% → 45% (+45 pts — first real coverage, 199 stmts with 110 newly covered).** Full Cluster 8 run: 8a 4 + 8g 9 + 8h 6 = **19 tests, 0 fail, 0 xfail, ~19s**. Strategic note for a follow-up 8i: this pattern generalises to any production-dispatch test. For scenarios that genuinely need broker/pickle fidelity (e.g. catching kwargs that don't JSON-serialise), Celery ships `celery.contrib.testing.worker.start_worker` + `memory://` broker — heavier setup, slower, but same code exercised. Not needed yet — no customer bug currently blames pickle-serialization on dispatch. Suite total 230 → 236 scenarios, 211 → 217 implemented, 184 → 190 passing. | 8 | 6 | 6 pass of 6; `celery_tasks.py` +9 pts, `CeleryTaskDispatcher.py` +45 pts first real coverage |
| 2026-04-24 | 34 | **Sub-cluster 8i landed — `WaitForTasks` / `FireAndForget` scope contracts.** User flagged that FF and WFT deserved "heavy" coverage across the nested + filter + no-op scenarios documented in `docs/lex_topics/12-celery-async-dispatch.md` — 8g/8h had only exercised the headline priority case (FF wins over WFT). This sub-cluster closes every other branch in the 60-line ContextVar state machine: nesting, filters, no-op pass-through, and exception propagation. Nine scenarios across six classes in `test_8i_scope_contracts.py`, split between real eager execution (for anything observing `is_calculated` after a real task body ran) and ContextVar inspection with a fake task (for pure routing decisions made by `EnhancedBoundTaskMethod.__call__` before `.delay` would ever fire). **TestCluster08i_FFInsideWFT** — **8.22** (FF nested inside WFT, all three scenarios: parent-A / FF / parent-B — the FF result lands on the inner FF scope, the two parents land on the outer WFT, and at no point does the FF result appear on the outer WFT's `dispatched_results`; after exit every row reaches SUCCESS because eager ran them all inline). **TestCluster08i_NestedWFT** — **8.23** (outer WFT wrapping an inner WFT — inner exit drains only the inner's own results; outer's result survives the inner scope boundary and is drained on outer exit; neither WFT steals the other's result; the entire composability contract in one test). **TestCluster08i_WFTFilters** — **8.24** (`WaitForTasks(include_tasks={"compute_a"})` — only `compute_a` `.delay`'d, `compute_b` runs sync; uses fake tasks + MagicMock `.delay` so the routing decision is assertable without eager execution) + **8.25** (`WaitForTasks(exclude_tasks={"compute_b"})` — excluded name stays sync inside an otherwise-async block; mirror of 8.24 for the blacklist branch). **TestCluster08i_FFFilters** — **8.26** (`FireAndForget(force_tasks={"email"})` inside a surrounding WFT — only `email` is FF-forced; `compute_nav` falls through to the WFT scope and gets `.delay`'d there; proves `force_tasks` is a scope-selective filter, not a blanket FF). **TestCluster08i_NoOpWhenCeleryInactive** — **8.27** (with `CELERY_ACTIVE` unset, entering WFT **and** FF is a pure pass-through: `tasks_context` stack stays empty, `unblock_tasks_context` stack stays empty, `.delay` is never called, task body returns its sync result; this is the "your code doesn't change" promise from the docs). **TestCluster08i_ExceptionPropagation** — **8.28** (a `should_fail=True` task dispatched inside WFT with `propagate=False` — Celery's trace invokes `on_failure` first, then WFT's scope-exit `wait_for_completion` calls `.get()` which re-raises the original `RuntimeError`; the exception surfaces at the outer `with` boundary verbatim — a silent failure here would be a data-loss bug; fixture learning: we create the row with default `NOT_CALCULATED` instead of passing `is_calculated=IN_PROGRESS` at create time, because the AFTER_CREATE `calculate_hook` would otherwise fire the failing calc synchronously at `.save()` time before we enter the eager scope). **TestCluster08i_MultipleDispatchesInWFT** — **8.29** (three rows dispatched in one WFT block; scope captures all three on `dispatched_results`; exit drains every one; all three rows land at SUCCESS — proves WFT scales to batch dispatch, not just the single-task case). **TestCluster08i_FFDoesNotBlockOnExit** — **8.30** (FF `__exit__` does **not** drain `dispatched_results` — that is the entire behavioural difference from WFT, and it must be explicit: only a subsequent `wait_for_completion()` call drains; asserted with MagicMock `.delay` so we can observe `.get()` being called exactly once per result after the explicit drain). Full Cluster 8 run: **8a 4 + 8g 9 + 8h 6 + 8i 9 = 28 tests, 0 fail, 0 xfail, ~13s**. Suite total 236 → 245 scenarios, 217 → 226 implemented, 190 → 199 passing. Cluster 8 now has genuinely **exhaustive** coverage of the customer-facing Celery primitives — every documented contract in `12-celery-async-dispatch.md` is gated by a test. | 8 | 9 | 9 pass of 9; exhaustive WFT/FF scope-contract coverage per docs/lex_topics/12-celery-async-dispatch.md |
| 2026-04-24 | 35 | **Plan / progress reconciliation — docs pulled back in line with the implementation.** Walked the full 245-scenario plan against the on-disk test files and found three sub-clusters where the plan's narrative had drifted from what actually shipped during Sessions 17 / 18 / 19 / 20 / 23 / 29. (The dashboard was also stale — it said 226 implemented + 19 not started, but every plan scenario is actually covered; the 19 gap was counting the original plan text for 4f / 10e / 12f scenarios that were intentionally reshaped during implementation and never updated here.) Fixes landed in this session: **(1) 4f Serializer-level masking** — `test-clusters.md` rewritten to describe what 17+19 actually shipped (machinery class: `_camel_to_snake` / `_get_non_editable_fields` / `add_permission_checks` decorator / `PermissionAwareSerializerMetaclass` → 4.19–4.22; `run_validation` class: change-detection / per-field deny / `lexReservedMeta` bypass / create-path `permission_create` → 4.23–4.26) instead of the original "read-mask drops denied fields / list-detail-many consistency / nested FK redaction / allow_fields-vs-allow_all_except equivalence" plan. The machinery + `run_validation` coverage is stronger than the original read-mask plan because the read-mask outcome is already gated by cluster 4b (BUG-010 xfail) and 12a/12c (which assert it at the serializer JSON boundary from the customer's side). **(2) 10e Schema introspection** — rewritten to describe the four scenarios 18+20 shipped (`create_field_info` type mapping / editable-required-default-pk flags / FK `target` / `delete_restricted_nodes_from_model_structure` pruning) instead of the original "full structure-tree + widget hints + permission-aware schema" plan. Widget hints (`XLSXField` / `PDFField`) were scoped out during implementation because the `XLSXField` subclass-check is already covered by `lex/tests/unit/api/test_xlsx_field.py` and `PDFField` does not exist as a named class in the framework — the original plan item was speculative. The permission-aware schema surface is covered by the same pruning helper that 10.14 drives. **(3) 12f Serializer write paths** — plan updated to reflect the renumbering 12.26–12.28 → **12.29–12.31** that was applied in Session 29 when the canonical 12e factory-contract scenarios (12.26 / 12.27 / 12.28) were added to `test-clusters.md`. Scenario content also rewritten: "M2M POST / PATCH replaces / nested-FK inline create" → "M2M through-table atomic create / PATCH replaces / nullable FK attach-rewire-detach lifecycle" — matching the actual tests in `test_12f_write_paths.py` (the inline-nested-FK-create scenario was scoped out because the framework does not support it: nested FK writes require a pk-only payload). All three sub-clusters stay  Complete — coverage is equivalent or better than the original plan text, the plan is just now honest about what's there. **Dashboard Total row updated**: 245 planned / **245 implemented** / **222 passing** / 22 xfail / **0 not started**. The 222-passing number rolls the 3 "read-mask" plan scenarios that were reconceived into 4f machinery (4.19–4.22, all passing) and the 1 plan scenario "widget hints" that was dropped (no longer counted against passing). Also reconciled the on-disk test count vs. the scenario count: `grep -c "def test_"` reports **428 test methods**, because several scenarios drive multiple fixture classes (e.g. 4e's filter-backend scenarios run against regular-user / admin / deny-all profiles, each a separate `TestCase` class) — the dashboard "Implemented" column counts scenarios, not method bodies. No test files added, no test files removed, no scenario semantics changed — this is a pure doc pass that closes the gap between plan / dashboard / on-disk reality so future sessions can rely on the documents again. Environment note: during verification an IntegrityError on `django_content_type(app_label=audit_logging, model=auditlog)` surfaced when running `--keepdb`; reproducing without `--keepdb` (fresh test database) ran the Cluster 3 validation suite cleanly (9/9 pass in 4.3s). The `--keepdb` failure is a stale-database hygiene issue unrelated to any framework or test drift. | 4, 10, 12 | 0 (doc reconciliation only) | 245 scenarios implemented; dashboard + plan now consistent |
| 2026-04-24 | 30 | **Diagnosed pre-existing red (13.17) → tracked as BUG-018; no framework fix applied per user directive.** Scenario `test_13_17_end_row_limit_caps_written_rows` in `test_13e_streaming_fast_export.py` asserts the documented contract: AG payload `endRow=2` against a 5-row DB must cap the streamed `.xlsx` at 2 rows. Actual: sheet carries all 5. Root-caused to `ModelExportView._normalize_ag_request` (`lex/api/views/file_operations/ModelExport.py` line ~151), which unconditionally overwrites `request_payload["endRow"] = MAX_AG_EXPORT_ROWS` (1,000,000) **before** the streaming path at `_try_stream_universal_fast_export` reads it. The per-row cap loop at line ~1229 (`if written >= end_row: break`) is therefore dead code for every call — `end_row` is always 1M. Side finding: scenario 13.7 ("`endRow` over `MAX_AG_EXPORT_ROWS` → clamped silently") is green only because the same line happens to clamp that direction too; the upward-clamp behaviour is correct but the implementation is the wrong mechanism — it clobbers rather than `min()`s. **BUG-018** added: every "export only the first N rows" affordance in the UI silently falls through to a full-table dump. Fix is a 1-line change to clamp upward while preserving user values (`request_payload["endRow"] = max(1, min(int(raw or MAX), MAX))`). Fix deferred per user directive — documenting as BUG-018 and leaving 13.17 as the one pre-existing red until a dedicated session. Cluster 13 dashboard unchanged (13.17 sits in `test_13e_streaming_fast_export.py` which tests the streaming fast path, not the 12-scenario main sub-cluster count). | 13 | 0 (diagnosis + BUG-018 entry only) | BUG-018 documented; 13.17 remains the sole unexpected red in the suite |
| 2026-04-24 | 36 | **Sub-cluster 1i landed — initial-data upload journey end-to-end.** User directive (Cluster B in the A/B/C gap bucket): drive `InitialDataAuditLogger.py` (148 stmts, **12.64%** baseline) + the `ProcessAdminTestCase` seed walker that Cluster 1c never reached. 1c only asserts post-`Init` DB state — it doesn't touch the JSON walker (tag:/datetime: prefix resolvers, subprocess flattening), doesn't exercise the audit-logger create/update/delete methods, and never drives the end-to-end action dispatcher. New `test_1i_initial_data_journey.py` in `lex/test_project/tests/init/` lands 14 scenarios across 5 classes: **TestCluster01i_ReplaceTaggedParameters** (`SimpleTestCase`, no DB) — **1.51a** `tag:foo` resolves to the previously-stored in-memory object (FK-by-reference mechanism; non-prefixed strings and non-string values pass through untouched — the resolver must not mangle literals); **1.51b** `datetime:YYYY-MM-DD` parses through `dateutil.parser` into a real `datetime` (seed files ship dates as strings; models need `datetime` instances). **TestCluster01i_SubprocessFlattening** (`SimpleTestCase`) — **1.52** `{"subprocess": "path"}` entries flatten recursively, preserving the declared parent-file order AND each sub-file's internal order (parent-before-child FK resolution depends on this); patched `PROJECT_ROOT` env var to point at the `lex/` root so the fixture-relative subprocess path `test_project/tests/fixtures/…` resolves. **TestCluster01i_AuditLoggerCRUD** (`E2ETestCase` with `e2e_models = [SimpleItem]`, `e2e_framework_models = [AuditLog, AuditLogStatus]`) — **1.54** `log_object_creation` writes AuditLog + pending AuditLogStatus with `author = "system (initial_data_upload)"` and `_audit_tag` in payload for trace-back; **1.54b** `_logged_ids` tracks every log in declaration order (finalize_batch's sweep is scoped by this list — must not touch earlier sessions); **1.55** `log_object_update` embeds the target instance's pk in payload (compliance view links audit row → DB row); **1.56** `log_object_deletion` snapshots the instance via `generic_instance_payload` (deleted row's fields survive in the audit trail after the main row is gone — payload `value` is stringified by `_serialize_payload` for JSON stability, so the assertion compares `str(...)`); **1.57** `mark_operation_success` flips status to `success`, `mark_operation_failure` to `failure` with error_traceback populated; both idempotent via queryset `.update()` — calling twice does not duplicate status rows; **1.57b** mark_operation_* on `None` audit log is a no-op (the loader passes whatever `log_object_creation` returned, which can be `None` on failure — must not raise). **TestCluster01i_AuditLoggerFinalize** — **1.58a** clean run → `finalize_batch()` with no error resolves lingering pending → success; summary reports `pending_resolved` count (non-zero signals a handler forgot to mark); **1.58b** `finalize_batch(failure_error=…)` collapses pending → failure with the error string on every swept row (outer-driver exception path — audit trail closes out completely even for aborted runs). **TestCluster01i_FullSeedJourney** — **1.59** full create/update/delete drive with audit enabled (env-gated on `INITIAL_DATA_AUDIT_LOGGING`) asserts DB has the correct surviving row AND 4 AuditLog rows in `[create, create, update, delete]` order AND all statuses `success`; **1.59b** full journey without audit still lands the right DB state (2 created − 1 deleted = 1 surviving row with `value=999` from the update — audit is observability, disabling it cannot regress data transitions); **1.60** crash-path simulation — 3 pending ops then `finalize_batch(failure_error=…)` sweeps every row to `failure` with the error string (`pending_resolved == 3`; `len(_logged_ids) == 3`). Harness trick: `ProcessAdminTestCase` is itself a `unittest.TestCase`, so instantiating it as a fixture needs a `runTest = lambda self: None` placeholder; `get_test_data` is monkey-patched to read from our fixture path; `apps.get_app_config(settings.repo_name)` is stubbed (the test project's app label isn't registered as an installed app in the sandbox) so the harness sees `SimpleItem` via `models.values()`. Fixtures all pre-existed: `seed_parent.json`, `seed_child_01.json`, `seed_child_02.json` (subprocess test), `test_seed_journey.json` (full journey). First-run result: **14 tests, 13 pass + 1 env-gated skip, 0 fail, ~5.4s**. Suite total 245 → 259 scenarios; passing 222 → 235. | 1 | 14 | 13 pass + 1 env-gated skip of 14; closes 1i and Bucket-B of the April-24 coverage push |
| 2026-04-24 | 37 | **Sub-cluster 8j landed — Celery task bodies end-to-end (Bucket C).** User directive (Cluster C in the A/B/C gap bucket): cover the three `@lex_shared_task`-decorated task bodies in `lex/lex_app/celery_tasks.py` lines 696–957 — `load_data` (the `initial_data_upload` task), `calc_and_save` (the per-model batch loop every `CalculatedModelMixin.create()` dispatch eventually runs), and `activate_history_version` (the Celery Beat bitemporal-activation task). 8g/8h/8i covered the infrastructure (CallbackTask, CeleryCalculationContext, EnhancedBoundTaskMethod, WaitForTasks/FireAndForget scope contracts) but the task bodies themselves — 150+ lines of customer-visible work — were dark. **Broker-free by design**: `@lex_shared_task` returns an `EnhancedTaskMethodDescriptor` whose `__call__` routes to `self.task(...)` (the underlying celery shared_task shim) when `CELERY_ACTIVE` is unset — which invokes the real body synchronously in the calling thread via `lex_shared_task`'s `wrapper` function. No Redis, no RabbitMQ, no worker process; the wrapper returns `(inner_result, args)` and we assert on side effects. New `test_8j_task_bodies.py` in `lex/test_project/tests/celery_async/` with 14 scenarios across 3 classes: **TestCluster08j_LoadData** (`SimpleTestCase`, fake harness) — **8.31** `initial_data_load` falsy → early-return no-op (neither `setUp` nor `setUpCloudStorage` called, audit factory not invoked — the "customer never configured initial data" gate); **8.32** LEGACY + `is_running_in_celery=True` → `test.setUp(audit_logger)` direct, `asyncio.run` NOT called, `finalize_batch(failure_error=None)`, `test.test_path` stamped from the `initial_data_load` value; **8.33** LEGACY + not-in-Celery → `asyncio.run` invoked exactly once (bridges through `sync_to_async`), `setUpCloudStorage` untouched; **8.34** non-LEGACY + `CELERY_ACTIVE=true` → `setUpCloudStorage(models, audit_logger)` direct, `setUp` never called; **8.35** setUp raises → original `RuntimeError` propagates (identity-preserved via `assertIs ctx.exception is boom` — framework must not swallow or rewrap because `CallbackTask.on_failure` depends on the original type), `finalize_batch` fires in `finally` with `failure_error` kwarg embedding `"RuntimeError: seed crashed"` + traceback; **8.36** `audit_logger` is `None` (audit disabled or import failed) → `finally` guard `if audit_logger:` prevents `AttributeError`, clean exit, `setUp(None)` called. **TestCluster08j_CalcAndSave** (`E2ETestCase` with `CelerySyncCalc`) — **8.37** 2-row happy path → both saved, summary `{total:2, processed:2, conflicts:0, errors:0}` (bookkeeping drift here would silently skip rows); **8.38** `IntegrityError` on first save → conflict resolver: `delete_models_with_same_defining_fields` returns the pre-existing row, task rewires current-instance pk to the existing pk, retry save succeeds (now an UPDATE not an INSERT), summary `conflicts_resolved:1` + `processed_successfully:1`; driven by `patch.object(CelerySyncCalc, "save", flaky_save)` that raises once then delegates to real save — MagicMock on `delete_models_with_same_defining_fields` as an instance attribute (so `hasattr(model, ...)` returns True); **8.39** `calculate()` raises non-IntegrityError → re-raised verbatim (`CallbackTask.on_failure` depends on it), row not persisted. **TestCluster08j_ActivateHistoryVersion** (`SimpleTestCase` with mocked `apps.get_model`) — **8.40** LookupError on model resolution → returns `"failed_model_lookup"` (stale Beat schedule against renamed/removed model, must not crash worker); **8.41** history row `DoesNotExist` (race: user deletes while Beat has activation queued) → returns `"skipped_missing_record"` — built by attaching a custom `_HDN(Exception)` class to `fake_model.history.model.DoesNotExist` and making `.objects.get` raise it; **8.42** `valid_from` 1 hour in future → returns `"failed_too_early"`, `BitemporalSynchronizer.sync_record_for_model` NOT called (the 5-second clock-drift guard short-circuits before sync); **8.43** valid due record → `sync_record_for_model(fake_model, 42, history_model)` fires with exact args, `MetaModel.objects.filter(history_object_id=7, meta_task_status="SCHEDULED").update(meta_task_status="DONE")` observed via MagicMock chain, returns `"success"`; **8.44** sync blows up → `RuntimeError` propagates (not silently swallowed — Beat retries/alerts, silent None would look like a dead task and hide real bitemporal drift). One implementation learning: `_create_audit_logger_for_task` and `should_load_data` are imported **inside** `load_data`'s function body (not at module top), so patching must target `lex.lex_app.apps._create_audit_logger_for_task` (the source module) not the celery_tasks binding. Full run: **14 tests, 0 fail, 0 skip, 1.5s**. Suite total 259 → 273 scenarios; passing 235 → 249. Closes Cluster-A/B/C Bucket C (the April-24 coverage push targeting the 52% → 70% baseline gap). | 8 | 14 | 14 pass of 14; closes Bucket-C with end-to-end coverage of `celery_tasks.py` lines 696–957 |
| 2026-04-25 | 38 | **Sub-clusters 1j / 1k / 1l landed — Keycloak client safety pre-flight (mocked + real-Keycloak integration).** Customer-facing risk closed: `lex init` will now refuse to mutate a non-DEVELOPMENT or non-confidential Keycloak client. Without this gate, an operator who points the framework at a STANDARD/production client by accident silently rewrites its resources, policies, and permissions — the very accident that motivated the controller's `is_confidential` + `client_type="DEVELOPMENT"` invariants in the first place. Three new test files + one framework feature: **(framework, `lex/lex_app/management/commands/init.py`)** added module-level constant `KEYCLOAK_DEV_REDIRECT_HOST = "localhost"` and helper `_redirect_uris_indicate_development(uris)` which uses `urllib.parse.urlparse(...).hostname` (case-insensitive `== "localhost"`) so look-alike hosts (`localhost.example.com`) and loopback IPs (`127.0.0.1`) do NOT count — non-string entries are skipped silently. Added `KeycloakSyncManager.verify_client_is_safe_for_init()` which fetches the client representation via `admin.get_client(client_uuid)` and raises `CommandError` (chained on the underlying exception when relevant) for any of: empty `client_uuid`, network failure, non-dict response, missing `publicClient` flag, `publicClient is True`, missing/malformed/empty `redirectUris`, or no localhost redirect. Returns the rep on success so callers can avoid a second round-trip. Added `--skip-client-preflight` CLI flag + wired the call into `Command.handle` immediately after `sync_manager = KeycloakSyncManager()` and before any sync work — when `--skip-client-preflight` is set the command writes "Skipping Keycloak client safety pre-flight (--skip-client-preflight is set)." to stdout and bypasses the gate. **(test 1j — `test_1j_client_preflight.py`)** 20 scenarios across 6 classes, all `TestCase` with mocked `kc_manager`: **TestCluster01j_HappyPath** (1.71 confidential + localhost passes; 1.72 `localhost:8000` works; 1.73 only-localhost is also valid); **TestCluster01j_ConfidentialCheck** (1.74 `publicClient=true` raises with "confidential" + clientId in message; 1.75 missing `publicClient` flag is treated as unsafe — never assume confidential by default); **TestCluster01j_DevelopmentCheck** (1.76 prod-only redirect raises; 1.77 empty list raises with `<empty>` sentinel; 1.78 missing `redirectUris` field raises; 1.79 string-instead-of-list raises; 1.80 `localhost.example.com` does NOT match — proves we use parsed `.hostname`, not substring); **TestCluster01j_RedirectHostParser** (1.81–1.85 direct unit tests on `_redirect_uris_indicate_development` covering known dev shapes incl. case-insensitivity, rejection of 127.0.0.1 / look-alikes / non-URLs, non-string entries skipped, None/empty list returns False, and a pin on the `KEYCLOAK_DEV_REDIRECT_HOST` constant so any change is deliberate); **TestCluster01j_ErrorHandling** (1.86 missing `client_uuid` raises BEFORE any HTTP call — `admin.get_client.assert_not_called()`; 1.87 `admin.get_client` raising → `CommandError` wraps + `__cause__` chains to the original; 1.88 non-dict response shape raises); **TestCluster01j_CommandWiring** (1.89 a failing pre-flight aborts `init` BEFORE `process_model_changes` is called; 1.90 `--skip-client-preflight` short-circuits — `admin.get_client.assert_not_called()` AND stdout contains "--skip-client-preflight is set" AND `process_model_changes.assert_called_once()`). All 20 pass in 0.007s. **(test 1k — `test_1k_client_preflight_integration.py`)** 4 real-Keycloak scenarios (env-gated skip — pass only when `LEX_KEYCLOAK_INTEGRATION` is truthy AND `KEYCLOAK_URL` / `OIDC_RP_CLIENT_UUID` / credentials resolve). Drives `verify_client_is_safe_for_init` against a live admin REST API via the real `KeycloakManager` SDK (no mocks): asserts confidential + DEVELOPMENT contracts hold for the configured client and round-trip the rep verbatim. **(test 1l — `test_1l_full_pipeline_integration.py`)** 7 real-Keycloak scenarios (same env-gating) driving the full `lex init` pipeline against a live server — asserts the admin REST API actually accepts the payloads `KeycloakSyncManager` builds, end-to-end timing works (token refresh, multi-call sequences, no race against the authz-import endpoint), and `last_authz_import_error` round-trips to `None` on success. In the local sandbox both 1k + 1l skip cleanly (11 skipped, 0 fail). **CI behaviour:** 1j gates every push (mocked, no external deps); 1k/1l only run when the integration env is wired — the sandbox documents how to enable them. Suite total 273 → 304 scenarios (20 + 4 + 7); passing 249 → 269 (20 1j pass; 1k/1l skip in sandbox). | 1 | 31 (20 mocked + 11 env-gated integration) | 20 pass + 11 env-gated skip; framework-side `verify_client_is_safe_for_init` + `--skip-client-preflight` shipped |
| 2026-04-25 | 39 | **Initial-data tests consolidated under `tests/init/initial_data/`.** Pure folder reorganization — no test logic changed, no scenarios added or removed. Moved the three files that exercise the customer-facing `INITIAL_DATA` seed-loading pipeline into a dedicated subfolder so the relationship is obvious from the directory tree alone: `test_1c_initial_data.py` (seed-data parse contract — JSON shape + `lex_config.py` declarations), `test_1f_seed_idempotency.py` (idempotency gate — `load_data` "all-or-nothing" guard + declaration-order preservation), `test_1i_initial_data_journey.py` (full upload journey — seed walker + `InitialDataAuditLogger` CRUD + batch finalize + create/update/delete arc with audit on/off). Added `__init__.py` documenting the grouping. Sub-cluster letters (1c / 1f / 1i) and scenario numbers (1.17–1.22, 1.18b/c, 1.20b, 1.51 / 1.51b, 1.52, 1.54–1.60) preserved verbatim — Django's test discovery picks up the new path automatically, no CI label change needed. | 1 | 0 (move-only) | unchanged |
| 2026-04-25 | 40 | **Sub-cluster 1m landed — `lex` CLI ↔ PyCharm `.run.xml` cross-file contract.** Closes the long-standing drift risk between `generate_pycharm_configs.py` (declares 16 `.run/*.run.xml` files an operator can click in PyCharm) and `lex/bin/lex.py` (declares the actual CLI subcommands). 1a covered three of the sixteen files; the per-builder helpers in `lex/tests/unit/cli/test_lex_cli.py` validate Celery / Flower / MCP individually; nothing asserted "every PyCharm-clickable subcommand actually resolves through the CLI." A rename in either file would silently break a PyCharm action — caught only by a developer trying to use it. New `test_1m_cli_commands.py` (`SimpleTestCase`-only, fast, in-process, no subprocess) lands 8 scenarios across 5 classes: **TestCluster01m_RunFileSetParity** (1.102 — scaffolding produces exactly the 16 expected `.run.xml` files, no orphans, no missing — canary against "I removed Test_Audit but the constant still has it" drift); **TestCluster01m_RunFileScriptName** (1.103 — every `SCRIPT_NAME` is `lex` so a copy-paste of a different binary cannot bypass our env handling / Django bootstrap); **TestCluster01m_RunFileSubcommandResolution** (1.104 — every first-token of `PARAMETERS` resolves to either an explicit `@lex.command(...)` Click handler or a registered Django management command via `_install_dynamic_commands`; uses `shlex.split` to handle quoted-prompt prefixes; this is the cross-file contract that nothing else asserts); **TestCluster01m_ExplicitClickCommands** (1.105 — pins the explicit Click registry: `celery` / `celery-workers` / `flower` / `streamlit` / `start` / `setup` / `setup-with-ai` / `ai-update` / `ai-faq` — removing one is what would silently break a `.run.xml`; 1.106 — `_SKIP_BOOTSTRAP_COMMANDS` allow-list is a subset of the explicit registry, otherwise listed names silently fall through to dynamic forwarding); **TestCluster01m_HelpInvocability** (1.107 — `lex --help` exits 0 and names every explicit command; 1.108 — `lex <cmd> --help` exits 0 for every explicit handler via Click's in-process `CliRunner` — catches decorator/signature regressions that `--help` surfaces but real runs would mask); **TestCluster01m_DjangoSideContract** (1.109 — every Django-side subcommand a `.run.xml` references — `init` / `migrate` / `makemigrations` / `flush` / `test` / `create_db` — is registered with Django's command loader; otherwise the dynamic-forwarding fall-through to `execute_from_command_line` produces a less-helpful error). Implementation note: the test scaffolds `.run/` into a `TemporaryDirectory` per scenario so the live project's `.run/` (which the user has stagged-modified files in) is not touched. All 8 pass in 0.020s. Suite total 304 → 312 scenarios; passing 269 → 277. | 1 | 8 | 8 pass of 8; cross-file CLI ↔ PyCharm contract now gated |
| 2026-04-27 | 41 | **Final plan/test consistency pass + Many endpoint contract reconciliation.** Product contract clarified: `POST /many/` bulk create and `PATCH /many/` bulk update are not supported. Replaced the stale intent-driven bulk-create/bulk-patch scenarios with supported coverage: Cluster 2e now gates selected-id bulk DELETE (delete selected rows, preserve unselected rows, ignore stale ids safely); Cluster 10.7 now drives Many DELETE through the API layer; Cluster 11.10 now stress-checks bulk DELETE at volume; Cluster 12.22 now asserts read-only Many GET selected rows use the same serializer shape as list/detail. Removed unsupported bulk POST/PATCH expectations from `test-clusters.md` and retired BUG-006 in the tracker because the missing POST endpoint is no longer a bug. Also ran a plan-vs-test scenario-id audit and closed the remaining numbering gaps by adding explicit 8.5/8.6 dispatch-context tests, 9.1/9.2/9.3/9.6 state-store/broadcast tests, renaming 1.51a/1.58a methods to match the plan, and splitting the export plan's 13.2 row into implemented 13.2a/13.2b sub-scenarios. | 1, 2, 8, 9, 10, 11, 12, 13 | 6 added consistency tests + 0 net Many scenarios (semantics corrected) | targeted bulk/API/serializer/stress validation green; CRUD/API/query clusters green with 8 expected failures; new 8/9 consistency tests green |
| 2026-04-27 | 42 | **Sub-cluster 7i landed — exhaustive calculation parent/child atomicity matrix.** Added `NonAtomicChildCalc` plus four matrix parent fixtures (`AtomicParentAtomicChildMatrixCalc`, `AtomicParentNonAtomicChildMatrixCalc`, `NonAtomicParentAtomicChildMatrixCalc`, `NonAtomicParentNonAtomicChildMatrixCalc`) with independent `parent_should_fail` / `child_should_fail` toggles. New `test_7i_atomicity_matrix.py` covers all 16 combinations: parent atomicity by child atomicity by parent outcome by child outcome. Important branch captured from the user example: an **atomic parent failure after a successful non-atomic child** rolls back that child because the child save runs inside the parent transaction, while a non-atomic parent failure leaves a successful child persisted at SUCCESS. Child failures always persist child ERROR and propagate parent ERROR across all four atomicity pairings. Also restored the documented `@unittest.expectedFailure` marker on 7.14 / BUG-009 so the full calculation package is green with one tracked expected failure. | 7 | 16 | 16 pass; full `lex.test_project.tests.calculations` run: 45 tests OK, 1 expected failure |
| | | | | | | |

---

## Quality Gates

Before any release, these must hold:

1. **All clusters  or ** — no cluster in  or  state
2. **Zero unexpected failures** — every failure is either a passing test or an `expectedFailure` with a tracked bug
3. **CI pipeline green** — `lex test lex.test_project.tests --noinput` passes in CI
4. **No overfitting** — no test uses `skip_hooks=True` + `calculate_hook()` (the pattern that works around bugs). No test mocks the class under test. No test was written by reading the implementation instead of the docs.
5. **Every `expectedFailure` is tracked** — must have an entry in [Known Bugs Tracker](#known-bugs-tracker) with severity and cluster
6. **Coverage threshold met** — `COVERAGE_FAIL_UNDER` not decreased

---

> **Back to:** [Test Plan Index](index.md) | **See also:** [Test Clusters](test-clusters.md) | [Expected Results](expected-results.md)
